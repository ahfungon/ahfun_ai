# 总结功能修复总结

## 问题诊断

### 根本原因
Celery Worker 启动时没有指定 `-Q` 参数来监听所有队列，导致只监听了默认的 `default` 队列，而 `summary_jobs` 和 `periodic_tasks` 队列中的任务无法被处理。

### 症状
- 话题 Token 计数达到 83976（远超阈值 2000），但总结任务一直处于 `pending` 状态
- `summary_jobs` 队列中有 1 个任务，但 Worker 没有处理
- Worker 日志显示只监听了 `default` 队列
- 总结任务等待了 2.4 小时仍未执行

### 次要问题
- `summary_prompt` 也存在 JSON 转义问题（与 `scoring_prompt` 相同）
- 数据库中的 prompt 需要重新修复

## 修复方案

### 1. 修复 Worker 队列监听（✅ 完成）

**文件**: `restart_worker_quick.sh`, `restart_worker.sh`

在 Worker 启动命令中添加 `-Q` 参数，明确指定监听所有队列：

```bash
# 修复前
celery -A workers.celery_app worker --loglevel=info --logfile=logs/worker.log

# 修复后
celery -A workers.celery_app worker --loglevel=info --logfile=logs/worker.log -Q default,summary_jobs,periodic_tasks
```

### 2. 重新修复 summary_prompt 转义（✅ 完成）

运行 `fix_prompt_escaping.py` 脚本，确保数据库中的 `summary_prompt` 也正确转义了 JSON 示例。

### 3. 重启 Worker（✅ 完成）

重启 Worker 使修复生效，Worker 现在监听所有三个队列：
- `default` - 评分任务、生成新话题任务
- `summary_jobs` - 总结任务
- `periodic_tasks` - 定期任务（如检查关闭超时）

## 验证结果

### 修复前
```
📊 总结触发阈值: 2000 tokens

📈 活跃话题 Token 计数:
🔥 人活着的意义是什么 | Tokens: 83976 | 消息数: 545 | 待处理: 是

⚠️  有 1 个话题达到或超过阈值

📋 最近 5 个总结任务:
  pending    | 人活着的意义是什么 | 创建: 02-25 00:38  ← 等待 2.4 小时
```

### 修复后
```
📊 总结触发阈值: 2000 tokens

📈 活跃话题 Token 计数:
📝 人活着的意义是什么 | Tokens: 178 | 消息数: 558 | 待处理: 否

✓ 没有话题达到阈值

📋 最近 5 个总结任务:
  done       | 人活着的意义是什么 | 创建: 02-25 00:38  ← 已完成
```

### Worker 日志验证
```
[2026-02-25 11:06:33,740: INFO/MainProcess] Task workers.tasks.process_summary_job[...] received
```

Worker 成功接收并处理了总结任务。

## 测试命令

### 检查总结功能状态
```bash
python3 check_summary_status.py
```

### 检查 Worker 队列监听
```bash
# 查看 Worker 启动日志中的队列配置
tail -100 logs/worker.log | grep -A 10 "queues"
```

### 检查 Redis 队列
```bash
python3 -c "
import redis
from config.settings import settings

r = redis.from_url(settings.redis_url)
queues = ['default', 'summary_jobs', 'periodic_tasks']

for queue in queues:
    length = r.llen(queue)
    print(f'{queue}: {length} 个任务')
"
```

### 手动触发总结任务（测试用）
```bash
python3 -c "
from workers.tasks import process_summary_job
process_summary_job('任务ID')
"
```

## 相关文件

### 修改的文件
- `restart_worker_quick.sh` - 添加 `-Q` 参数
- `restart_worker.sh` - 添加 `-Q` 参数

### 新增的工具脚本
- `check_summary_status.py` - 检查总结功能状态

### 相关配置
- `workers/celery_app.py` - Celery 应用配置（任务路由）
- `workers/tasks.py` - 总结任务实现

## 技术细节

### Celery 队列机制
- Celery 默认只监听 `default` 队列
- 需要使用 `-Q` 参数明确指定要监听的队列列表
- 任务路由在 `celery_app.py` 中配置：
  ```python
  task_routes={
      "workers.tasks.process_summary_job": {"queue": "summary_jobs"},
      "workers.tasks.check_closing_timeouts": {"queue": "periodic_tasks"},
  }
  ```

### 总结触发流程
1. 消息服务在每次添加消息后更新 `token_count_since_summary`
2. 当 Token 计数达到阈值（默认 2000）时，创建 `SummaryJob`
3. 任务被路由到 `summary_jobs` 队列
4. Worker 从队列中取出任务并执行
5. 总结完成后，重置 Token 计数，更新话题摘要和建议

### 总结任务特性
- 使用数据库行锁（SELECT FOR UPDATE）防止并发
- 支持重试机制（最多 5 次，指数退避）
- 失败后会释放 `pending_summary_job` 标志
- 成功后重置 `token_count_since_summary` 为 0

## 后续建议

### 1. 监控总结功能
定期检查总结功能是否正常工作：
```bash
python3 check_summary_status.py
```

### 2. 调整阈值
如果发现总结触发过于频繁或不够频繁，可以在系统配置中调整 `summary_threshold`：
- 当前值：2000 tokens
- 建议范围：1000-10000 tokens

### 3. 检查 Worker 状态
确保 Worker 始终在运行并监听所有队列：
```bash
ps aux | grep "celery.*worker" | grep -v grep
```

### 4. 查看总结历史
检查话题的总结历史记录：
```sql
SELECT * FROM summary_history WHERE topic_id = '话题ID' ORDER BY created_at DESC;
```

## 总结

通过修复 Worker 启动脚本，添加 `-Q` 参数明确指定监听的队列列表，成功解决了总结功能失效的问题。系统现在可以正常处理总结任务，当话题 Token 计数达到阈值时，会自动触发总结并重置计数器。
