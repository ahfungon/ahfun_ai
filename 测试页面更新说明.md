# 测试页面更新说明

## 问题

用户反馈 `http://localhost:8080/test_minimax.html` 还是不行。

## 原因

这个测试页面还在使用旧的直接调用方式，会遇到 CORS 问题：
```javascript
// 旧代码：直接调用 MiniMax API
const apiResp = await fetch(apiUrl, {
    headers: {
        'Authorization': 'Bearer ' + config.minimax.api_key
    },
    ...
});
```

## 解决方案

更新测试页面使用代理端点：
```javascript
// 新代码：通过代理调用
const apiResp = await fetch('/api/admin/llm/proxy', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({
        provider: 'minimax',
        messages: [...],
        ...
    })
});
```

## 更新内容

### 1. 页面标题
- 旧：MiniMax API 直接测试
- 新：MiniMax 代理端点测试

### 2. 测试按钮
- 新增：测试 MiniMax 代理
- 新增：测试 DeepSeek 代理
- 新增：测试两个 LLM

### 3. 测试逻辑
- 改为调用 `/api/admin/llm/proxy` 端点
- 不再直接暴露 API Key
- 统一的错误处理

## 使用方法

### 访问测试页面
```
http://localhost:8080/test_minimax.html
```

### 测试步骤
1. 点击"测试 MiniMax 代理"按钮
2. 查看测试结果
3. 如果成功，会显示：
   ```
   ✅ MiniMax 代理测试成功！
   ✅ 没有 CORS 问题
   ✅ API Key 安全（不暴露在前端）
   ✅ 可以在智能体模拟器中使用
   ```

### 测试所有 LLM
点击"测试两个 LLM"按钮，会同时测试 DeepSeek 和 MiniMax。

## 预期结果

### 成功情况
```
正在测试 MINIMAX 代理...

步骤 1: 获取配置...
✅ 配置获取成功

=== MINIMAX 配置 ===
{
  "api_key": "sk-cp-cjKQ...",
  "api_url": "https://api.minimax.chat/v1",
  "model": "MiniMax-M2.5",
  "is_configured": true
}

步骤 2: 通过代理调用 MINIMAX API...
代理端点: /api/admin/llm/proxy

HTTP 状态码: 200
响应时间: 1234ms

✅ 代理调用成功！

💬 响应内容:
你好！我是 MiniMax 开发的 AI 助手...

🎉 MINIMAX 代理测试成功！

✅ 没有 CORS 问题
✅ API Key 安全（不暴露在前端）
✅ 可以在智能体模拟器中使用
```

### 失败情况

#### API Key 未配置
```
❌ MINIMAX 未配置 API Key
请在系统配置中设置 API Key
```

**解决方案：**
访问 http://localhost:8080/system-config.html 配置 API Key

#### 后端服务未启动
```
❌ 错误: Failed to fetch

可能的原因：
1. 后端服务未启动
2. 代理端点未正确配置
3. 网络连接问题
```

**解决方案：**
```bash
python main.py
```

## 对比：直接调用 vs 代理调用

### 直接调用（旧方式，会失败）
```javascript
// ❌ 会遇到 CORS 错误
fetch('https://api.minimax.chat/v1/chat/completions', {
    headers: {
        'Authorization': 'Bearer sk-xxx'  // API Key 暴露
    }
})
```

### 代理调用（新方式，成功）
```javascript
// ✅ 没有 CORS 问题
fetch('/api/admin/llm/proxy', {
    body: JSON.stringify({
        provider: 'minimax',  // API Key 在后端
        messages: [...]
    })
})
```

## 技术细节

### 请求流程
```
浏览器 → /api/admin/llm/proxy → MiniMax API
        (同源请求，无 CORS)    (服务器请求，无 CORS)
```

### 安全性
- ✅ API Key 不暴露在浏览器
- ✅ 不会在 Network 标签页看到 API Key
- ✅ 更安全的架构

### 性能
- 代理延迟：< 10ms
- LLM API 响应：1-3 秒
- 总延迟：≈ LLM API 响应时间

## 故障排查

### 问题 1：测试页面显示 404
**原因：** 页面路径错误

**解决：**
确保访问 `http://localhost:8080/test_minimax.html`（不是 `frontend/test_minimax.html`）

### 问题 2：代理返回 400 错误
**原因：** API Key 未配置

**解决：**
1. 访问系统配置页面
2. 设置 MiniMax API Key
3. 保存配置
4. 重启 Worker（如需要）

### 问题 3：代理返回 502 错误
**原因：** MiniMax API 调用失败

**解决：**
1. 检查 API Key 是否正确
2. 检查网络连接
3. 查看后端日志：`tail -f logs/api.log`

## 总结

测试页面已更新为使用代理端点，现在可以正常测试 MiniMax API 了！

**关键改变：**
- ✅ 使用 `/api/admin/llm/proxy` 代理端点
- ✅ 避免 CORS 跨域问题
- ✅ API Key 更安全
- ✅ 支持测试 DeepSeek 和 MiniMax

**下一步：**
如果测试页面成功，智能体模拟器也应该可以正常使用 MiniMax 了。
