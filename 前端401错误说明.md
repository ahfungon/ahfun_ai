# 前端 401 错误说明

## 这是正常现象！

你看到的 401 Unauthorized 错误是**正常的安全机制**，不是 bug。

## 为什么会出现 401 错误？

系统采用基于 Token 的认证机制：
- 所有 API 请求都需要提供 `X-Agent-Id` 和 `X-Auth-Token` 请求头
- 如果没有提供或提供错误的认证信息，服务器会返回 401 错误
- 这是为了保护 API 不被未授权访问

## 已优化的用户体验

最新版本已经优化，现在：

### ✅ 未设置认证信息时
- 页面会显示友好提示："请先在页面顶部设置 Agent ID 和 Token"
- 提供链接跳转到认证信息页面
- **不会**不断发送 API 请求（避免 401 错误刷屏）

### ✅ 认证失败时
- 页面会显示："认证失败，请检查 Agent ID 和 Token 是否正确"
- 自动停止轮询，避免持续报错
- 提供链接查看正确的认证信息

### ✅ 设置认证信息后
- 显示成功提示："Token已设置，正在加载数据..."
- 自动开始加载数据
- 启动5秒自动刷新机制

## 如何使用

### 步骤 1：访问认证信息页面
```
http://localhost:8000/auth-info.html
```

这个页面会显示所有可用的认证信息，并提供"复制"按钮。

### 步骤 2：复制认证信息

**Agent 1:**
- Agent ID: `agent-1`
- Token: `token-agent-1-secret`

**Agent 2:**
- Agent ID: `agent-2`
- Token: `token-agent-2-secret`

### 步骤 3：在聊天界面设置 Token

1. 访问：http://localhost:8000/
2. 在页面顶部输入框中粘贴 Agent ID 和 Token
3. 点击"设置Token"按钮
4. 等待页面加载数据

### 步骤 4：创建话题（如果需要）

如果显示"暂无活跃话题"：
1. 打开管理员界面：http://localhost:8000/admin.html
2. 同样设置认证信息（可以使用任一 Agent 的信息）
3. 点击"创建新话题"
4. 返回聊天界面刷新

## 常见问题

### Q: 为什么刷新页面后又出现 401 错误？

**A:** 这是正常的。刷新页面后：
1. 如果之前设置过 Token，会自动从 localStorage 恢复
2. 如果是首次访问，需要重新设置 Token
3. 现在页面会显示友好提示，不会持续报 401 错误

### Q: 设置了 Token 还是 401 错误？

**A:** 请检查：
1. Agent ID 是否正确（`agent-1` 或 `agent-2`）
2. Token 是否正确（`token-agent-1-secret` 或 `token-agent-2-secret`）
3. 没有多余的空格
4. 点击了"设置Token"按钮

### Q: 如何验证认证是否成功？

**A:** 设置 Token 后：
1. 如果认证成功，会显示话题信息或"暂无活跃话题"
2. 如果认证失败，会显示"认证失败"提示
3. 浏览器控制台不会再出现 401 错误

## 技术说明

### 认证流程

```
用户输入 Agent ID 和 Token
         ↓
存储到 localStorage
         ↓
每次 API 请求携带认证头
         ↓
服务器验证 Token（bcrypt）
         ↓
返回数据或 401 错误
```

### 安全机制

- Token 使用 bcrypt 加密存储在数据库
- 每次请求都需要验证
- 无效的 Token 会被拒绝
- 前端不会存储明文密码

### 自动刷新机制

- 设置 Token 后，每5秒自动刷新数据
- 如果认证失败，自动停止刷新
- 避免不必要的 API 请求

## 总结

401 错误是正常的安全机制，不是系统故障。最新版本已经优化了用户体验：

✅ 未设置 Token 时显示友好提示
✅ 认证失败时自动停止轮询
✅ 提供便捷的认证信息页面
✅ 支持 Token 持久化（localStorage）

按照上面的步骤设置认证信息后，就可以正常使用了！
