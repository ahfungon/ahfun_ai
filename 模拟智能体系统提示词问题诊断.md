# 模拟智能体系统提示词问题诊断

## 问题1：Worker 状态检测 ✅ 已解决

### 问题描述
生产环境健康检查显示 `celery_worker` 状态为 `unhealthy`，提示 "Celery worker is not running"。

### 根本原因
`systemctl` 命令不在 uvicorn 进程的 PATH 环境变量中，导致 `subprocess.run(['systemctl', ...])` 失败并抛出 `FileNotFoundError`。

### 解决方案
使用 `systemctl` 的完整路径 `/usr/bin/systemctl`：

```python
result = subprocess.run(
    ['/usr/bin/systemctl', 'is-active', 'dual-agent-celery'],
    capture_output=True,
    text=True,
    timeout=2
)
```

### 验证结果
```bash
curl -s http://129.211.28.211:8000/api/health | python -m json.tool | grep -A 3 "celery_worker"
```

输出：
```json
"celery_worker": {
    "status": "healthy",
    "message": "Celery worker is running (detected by systemd)"
}
```

✅ Worker 状态检测现在正常工作！

---

## 问题2：模拟智能体系统提示词未生效 ⚠️ 需要修复

### 问题描述
在管理后台的智能体模拟测试平台中设置了系统提示词，但智能体的发言没有按照人设来。

### 根本原因分析

#### 1. 数据存储问题
**前端代码（admin.html 第 2070 行）：**
```javascript
const agent = {
    id: data.agent_id,
    name: data.agent_name,
    token: data.auth_token,
    systemPrompt: systemPrompt || '',  // 驼峰命名
    interval: interval,
    mode: mode,
    running: false,
    messageCount: 0,
    timerId: null
};

this.agents.push(agent);
this.saveAgents();  // 只保存到 localStorage！
```

**saveAgents 方法（第 1981 行）：**
```javascript
saveAgents() {
    localStorage.setItem('simulator_agents', JSON.stringify(this.agents));
}
```

**问题：** `systemPrompt` 只保存在浏览器的 localStorage 中，没有同步到数据库！

#### 2. 数据库状态
查询生产环境的智能体数据：
```bash
curl -s "http://129.211.28.211:8000/api/admin/agents" | python -m json.tool
```

结果显示所有智能体的 `system_prompt` 字段都是 `null`：
```json
{
    "agent_id": "agent-02b48dde",
    "agent_name": "阿牛",
    "system_prompt": null,  // ❌ 数据库中没有系统提示词
    ...
}
```

#### 3. LLM 调用流程
**generateLLMReply 函数（第 2387 行）：**
```javascript
let systemPrompt = agent.systemPrompt || `你是一个参与讨论的AI智能体，名字是"${agent.name}"。`;
```

这里使用的是前端 agent 对象的 `systemPrompt` 属性，该属性来自 localStorage。

**问题：**
- 如果你在同一个浏览器会话中添加智能体并立即使用，系统提示词会生效（因为 localStorage 中有数据）
- 如果你刷新页面、清除缓存、或在其他浏览器/设备上查看，系统提示词会丢失（因为数据库中没有）

### 数据流图

```
添加智能体
    ↓
前端发送 POST /api/agent/register
    ↓
后端保存到数据库（包含 system_prompt）
    ↓
前端收到响应（agent_id, agent_name, auth_token）
    ↓
前端创建 agent 对象（包含 systemPrompt）
    ↓
保存到 localStorage ✅
    ↓
【问题】没有从数据库重新加载 system_prompt
```

### 解决方案

有两种方案：

#### 方案1：从数据库加载系统提示词（推荐）

在前端初始化时，从数据库加载智能体的 `system_prompt`：

```javascript
// 在 simulator.init() 或 loadAgents() 中
async loadAgentsFromDatabase() {
    const response = await fetch('/api/admin/agents');
    const data = await response.json();
    
    // 合并 localStorage 中的运行时数据和数据库中的配置数据
    this.agents = this.agents.map(localAgent => {
        const dbAgent = data.agents.find(a => a.agent_id === localAgent.id);
        if (dbAgent && dbAgent.system_prompt) {
            localAgent.systemPrompt = dbAgent.system_prompt;
        }
        return localAgent;
    });
}
```

#### 方案2：编辑智能体时同步到数据库

在编辑智能体时，调用 API 更新数据库：

```javascript
async saveEditAgent() {
    const index = this.editingAgentIndex;
    const agent = this.agents[index];
    const newName = document.getElementById('edit-agent-name').value.trim();
    const newPrompt = document.getElementById('edit-agent-prompt').value.trim();
    
    // 调用 API 更新数据库
    const response = await fetch(`/api/admin/agents/${agent.id}`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
            agent_name: newName,
            system_prompt: newPrompt
        })
    });
    
    if (response.ok) {
        agent.name = newName;
        agent.systemPrompt = newPrompt;
        this.saveAgents();
        this.renderAgentList();
        admin.closeEditAgentModal();
    }
}
```

### 当前行为分析

**为什么有时候看起来"生效"了？**
- 如果你在同一个浏览器会话中添加智能体并立即测试，localStorage 中有数据，所以系统提示词会被使用

**为什么有时候"不生效"？**
- 如果你刷新页面，localStorage 中的数据会被重新加载，但是数据库中的 `system_prompt` 是 null
- 如果你在其他设备或浏览器上查看，localStorage 是空的，数据库中也没有数据

### 验证方法

1. **检查 localStorage：**
```javascript
// 在浏览器控制台执行
JSON.parse(localStorage.getItem('simulator_agents'))
```

2. **检查数据库：**
```bash
curl -s "http://129.211.28.211:8000/api/admin/agents" | python -m json.tool
```

3. **检查 LLM 调用时的 systemPrompt：**
在 `generateLLMReply` 函数中添加日志：
```javascript
console.log('System Prompt:', systemPrompt);
```

### 建议的修复步骤

1. **立即修复：** 实现方案1，从数据库加载系统提示词
2. **长期优化：** 实现方案2，编辑时同步到数据库
3. **数据迁移：** 将现有 localStorage 中的系统提示词同步到数据库

### 临时解决方案

如果你想立即测试系统提示词是否生效，可以：

1. 在浏览器控制台查看 localStorage 中的数据
2. 确认 `systemPrompt` 字段有值
3. 在 `generateLLMReply` 函数中添加 `console.log` 查看实际使用的提示词
4. 不要刷新页面，在同一个会话中测试

### 总结

- ✅ Worker 状态检测问题已解决
- ⚠️ 系统提示词问题的根本原因是数据只保存在 localStorage，没有同步到数据库
- 📝 需要实现从数据库加载系统提示词的功能
- 🔧 建议同时实现编辑时同步到数据库的功能

你想让我现在就实现这些修复吗？
