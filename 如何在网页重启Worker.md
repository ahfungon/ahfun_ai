# 如何在网页重启 Worker

## 🎯 快速指南

### 问题
> 在网页里修改了 LLM，怎么触发重启 Worker？

### 答案
✅ 直接点击"重启 Worker"按钮！

## 📋 操作步骤

### 1. 打开系统配置页面

```
http://localhost:8080/admin.html
→ 点击"系统配置"
```

### 2. 查看 Worker 状态

页面顶部会显示：
- ✅ Worker 运行中 (1 进程) - 绿色背景
- ❌ Worker 未运行 - 红色背景

### 3. 修改 LLM 配置

例如：
- 切换 LLM 提供商（DeepSeek → MiniMax）
- 修改 API Key
- 修改模型名称

点击"保存"按钮。

### 4. 重启 Worker

1. 点击页面顶部的"重启 Worker"按钮
2. 在确认对话框中点击"确定"
3. 等待 5-10 秒
4. 看到"Worker 重启成功！"提示
5. Worker 状态变为"✅ Worker 运行中"

## 🎨 界面示意

```
┌─────────────────────────────────────────────────────────┐
│ 系统配置                                                 │
│ 管理系统运行参数、Prompt 模板和 LLM 配置                │
│                                                          │
│ [保存所有配置] [刷新] [重启 Worker] ✅ Worker 运行中 (1) │
└─────────────────────────────────────────────────────────┘
```

## ⚡ 什么时候需要重启？

### 需要重启 ✅

- 切换 LLM 提供商（DeepSeek ↔ MiniMax）
- 修改 API Key
- 修改 API URL
- 修改模型名称

### 不需要重启 ❌

- 修改 Prompt（立即生效）
- 修改 Token 阈值（立即生效）

## 💡 提示

1. **自动状态检查**
   - 页面每 30 秒自动检查 Worker 状态
   - 无需手动刷新

2. **重启确认**
   - 点击重启按钮会弹出确认对话框
   - 避免误操作

3. **重启时间**
   - 通常需要 5-10 秒
   - 期间无法处理总结任务

4. **失败处理**
   - 如果自动重启失败
   - 会显示手动重启命令
   - 可以复制命令到终端执行

## 🔍 验证重启成功

### 方法 1：查看状态

重启后，Worker 状态应该显示：
```
✅ Worker 运行中 (1 进程)
```

### 方法 2：查看日志

```bash
tail -f logs/worker.log
```

应该看到：
```
[2024-xx-xx xx:xx:xx] INFO: Worker starting...
[2024-xx-xx xx:xx:xx] INFO: Using LLM provider: MiniMax
```

### 方法 3：测试功能

1. 发送一条消息
2. 触发总结任务
3. 查看是否使用新的 LLM

## ❓ 常见问题

### Q1: 点击重启按钮没反应？

**A**: 检查：
1. 是否有确认对话框弹出？
2. 浏览器控制台是否有错误？
3. 尝试刷新页面后再试

### Q2: 重启失败怎么办？

**A**: 使用手动命令：
```bash
./restart_worker.sh
```

或：
```bash
pkill -f "celery -A workers.celery_app worker"
celery -A workers.celery_app worker --loglevel=info --logfile=logs/worker.log &
```

### Q3: 重启后状态还是"未运行"？

**A**: 
1. 等待 3-5 秒后刷新页面
2. 检查 Worker 日志是否有错误
3. 手动启动 Worker

### Q4: 可以在服务器上使用吗？

**A**: 可以！但需要确保：
1. `restart_worker.sh` 脚本有执行权限
2. 服务器上有 `pkill` 和 `celery` 命令
3. 当前用户有权限管理进程

## 📚 相关文档

- `网页重启Worker功能说明.md` - 详细功能说明
- `系统配置生效时机说明.md` - 配置生效时机
- `配置修改快速参考.md` - 快速参考卡片

## ✨ 总结

现在你可以：
1. ✅ 在网页上查看 Worker 状态
2. ✅ 一键重启 Worker
3. ✅ 无需使用命令行
4. ✅ 自动验证重启结果

非常方便！🎉
