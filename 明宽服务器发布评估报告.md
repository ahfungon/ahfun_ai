# 明宽服务器发布评估报告

生成时间：2026-02-25

## 一、项目概况

### 当前版本信息
- 最新提交：`0dd1857` - 优化总结Prompt：按发言人归纳观点，结构化输出对话脉络与共识分歧
- 分支状态：main 分支，工作区干净
- Python 文件数量：126 个（不含虚拟环境）
- 前端页面：15 个 HTML 文件

### 项目类型
智能体圆桌派（Dual Agent Chat Platform）
- 后端：FastAPI + SQLAlchemy + Celery
- 数据库：PostgreSQL
- 缓存/队列：Redis
- 前端：原生 HTML/JavaScript

---

## 二、数据库变更评估

### ✅ 需要执行数据库迁移

服务器数据库需要升级，包含以下变更：

### 2.1 新增表结构

#### 表1：`message_relevance_scores` (消息评分表)
**用途**：存储消息质量评分和评估意见

```sql
CREATE TABLE message_relevance_scores (
    id VARCHAR(36) PRIMARY KEY,
    message_id VARCHAR(36) NOT NULL UNIQUE,
    topic_id VARCHAR(36) NOT NULL,
    agent_id VARCHAR(36) NOT NULL,
    relevance_score FLOAT NOT NULL CHECK (relevance_score >= 0 AND relevance_score <= 100),
    evaluation_comment TEXT,
    evaluated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (message_id) REFERENCES messages(id) ON DELETE CASCADE,
    FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE
);
```

**索引**：
- `idx_message_scores_message` - 按消息ID查询
- `idx_message_scores_topic` - 按话题ID查询
- `idx_message_scores_agent` - 按智能体ID和时间查询

#### 表2：`system_configs` (系统配置表)
**用途**：存储运行时系统配置（LLM API密钥、Prompt模板等）

```sql
CREATE TABLE system_configs (
    key VARCHAR(100) PRIMARY KEY,
    value TEXT NOT NULL,
    config_type VARCHAR(20) NOT NULL DEFAULT 'text',
    category VARCHAR(50) NOT NULL DEFAULT 'general',
    display_name VARCHAR(200) NOT NULL,
    description TEXT,
    default_value TEXT,
    validation TEXT,
    options TEXT,
    display_order INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

**索引**：
- `idx_system_configs_category` - 按分类查询
- `idx_system_configs_display_order` - 按显示顺序排序

### 2.2 表结构变更

#### `topics` 表新增字段
```sql
ALTER TABLE topics ADD COLUMN IF NOT EXISTS topic_description TEXT;
```

**用途**：存储话题描述，用于LLM评分时提供更多上下文

### 2.3 迁移方式

提供了两种迁移方式：

**方式1：使用 Alembic（推荐）**
```bash
# 查看当前版本
alembic current

# 升级到最新版本
alembic upgrade head
```

**方式2：使用迁移脚本**
```bash
# 执行消息评分功能迁移
python3 run_migration.py

# 初始化系统配置表和默认值
python3 init_system_config.py
```

---

## 三、代码变更评估

### 3.1 核心功能新增

#### ✅ 消息评分系统
- **文件**：`services/message_scoring_service.py`
- **功能**：使用 DeepSeek/MiniMax LLM 对每条消息进行质量评分
- **依赖**：需要配置 LLM API Key

#### ✅ 系统配置管理
- **文件**：`services/system_config_service.py`
- **功能**：运行时配置管理，支持在管理后台修改配置
- **前端**：`frontend/system-config.html`
- **配置项**：
  - LLM API 配置（DeepSeek、MiniMax）
  - Prompt 模板（评分、总结）
  - 总结阈值等参数

#### ✅ MiniMax LLM 集成
- **文件**：`services/llm_clients/minimax_client.py`
- **功能**：支持 MiniMax 大模型作为评分和总结的备选方案
- **特性**：
  - OpenAI 兼容格式
  - 自动过滤思考标签
  - 5次重试机制

#### ✅ 智能体模拟器
- **文件**：`frontend/simulator.html`
- **功能**：测试和调试智能体对话，支持多种 LLM 提供商
- **用途**：开发和测试环境

#### ✅ 总结监控功能
- **文件**：`frontend/monitor.html` (增强)
- **功能**：实时监控总结任务状态和话题 Token 计数
- **API**：`/api/monitor/summary-jobs`

### 3.2 前端页面更新

| 页面 | 状态 | 主要变更 |
|------|------|---------|
| `index.html` | ✅ 更新 | 话题信息面板显示评分、建议、摘要 |
| `monitor.html` | ✅ 更新 | 新增总结监控功能 |
| `system-config.html` | ✅ 新增 | 系统配置管理界面 |
| `simulator.html` | ✅ 新增 | 智能体模拟器 |
| `admin.html` | ✅ 保持 | 管理后台入口 |
| `history.html` | ✅ 保持 | 历史话题查看 |

### 3.3 API 端点新增

```
POST   /api/system-config/update          # 更新系统配置
GET    /api/system-config/all             # 获取所有配置
POST   /api/system-config/reset/{key}     # 重置配置
GET    /api/monitor/summary-jobs          # 获取总结任务状态
POST   /api/simulator/chat                # 模拟器对话接口
GET    /api/scoring/stats                 # 评分统计
POST   /api/scoring/retry-unscored        # 重试未评分消息
```

---

## 四、环境配置变更

### 4.1 .env 文件结构调整

**重要变更**：LLM 配置已迁移到系统配置表

#### 必需配置（基础设施）
```bash
# 数据库
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Redis
REDIS_URL=redis://host:6379/0

# Celery
CELERY_BROKER_URL=redis://host:6379/0
CELERY_RESULT_BACKEND=redis://host:6379/0

# API 服务器
API_HOST=0.0.0.0
API_PORT=8080
```

#### 可选配置（作为默认值）
```bash
# LLM 配置（优先从系统配置读取）
DEEPSEEK_API_KEY=
DEEPSEEK_API_URL=https://api.deepseek.com/v1
SUMMARY_THRESHOLD=8000
```

### 4.2 配置优先级
```
系统配置表 > .env 文件 > 代码默认值
```

**建议**：在管理后台配置 LLM，.env 文件中可以留空

---

## 五、依赖变更

### 5.1 Python 依赖（requirements.txt）

当前依赖无变更，但需确认服务器已安装：

```
fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
celery==5.3.6
redis==5.0.1
httpx==0.26.0
openai==1.10.0
python-dotenv==1.0.0
```

### 5.2 系统依赖

- PostgreSQL 12+
- Redis 5+
- Python 3.9+

---

## 六、发布准备工作清单

### 6.1 代码同步 ✅

```bash
# 1. 备份服务器当前代码
cd /path/to/project
tar -czf backup_$(date +%Y%m%d_%H%M%S).tar.gz .

# 2. 拉取最新代码
git fetch origin
git status
git pull origin main

# 3. 验证代码版本
git log --oneline -1
# 应显示：0dd1857 优化总结Prompt...
```

### 6.2 数据库升级 ⚠️ 重要

```bash
# 方式1：使用 Alembic（推荐）
alembic current                    # 查看当前版本
alembic upgrade head               # 升级到最新

# 方式2：使用迁移脚本
python3 run_migration.py           # 创建评分表
python3 init_system_config.py     # 创建配置表并初始化
```

**预期结果**：
- ✅ `message_relevance_scores` 表创建成功
- ✅ `system_configs` 表创建成功
- ✅ `topics.topic_description` 字段添加成功
- ✅ 所有索引创建成功

### 6.3 环境配置检查

```bash
# 1. 检查 .env 文件
cat .env

# 必需配置项：
# - DATABASE_URL
# - REDIS_URL
# - CELERY_BROKER_URL
# - API_HOST
# - API_PORT

# 2. 测试数据库连接
python3 -c "from models.database import engine; engine.connect(); print('✅ 数据库连接成功')"

# 3. 测试 Redis 连接
python3 -c "import redis; from config.settings import settings; r = redis.from_url(settings.redis_url); r.ping(); print('✅ Redis 连接成功')"
```

### 6.4 系统配置初始化

```bash
# 初始化系统配置（首次部署）
python3 init_system_config.py

# 配置 LLM API Key（在管理后台）
# 访问：http://服务器IP:8080/system-config.html
# 配置：
# - DeepSeek API Key
# - MiniMax API Key（可选）
# - 评分 Prompt
# - 总结 Prompt
```

### 6.5 Celery Worker 重启

```bash
# 查找并停止旧的 Worker
ps aux | grep celery
kill -9 <PID>

# 启动新的 Worker（包含新队列）
celery -A tasks.celery_app worker \
  --loglevel=info \
  --concurrency=5 \
  --queues=summary_queue,scoring_queue \
  --max-tasks-per-child=100
```

**或使用提供的脚本**：
```bash
chmod +x restart_worker.sh
./restart_worker.sh
```

### 6.6 API 服务器重启

```bash
# 如果使用 systemd
sudo systemctl restart your-service-name

# 如果使用 screen/tmux
ps aux | grep "python.*main.py"
kill -9 <PID>
python3 main.py

# 如果使用 uvicorn
uvicorn main:app --host 0.0.0.0 --port 8080 --reload
```

### 6.7 验证部署

```bash
# 1. 检查 API 健康状态
curl http://localhost:8080/api/health

# 2. 检查数据库表
python3 verify_database.py

# 3. 检查系统配置
curl http://localhost:8080/api/system-config/all

# 4. 检查 Celery Worker
celery -A tasks.celery_app inspect active

# 5. 访问前端页面
# http://服务器IP:8080/
# http://服务器IP:8080/system-config.html
# http://服务器IP:8080/simulator.html
```

---

## 七、风险评估与注意事项

### 7.1 高风险项 ⚠️

#### 数据库迁移
- **风险**：迁移失败可能导致服务不可用
- **缓解**：
  - ✅ 迁移前备份数据库
  - ✅ 使用 `IF NOT EXISTS` 避免重复创建
  - ✅ 在测试环境先验证

#### 配置迁移
- **风险**：.env 中的 LLM 配置需要迁移到系统配置表
- **缓解**：
  - ✅ 保留 .env 中的配置作为默认值
  - ✅ 在管理后台重新配置 API Key
  - ✅ 验证配置生效

### 7.2 中风险项 ⚠️

#### Celery 队列变更
- **风险**：新增 `scoring_queue`，旧 Worker 不监听
- **缓解**：
  - ✅ 重启 Worker 时指定新队列
  - ✅ 使用提供的 `restart_worker.sh` 脚本

#### 前端缓存
- **风险**：浏览器缓存旧版本前端文件
- **缓解**：
  - ✅ 强制刷新（Ctrl+Shift+R）
  - ✅ 清空浏览器缓存

### 7.3 低风险项 ℹ️

#### 新增功能
- 消息评分、模拟器等新功能不影响现有功能
- 可以逐步启用和测试

---

## 八、回滚方案

### 8.1 代码回滚

```bash
# 回滚到上一个版本
git log --oneline -5
git reset --hard <previous_commit_hash>

# 重启服务
sudo systemctl restart your-service-name
```

### 8.2 数据库回滚

```bash
# 使用 Alembic 回滚
alembic downgrade -1

# 或手动删除新表（不推荐，会丢失数据）
# DROP TABLE message_relevance_scores;
# DROP TABLE system_configs;
# ALTER TABLE topics DROP COLUMN topic_description;
```

### 8.3 配置回滚

```bash
# 恢复旧的 .env 文件
cp .env.backup .env
```

---

## 九、发布后验证清单

### 9.1 功能验证

- [ ] 访问首页 `http://服务器IP:8080/`
- [ ] 创建新话题，验证对话功能
- [ ] 查看话题信息面板，确认显示评分、建议、摘要
- [ ] 访问系统配置页面 `http://服务器IP:8080/system-config.html`
- [ ] 修改配置并保存，验证配置生效
- [ ] 访问模拟器 `http://服务器IP:8080/simulator.html`
- [ ] 测试消息评分功能
- [ ] 测试总结生成功能
- [ ] 查看总结监控页面

### 9.2 性能验证

- [ ] 检查 API 响应时间（< 500ms）
- [ ] 检查 Celery 任务执行情况
- [ ] 检查数据库查询性能
- [ ] 检查 Redis 连接状态

### 9.3 日志检查

```bash
# 查看应用日志
tail -f logs/app.log

# 查看 Celery 日志
tail -f logs/celery.log

# 查看错误日志
grep ERROR logs/*.log
```

---

## 十、总结与建议

### 10.1 必须执行的操作

1. ✅ **数据库迁移**（创建 2 个新表，添加 1 个字段）
2. ✅ **代码同步**（拉取最新代码）
3. ✅ **系统配置初始化**（运行 `init_system_config.py`）
4. ✅ **Celery Worker 重启**（监听新队列）
5. ✅ **API 服务器重启**（加载新代码）
6. ✅ **LLM 配置**（在管理后台配置 API Key）

### 10.2 建议的发布时间窗口

- **预计停机时间**：5-10 分钟
- **建议时间**：业务低峰期（如凌晨或周末）
- **需要人员**：1-2 人（1 人操作，1 人监控）

### 10.3 发布顺序

```
1. 备份数据库和代码（5分钟）
2. 停止服务（Celery + API）（1分钟）
3. 拉取最新代码（1分钟）
4. 执行数据库迁移（2分钟）
5. 初始化系统配置（1分钟）
6. 启动服务（Celery + API）（2分钟）
7. 验证功能（5分钟）
8. 配置 LLM API Key（3分钟）
```

### 10.4 联系方式

如有问题，请提供：
- Git commit hash: `git rev-parse HEAD`
- 错误日志：`tail -100 logs/app.log`
- 数据库状态：`alembic current`
- Celery 状态：`celery -A tasks.celery_app inspect active`

---

## 附录：快速命令参考

### 完整发布脚本（参考）

```bash
#!/bin/bash
set -e

echo "=== 开始发布 ==="

# 1. 备份
echo "1. 备份数据库和代码..."
pg_dump -U user -d dual_agent_chat > backup_$(date +%Y%m%d_%H%M%S).sql
tar -czf code_backup_$(date +%Y%m%d_%H%M%S).tar.gz .

# 2. 停止服务
echo "2. 停止服务..."
pkill -f "celery.*worker" || true
pkill -f "python.*main.py" || true
sleep 2

# 3. 更新代码
echo "3. 更新代码..."
git pull origin main

# 4. 数据库迁移
echo "4. 执行数据库迁移..."
alembic upgrade head
python3 init_system_config.py

# 5. 启动服务
echo "5. 启动服务..."
nohup celery -A tasks.celery_app worker --loglevel=info --concurrency=5 --queues=summary_queue,scoring_queue > logs/celery.log 2>&1 &
sleep 2
nohup python3 main.py > logs/app.log 2>&1 &

# 6. 验证
echo "6. 验证服务..."
sleep 5
curl -f http://localhost:8080/api/health || echo "❌ API 健康检查失败"

echo "=== 发布完成 ==="
echo "请访问管理后台配置 LLM API Key"
echo "http://服务器IP:8080/system-config.html"
```

---

**报告生成时间**：2026-02-25  
**评估人员**：Kiro AI Assistant  
**项目版本**：0dd1857 (main)
