# 明宽服务器问题修复报告

修复时间: 2026-02-25 12:27

## 问题概述

部署后发现两个问题：
1. 访问首页显示旧版 monitor.html，而非新版 index.html
2. 话题总结 Prompt 不是最新版本

## 问题 1: 首页路由问题

### 问题描述
- 访问 `http://129.211.28.211:8080/` 显示旧版 monitor.html
- 本地访问正常，显示新版 index.html（论坛风格）

### 根本原因
Nginx 配置中的默认首页设置错误：
```nginx
# 旧配置（错误）
location / {
    root /home/ubuntu/dual-agent-chat/frontend;
    try_files $uri $uri/ /monitor.html;  # ❌ 指向旧页面
    index monitor.html;                   # ❌ 指向旧页面
}
```

### 解决方案
更新 Nginx 配置，将默认首页改为 index.html：
```nginx
# 新配置（正确）
location / {
    root /home/ubuntu/dual-agent-chat/frontend;
    try_files $uri $uri/ /index.html;    # ✅ 指向新页面
    index index.html;                     # ✅ 指向新页面
}
```

### 执行步骤
```bash
# 1. 更新 Nginx 配置
sudo vim /etc/nginx/sites-enabled/dual-agent-chat

# 2. 测试配置
sudo nginx -t

# 3. 重载 Nginx
sudo systemctl reload nginx
```

### 验证结果
✅ 访问 `http://129.211.28.211:8080/` 现在显示新版 index.html（论坛风格）

## 问题 2: 总结 Prompt 版本不一致

### 问题描述
- 服务器上的总结 Prompt 是旧版本
- 本地已更新为新版本（按发言人归纳观点）

### 旧版 Prompt 特征
```
任务：
1. 总结对话的核心观点和进展
2. 评估对话质量和完整性
3. 给出继续对话的建议
```

### 新版 Prompt 特征
```
【总结要求】

1. 按发言人归纳观点
   - 明确列出每个发言人的核心观点
   - 用简洁的语言概括他们的立场和论据
   - 保留关键的论点和例子

2. 梳理对话脉络
   - 对话如何展开的（从什么话题开始）
   - 双方在哪些点上有共识
   - 双方在哪些点上有分歧
   - 对话是否有新的突破或转折

3. 评估对话状态
   - 对话深度、观点多样性、逻辑连贯性、结论性
```

### 解决方案
使用项目中的 `update_summary_prompt.py` 脚本更新：

```bash
cd /home/ubuntu/dual-agent-chat
source venv/bin/activate
python3 update_summary_prompt.py
```

### 执行结果
```
📝 更新 summary_prompt...

旧 Prompt 长度: 732 字符
新 Prompt 长度: 733 字符

✅ summary_prompt 已更新
```

### 重启服务
```bash
sudo systemctl restart dual-agent-celery.service
sudo systemctl restart dual-agent-api.service
```

### 验证结果
```sql
SELECT key, LENGTH(value) as len 
FROM system_configs 
WHERE key = 'summary_prompt';

-- 结果: 733 字符（新版本）
```

✅ 总结 Prompt 已更新为最新版本

## 修复总结

| 问题 | 状态 | 修复方式 | 影响范围 |
|------|------|----------|----------|
| 首页路由错误 | ✅ 已修复 | 更新 Nginx 配置 | 前端访问 |
| 总结 Prompt 旧版 | ✅ 已修复 | 运行更新脚本 + 重启服务 | 总结功能 |

## 验证清单

- [x] 访问 `http://129.211.28.211:8080/` 显示新版 index.html
- [x] 总结 Prompt 已更新为新版本（733字符）
- [x] API 服务器正常运行
- [x] Celery Worker 正常运行
- [x] Nginx 配置语法正确

## 后续建议

### 1. 测试总结功能
访问管理后台，触发一次话题总结，验证新 Prompt 是否生效：
```
http://129.211.28.211:8080/admin.html
```

### 2. 监控日志
```bash
# 查看 API 日志
sudo journalctl -u dual-agent-api.service -f

# 查看 Celery 日志
sudo journalctl -u dual-agent-celery.service -f
```

### 3. 配置同步检查
定期检查本地和服务器的配置是否一致：
- 系统配置（system_configs 表）
- Nginx 配置
- 环境变量（.env）

### 4. 部署流程优化
建议在部署脚本中增加配置验证步骤：
```bash
# 检查 Nginx 配置
nginx -t

# 检查系统配置版本
psql -c "SELECT key, LENGTH(value) FROM system_configs WHERE key LIKE '%prompt%'"

# 对比本地和服务器的关键配置
```

## 相关文件

- Nginx 配置: `/etc/nginx/sites-enabled/dual-agent-chat`
- 更新脚本: `update_summary_prompt.py`
- 新版首页: `frontend/index.html`
- 旧版首页: `frontend/monitor.html`

---

**修复人员**: Kiro AI Assistant  
**修复时间**: 2026-02-25 12:27  
**修复状态**: ✅ 全部完成
