# 环境一致性说明

## 概述

本文档说明本地开发环境和生产环境的配置一致性，确保部署后的行为完全相同。

## 架构设计

### 本地环境

```
┌─────────────────────────────────────────┐
│         浏览器 (localhost:8080)          │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│         Nginx (端口 8080)                │
│  - 静态文件: frontend/                   │
│  - 静态资源: static/                     │
│  - API 代理: → FastAPI (8000)           │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│      FastAPI (端口 8000)                 │
│  - API 接口: /api/*                      │
│  - API 文档: /docs, /redoc              │
└─────────────────────────────────────────┘
```

### 生产环境

```
┌─────────────────────────────────────────┐
│   浏览器 (129.211.28.211:8080)          │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│         Nginx (端口 8080)                │
│  - 静态文件: frontend/                   │
│  - 静态资源: static/                     │
│  - API 代理: → FastAPI (8000)           │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│      FastAPI (端口 8000)                 │
│  - API 接口: /api/*                      │
│  - API 文档: /docs, /redoc              │
└─────────────────────────────────────────┘
```

## 文件部署策略

### ai-agent-guide.html 双位置部署

为了确保访问的灵活性，`ai-agent-guide.html` 文件部署在两个位置：

1. **frontend/ai-agent-guide.html**
   - 通过 Nginx 的 `location /` 规则直接访问
   - URL: `http://localhost:8080/ai-agent-guide.html`
   - URL: `http://129.211.28.211:8080/ai-agent-guide.html`

2. **static/ai-agent-guide.html**
   - 通过 Nginx 的 `location /static/` 规则访问
   - URL: `http://localhost:8080/static/ai-agent-guide.html`
   - URL: `http://129.211.28.211:8080/static/ai-agent-guide.html`

### 文件同步机制

- **源文件**: `static/ai-agent-guide.html` (主文件)
- **同步目标**: `frontend/ai-agent-guide.html` (副本)
- **同步时机**:
  1. 本地启动服务时 (`start_services.sh`)
  2. 部署到生产环境时 (`deploy_to_mingkuan.sh`)
  3. 手动更新文件后

## 访问地址对照表

| 页面 | 本地环境 | 生产环境 |
|------|---------|---------|
| 监控页面 | http://localhost:8080/monitor.html | http://129.211.28.211:8080/monitor.html |
| 聊天页面 | http://localhost:8080/index.html | http://129.211.28.211:8080/index.html |
| 管理页面 | http://localhost:8080/admin.html | http://129.211.28.211:8080/admin.html |
| 历史记录 | http://localhost:8080/history.html | http://129.211.28.211:8080/history.html |
| 智能体指南 | http://localhost:8080/ai-agent-guide.html | http://129.211.28.211:8080/ai-agent-guide.html |
| 智能体指南 (static) | http://localhost:8080/static/ai-agent-guide.html | http://129.211.28.211:8080/static/ai-agent-guide.html |
| API 文档 | http://localhost:8080/docs | http://129.211.28.211:8080/docs |

## Nginx 配置一致性

### 本地配置 (nginx_local.conf)

```nginx
server {
    listen 8080;
    server_name localhost;

    # 前端静态文件
    location / {
        root /Users/ahfun/ahfun_ai/frontend;
        try_files $uri $uri/ /monitor.html;
        index monitor.html;
    }

    # 静态资源
    location /static/ {
        alias /Users/ahfun/ahfun_ai/static/;
    }

    # API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        # ... 其他配置
    }
}
```

### 生产配置 (服务器上的 Nginx)

```nginx
server {
    listen 8080;
    server_name 129.211.28.211;

    # 前端静态文件
    location / {
        root /home/ubuntu/dual-agent-chat/frontend;
        try_files $uri $uri/ /monitor.html;
        index monitor.html;
    }

    # 静态资源
    location /static/ {
        alias /home/ubuntu/dual-agent-chat/static/;
    }

    # API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        # ... 其他配置
    }
}
```

**关键点**: 除了路径前缀不同（本地 vs 服务器），配置结构完全一致。

## 服务启动流程

### 本地环境

```bash
# 启动所有服务
./start_services.sh

# 服务包括:
# 1. Redis (6379)
# 2. PostgreSQL (5432)
# 3. FastAPI (8000)
# 4. Celery Worker
# 5. Celery Beat
# 6. Nginx (8080)
```

### 生产环境

```bash
# 通过 systemd 管理服务
sudo systemctl start dual-agent-api        # FastAPI
sudo systemctl start dual-agent-celery     # Celery Worker
sudo systemctl start dual-agent-celery-beat # Celery Beat
sudo systemctl start nginx                 # Nginx
```

## 部署流程

### 1. 部署前检查

```bash
# 运行一致性检查
./check_environment_consistency.sh
```

### 2. 部署到生产环境

```bash
# 执行部署脚本
./deploy_to_mingkuan.sh
```

部署脚本会自动：
1. 导出本地数据库
2. 同步 `static/ai-agent-guide.html` 到 `frontend/ai-agent-guide.html`
3. 同步代码到服务器
4. 在服务器上同步文件
5. 重启服务
6. 验证访问

### 3. 部署后验证

部署脚本会自动验证以下内容：
- API 健康检查
- 智能体指南访问 (两个 URL)
- 监控页面访问

## 常见问题

### Q1: 为什么要在两个位置部署 ai-agent-guide.html？

**A**: 为了提供访问的灵活性：
- `frontend/` 位置：直接通过根路径访问，URL 更简洁
- `static/` 位置：与其他静态资源保持一致，便于管理

### Q2: 如何更新 ai-agent-guide.html？

**A**: 
1. 编辑 `static/ai-agent-guide.html`
2. 运行 `cp static/ai-agent-guide.html frontend/ai-agent-guide.html`
3. 部署到生产环境: `./deploy_to_mingkuan.sh`

或者直接运行部署脚本，它会自动同步文件。

### Q3: 本地环境和生产环境有什么区别？

**A**: 
- **相同点**: 架构、配置、访问方式完全一致
- **不同点**: 
  - 路径前缀 (本地用户目录 vs 服务器 home 目录)
  - 服务管理方式 (手动启动 vs systemd)
  - 域名/IP (localhost vs 129.211.28.211)

### Q4: 如何验证环境一致性？

**A**: 运行一致性检查脚本:

```bash
./check_environment_consistency.sh
```

该脚本会检查：
- 文件一致性
- 服务状态
- 本地访问
- 生产环境访问
- Nginx 配置

## 维护建议

1. **定期检查**: 每次修改配置后运行一致性检查
2. **文件同步**: 更新 `static/` 目录的文件后，记得同步到 `frontend/`
3. **部署验证**: 每次部署后检查所有访问地址
4. **配置备份**: 定期备份 Nginx 配置文件

## 相关文件

- `start_services.sh` - 本地服务启动脚本
- `deploy_to_mingkuan.sh` - 生产环境部署脚本
- `check_environment_consistency.sh` - 环境一致性检查脚本
- `nginx_local.conf` - 本地 Nginx 配置
- `frontend/ai-agent-guide.html` - 智能体指南 (前端位置)
- `static/ai-agent-guide.html` - 智能体指南 (静态资源位置)
