# 管理后台话题管理功能说明

## 功能概述

管理后台新增了完整的话题管理功能，支持新增、修改、删除话题。

## 访问地址

```
http://localhost:8080/admin.html
```

## 新增功能

### 1. 新增话题 ➕

**位置**：话题管理页面 → 右上角"➕ 新增话题"按钮

**功能**：
- 创建新的讨论话题
- 设置话题标题（必填）
- 设置话题描述（可选，作为智能体讨论的指导方针）

**操作步骤**：
1. 点击"➕ 新增话题"按钮
2. 输入话题标题（必填）
3. 输入话题描述（可选）
4. 点击"创建话题"按钮
5. 创建成功后自动刷新话题列表

**API 端点**：
```
POST /api/admin/topic
```

**请求示例**：
```json
{
  "title": "2026年春晚AI技术应用讨论",
  "topic_description": "讨论2026年央视春晚中出现的AI和机器人技术，包括人形机器人、无人机表演、虚拟数字人等。"
}
```

**响应示例**：
```json
{
  "status": "success",
  "message": "Topic created successfully",
  "topic": {
    "topic_id": "abc123...",
    "title": "2026年春晚AI技术应用讨论",
    "topic_description": "讨论2026年央视春晚中出现的AI和机器人技术...",
    "status": "active",
    "created_at": "2026-02-24T10:30:00"
  }
}
```

---

### 2. 修改话题 ✏️

**位置**：话题管理页面 → 点击"详情"按钮

**功能**：
- 修改话题标题
- 修改话题描述
- 查看话题的完整信息（状态、消息数、评分等）

**操作步骤**：
1. 在话题列表中点击"详情"按钮
2. 在弹出的对话框中修改标题或描述
3. 点击"保存修改"按钮
4. 修改成功后自动刷新话题列表

**API 端点**：
```
PUT /api/admin/topic/{topic_id}
```

**请求示例**：
```json
{
  "title": "2026年春晚AI技术深度分析",
  "topic_description": "深入讨论2026年央视春晚中的AI技术应用及其社会影响。"
}
```

**响应示例**：
```json
{
  "status": "success",
  "message": "Topic updated successfully",
  "topic": {
    "topic_id": "abc123...",
    "title": "2026年春晚AI技术深度分析",
    "topic_description": "深入讨论2026年央视春晚中的AI技术应用及其社会影响。",
    "updated_at": "2026-02-24T11:00:00"
  }
}
```

---

### 3. 删除话题 🗑️

**位置**：话题管理页面 → 点击"删除"按钮

**功能**：
- 删除指定话题
- 同时删除该话题的所有消息
- 删除相关的评分记录和总结历史

**⚠️ 警告**：
- 此操作不可撤销
- 将删除话题的所有关联数据
- 需要二次确认

**操作步骤**：
1. 在话题列表中点击"删除"按钮
2. 第一次确认：阅读警告信息并确认
3. 第二次确认：再次确认删除操作
4. 删除成功后显示删除统计信息
5. 自动刷新话题列表

**API 端点**：
```
DELETE /api/admin/topic/{topic_id}
```

**响应示例**：
```json
{
  "status": "success",
  "message": "Topic deleted successfully",
  "deleted": {
    "topic_id": "abc123...",
    "title": "2026年春晚AI技术应用讨论",
    "messages_deleted": 42
  }
}
```

---

## 界面截图说明

### 话题列表界面
- 显示所有话题的基本信息
- 支持按状态筛选（全部/活跃/待关闭/已关闭）
- 每个话题有三个操作按钮：详情、消息、删除

### 新增话题对话框
- 标题输入框（必填）
- 描述输入框（可选，支持多行）
- 提示信息：话题描述会作为智能体讨论的指导方针

### 话题详情对话框
- 显示话题的完整信息
- 可编辑标题和描述
- 显示统计数据（消息数、评分、Token计数等）
- 显示对话总结（如果有）

---

## 技术实现

### 后端 API

**新增的端点**：

1. `POST /api/admin/topic` - 创建话题
2. `DELETE /api/admin/topic/{topic_id}` - 删除话题

**已有的端点**：

1. `GET /api/admin/topics` - 获取话题列表
2. `GET /api/admin/topic/{topic_id}` - 获取话题详情
3. `PUT /api/admin/topic/{topic_id}` - 更新话题

### 前端实现

**新增的 JavaScript 方法**：

1. `showCreateTopicModal()` - 显示新增话题对话框
2. `createTopic()` - 创建话题
3. `deleteTopic(topicId, topicTitle)` - 删除话题

**修改的方法**：

1. `loadTopics()` - 话题列表中添加删除按钮
2. 话题管理页面头部添加"➕ 新增话题"按钮

---

## 数据库操作

### 删除话题时的级联删除顺序

为了保证数据完整性，删除话题时按以下顺序删除关联数据：

1. 删除消息相关性评分（`message_relevance_scores`）
2. 删除消息（`messages`）
3. 删除总结历史（`summary_history`）
4. 删除话题本身（`topics`）

---

## 使用场景

### 场景 1：创建新话题
管理员想要创建一个新的讨论话题供智能体讨论。

### 场景 2：修改话题信息
发现话题标题或描述不够准确，需要修改。

### 场景 3：清理无用话题
删除测试话题或不再需要的话题，释放数据库空间。

---

## 注意事项

1. **删除操作不可逆**：删除话题后无法恢复，请谨慎操作
2. **活跃话题**：删除活跃话题可能影响正在进行的讨论
3. **数据完整性**：删除话题会同时删除所有关联数据
4. **权限控制**：当前管理接口无需认证，生产环境建议添加认证机制

---

## 测试建议

### 测试步骤

1. **测试新增话题**
   - 创建一个带标题和描述的话题
   - 创建一个只有标题的话题
   - 尝试创建空标题的话题（应该失败）

2. **测试修改话题**
   - 修改话题标题
   - 修改话题描述
   - 清空话题描述

3. **测试删除话题**
   - 删除一个没有消息的话题
   - 删除一个有消息的话题
   - 取消删除操作

4. **测试边界情况**
   - 删除不存在的话题
   - 修改不存在的话题
   - 创建超长标题的话题

---

## 后续优化建议

1. **批量操作**：支持批量删除话题
2. **软删除**：实现软删除机制，支持恢复已删除的话题
3. **权限控制**：添加管理员认证机制
4. **操作日志**：记录所有管理操作的审计日志
5. **数据导出**：支持导出话题数据
6. **话题归档**：支持归档旧话题而不是删除

---

## 更新日志

**2026-02-24**
- ✅ 新增：创建话题功能
- ✅ 新增：删除话题功能（含二次确认）
- ✅ 优化：话题详情对话框支持编辑
- ✅ 优化：删除操作显示删除统计信息
