# 明宽服务器部署完成报告

部署时间: 2026-02-25 12:18

## 部署概览

✅ 成功将本地代码（94个提交）部署到明宽服务器，并完成数据库迁移。

## 执行步骤

### ✅ 步骤 1: 数据库和代码备份
```
- topics: 4 条记录 → /tmp/topics_backup.csv
- messages: 62 条记录 → /tmp/messages_backup.csv
- agents: 24 条记录 → /tmp/agents_backup.csv
- 代码备份: 37MB → /tmp/dual-agent-chat-backup-20260225-120837.tar.gz
```

### ✅ 步骤 2: 停止服务
```
- 停止了所有 Celery Worker 和 Beat 进程
- 停止了 Uvicorn API 服务器
```

### ✅ 步骤 3: 更新代码（Git 方式）
```bash
cd /home/ubuntu/dual-agent-chat
git fetch origin
git reset --hard origin/main
# 从 3f492b4 更新到 a8ad8f5（94个提交）
```

### ✅ 步骤 4: 执行数据库迁移
```sql
-- 创建 system_configs 表
CREATE TABLE system_configs (
    key VARCHAR(100) PRIMARY KEY,
    value TEXT NOT NULL,
    config_type VARCHAR(20) NOT NULL DEFAULT 'text',
    category VARCHAR(50) NOT NULL DEFAULT 'general',
    display_name VARCHAR(200) NOT NULL,
    description TEXT,
    default_value TEXT,
    validation TEXT,
    options TEXT,
    display_order INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- 授予权限
GRANT ALL PRIVILEGES ON TABLE system_configs TO dual_agent_user;

-- 初始化配置数据（11条）
python3 init_system_config.py
```

**初始化的配置项**:
- SUMMARY: 总结触发阈值 (8000)
- PROMPTS: 消息评分 Prompt、对话总结 Prompt
- LLM: 消息评分 LLM、对话总结 LLM、DeepSeek 配置、MiniMax 配置

### ✅ 步骤 5: 检查依赖
```
- SQLAlchemy: 2.0.25 ✓
- celery: 5.3.6 ✓
- fastapi: 0.109.0 ✓
- redis: 5.0.1 ✓
- uvicorn: 0.27.0 ✓
```

### ✅ 步骤 6: 重启服务（systemd）
```bash
sudo systemctl restart dual-agent-celery.service
sudo systemctl restart dual-agent-celery-beat.service
sudo systemctl restart dual-agent-api.service
```

### ✅ 步骤 7: 验证部署

#### 7.1 进程检查
```
- Celery Worker: 运行中（6个进程）
- Celery Beat: 运行中
- Uvicorn API: 运行中（端口 8000）
```

#### 7.2 数据库验证
```sql
-- 表数量: 9 个（新增 system_configs）
SELECT COUNT(*) FROM system_configs;
-- 结果: 11 条配置记录
```

#### 7.3 API 验证
```bash
curl http://localhost:8000/
# 返回: 实时监控页面 HTML（正常）
```

#### 7.4 服务状态
```
● dual-agent-api.service - Active: active (running)
● dual-agent-celery.service - Active: active (running)
● dual-agent-celery-beat.service - Active: active (running)
```

## 部署结果

### 数据库变更
| 表名 | 状态 | 说明 |
|------|------|------|
| system_configs | ✅ 新增 | 系统配置表，包含11条初始配置 |
| message_relevance_scores | ✓ 已存在 | 无需迁移 |
| topics.topic_description | ✓ 已存在 | 无需迁移 |

### 代码版本
| 项目 | 部署前 | 部署后 |
|------|--------|--------|
| Git Commit | 3f492b4 | a8ad8f5 |
| 提交数量 | - | +94 commits |
| 主要功能 | 基础功能 | 总结监控、系统配置、MiniMax集成、Worker管理等 |

### 服务配置
| 服务 | 管理方式 | 端口 | 状态 |
|------|----------|------|------|
| API Server | systemd | 8000 | ✅ Running |
| Celery Worker | systemd | - | ✅ Running |
| Celery Beat | systemd | - | ✅ Running |

## 新功能说明

部署后，服务器现在支持以下新功能：

### 1. 系统配置管理
- 访问地址: `http://129.211.28.211:8000/system-config.html`
- 功能: 在线配置 LLM API Key、模型、阈值等
- 优先级: 系统配置 > .env 文件 > 代码默认值

### 2. 总结监控
- 访问地址: `http://129.211.28.211:8000/summary-monitor.html`
- 功能: 实时监控总结任务状态和话题 Token 计数

### 3. MiniMax LLM 支持
- 支持 MiniMax API 作为 LLM 提供商
- 可在系统配置中切换 DeepSeek/MiniMax

### 4. Worker 管理
- 支持通过 API 重启 Celery Worker
- 提供 Worker 状态监控

### 5. 前端优化
- 实时监控页面优化
- 话题信息面板增强
- 消息评分显示改进

## 访问地址

| 页面 | URL |
|------|-----|
| 实时监控 | http://129.211.28.211:8000/ |
| 历史话题 | http://129.211.28.211:8000/history.html |
| 系统配置 | http://129.211.28.211:8000/system-config.html |
| 总结监控 | http://129.211.28.211:8000/summary-monitor.html |
| 智能体模拟器 | http://129.211.28.211:8000/simulator.html |
| 管理后台 | http://129.211.28.211:8000/admin.html |

## 备份信息

### 数据备份位置
```
/tmp/topics_backup.csv
/tmp/messages_backup.csv
/tmp/agents_backup.csv
/tmp/dual-agent-chat-backup-20260225-120837.tar.gz
```

### 回滚方案
如需回滚到部署前版本：
```bash
# 1. 停止服务
sudo systemctl stop dual-agent-celery.service dual-agent-celery-beat.service dual-agent-api.service

# 2. 恢复代码
cd /home/ubuntu/dual-agent-chat
git reset --hard 3f492b4

# 3. 删除 system_configs 表
PGPASSWORD=dual_agent_pass psql -h localhost -U dual_agent_user -d dual_agent_chat -c 'DROP TABLE IF EXISTS system_configs CASCADE'

# 4. 重启服务
sudo systemctl start dual-agent-celery.service dual-agent-celery-beat.service dual-agent-api.service
```

## 后续建议

### 1. 配置 LLM API Key
访问系统配置页面，配置 DeepSeek 或 MiniMax 的 API Key：
```
http://129.211.28.211:8000/system-config.html
```

### 2. 监控日志
```bash
# API 日志
sudo journalctl -u dual-agent-api.service -f

# Celery Worker 日志
sudo journalctl -u dual-agent-celery.service -f

# Celery Beat 日志
sudo journalctl -u dual-agent-celery-beat.service -f
```

### 3. 定期备份
建议设置定期备份任务：
```bash
# 每天备份数据库
0 2 * * * PGPASSWORD=dual_agent_pass pg_dump -h localhost -U dual_agent_user dual_agent_chat > /backup/db_$(date +\%Y\%m\%d).sql
```

### 4. 清理旧备份
```bash
# 保留备份7天后可删除
rm /tmp/*backup*.csv
rm /tmp/*backup*.tar.gz
```

## 部署总结

✅ 部署成功完成，所有服务正常运行
✅ 数据库迁移成功，新增 system_configs 表
✅ 代码更新成功，94个提交已同步
✅ 服务通过 systemd 管理，自动重启已配置
✅ 备份已创建，可随时回滚

---

**部署人员**: Kiro AI Assistant  
**部署时间**: 2026-02-25 12:18  
**部署状态**: ✅ 成功
