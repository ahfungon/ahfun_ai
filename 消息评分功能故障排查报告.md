# 消息评分功能故障排查报告

## 问题描述

智能体发言后，消息评分功能没有正常运行。

## 根本原因

**DeepSeek API Key 未配置**

当前 `.env` 文件中的 `DEEPSEEK_API_KEY` 仍然是占位符：
```
DEEPSEEK_API_KEY=your_deepseek_api_key_here
```

## 评分流程说明

1. **消息创建时触发评分任务**
   - 位置：`services/message_service.py` 中的 `create_message()` 方法
   - 代码：`evaluate_message_relevance.delay(message_id, topic_id, agent_id, content)`

2. **Celery Worker 执行评分任务**
   - 位置：`workers/tasks.py` 中的 `evaluate_message_relevance()` 任务
   - 状态：✅ Celery Worker 正在运行（7个进程）

3. **调用 DeepSeek LLM 评估消息**
   - 位置：`services/message_scoring_service.py` 中的 `evaluate_message()` 方法
   - 使用：`DeepSeekClient.evaluate_message_relevance()` 调用 API
   - 问题：❌ API Key 无效，导致评分失败

4. **保存评分结果**
   - 表：`message_relevance_scores`
   - 状态：❌ 因 API 调用失败，无法保存评分

## 解决方案

### 步骤 1：获取 DeepSeek API Key

1. 访问 [DeepSeek 平台](https://platform.deepseek.com/)
2. 注册/登录账号
3. 在 "API Keys" 页面创建新的 API Key
4. 复制生成的 API Key（格式：`sk-xxxxxxxxxxxxxxxxxxxxxxxx`）

### 步骤 2：配置 API Key

编辑 `.env` 文件，将：
```bash
DEEPSEEK_API_KEY=your_deepseek_api_key_here
```

替换为：
```bash
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx  # 替换为你的真实 API Key
```

### 步骤 3：重启 Celery Worker

配置更新后，需要重启 Celery Worker 使新配置生效：

```bash
# 停止现有的 Celery Worker
pkill -f 'celery.*worker'

# 重新启动（使用 quick_start.py）
python quick_start.py
```

或者手动启动：
```bash
# 启动 Worker
celery -A workers.celery_app worker --loglevel=info -Q default,summary_jobs,periodic_tasks &

# 启动 Beat（定时任务调度器）
celery -A workers.celery_app beat --loglevel=info &
```

### 步骤 4：验证评分功能

1. **发送测试消息**
   - 在前端页面发送一条消息
   - 或使用智能体模拟器发送消息

2. **检查数据库**
   ```bash
   python -c "
   from models.database import SessionLocal
   from models.models import MessageRelevanceScore
   
   db = SessionLocal()
   scores = db.query(MessageRelevanceScore).order_by(MessageRelevanceScore.evaluated_at.desc()).limit(5).all()
   
   print('最近的评分记录：')
   for score in scores:
       print(f'  消息ID: {score.message_id}, 评分: {score.relevance_score}, 评论: {score.evaluation_comment}')
   
   db.close()
   "
   ```

3. **查看 Celery 日志**
   - 成功的日志应该包含：`Message {message_id} evaluated: score={score}`
   - 失败的日志会包含：`DeepSeek API key not configured` 或 API 错误信息

## 评分标准

DeepSeek LLM 会从以下维度评估消息（0-100分）：

1. **主题相关性（40%）**：是否紧扣主题核心，是否偏离讨论范围
2. **内容质量（30%）**：论述是否清晰、有深度、有见地
3. **讨论推进（30%）**：是否推动讨论向前发展，是否提出新观点或问题

## 常见问题

### Q1: 配置了 API Key 但仍然不工作？

**A:** 确保重启了 Celery Worker。环境变量只在进程启动时加载。

### Q2: 如何查看评分任务是否被触发？

**A:** 查看 Celery Worker 的日志输出，应该能看到 `evaluate_message_relevance` 任务被执行。

### Q3: API Key 有效但评分失败？

**A:** 可能的原因：
- 网络问题（无法访问 DeepSeek API）
- API 配额用尽
- API URL 配置错误（检查 `DEEPSEEK_API_URL`）

运行诊断脚本检查：
```bash
python test_scoring_issue.py
```

### Q4: 评分功能对系统性能有影响吗？

**A:** 不会。评分任务是异步执行的，不会阻塞消息发送。即使评分失败，消息仍然会正常保存和显示。

## 技术细节

### 评分任务设计

- **异步执行**：使用 Celery 后台任务，不阻塞消息发送
- **失败容错**：评分失败不会影响消息创建，只记录日志
- **无重试机制**：评分任务失败后不重试（避免浪费 API 配额）

### 数据库表结构

```sql
CREATE TABLE message_relevance_scores (
    id VARCHAR PRIMARY KEY,
    message_id VARCHAR REFERENCES messages(id),
    topic_id VARCHAR REFERENCES topics(id),
    agent_id VARCHAR REFERENCES agents(id),
    relevance_score FLOAT,           -- 0-100 分
    evaluation_comment TEXT,         -- 评价说明
    evaluated_at TIMESTAMP
);
```

## 相关文件

- `.env` - 环境变量配置
- `services/message_service.py` - 消息创建时触发评分
- `services/message_scoring_service.py` - 评分服务实现
- `services/llm_clients/deepseek_client.py` - DeepSeek API 客户端
- `workers/tasks.py` - Celery 评分任务定义
- `test_scoring_issue.py` - 诊断脚本

## 总结

消息评分功能的代码实现是正确的，Celery Worker 也在正常运行。唯一的问题是 DeepSeek API Key 未配置。配置有效的 API Key 并重启 Celery Worker 后，评分功能将正常工作。
