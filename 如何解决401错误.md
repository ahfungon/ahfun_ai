# 如何解决 401 错误

## 这不是错误！

你看到的 401 错误是**正常的安全机制**。系统要求所有 API 请求都必须提供有效的认证信息。

## 快速解决方案

### 方法 1：使用认证信息页面（推荐）

1. 打开浏览器访问：**http://localhost:8080/auth-info.html**
2. 页面会显示所有可用的 Agent ID 和 Token
3. 点击"复制"按钮复制认证信息
4. 点击"打开聊天界面"按钮
5. 在页面顶部粘贴 Agent ID 和 Token
6. 点击"设置Token"按钮

### 方法 2：直接输入

在聊天界面（http://localhost:8080/index.html）顶部输入：

**Agent 1:**
- Agent ID: `agent-1`
- Token: `token-agent-1-secret`

**Agent 2:**
- Agent ID: `agent-2`
- Token: `token-agent-2-secret`

然后点击"设置Token"按钮。

## 验证是否成功

设置 Token 后：
- ✅ 如果成功：页面会显示话题信息或"暂无活跃话题"
- ❌ 如果失败：页面会显示"认证失败"提示

## 当前系统状态

✅ 后端服务运行正常（端口 8000）
✅ 前端服务运行正常（端口 8080）
✅ 数据库中已有 Agent 数据
✅ 认证机制工作正常

## 访问链接

- 认证信息页面：http://localhost:8080/auth-info.html
- 聊天界面：http://localhost:8080/index.html
- 管理界面：http://localhost:8080/admin.html
- 监控界面：http://localhost:8080/monitor.html
- API 文档：http://localhost:8000/docs

## 测试认证

你可以用以下命令测试认证是否正常：

```bash
curl -X GET "http://localhost:8000/api/topic/active" \
  -H "X-Agent-Id: agent-1" \
  -H "X-Auth-Token: token-agent-1-secret"
```

如果返回 JSON 数据（而不是 401 错误），说明认证正常。

## 总结

401 错误是正常的，只需要：
1. 访问 http://localhost:8080/auth-info.html
2. 复制认证信息
3. 在聊天界面设置 Token
4. 开始使用！
