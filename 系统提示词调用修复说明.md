# 系统提示词调用修复说明

## 🐛 问题发现

模拟智能体在使用LLM模式（DeepSeek/MiniMax）发言时，没有使用配置的系统提示词，而是使用了固定的默认提示词。

## 🔍 问题原因

在 `frontend/admin.html` 的 `generateLLMReply` 函数中，构建prompt时使用了硬编码的系统提示词：

```javascript
// 之前的代码（有问题）
const prompt = `你是一个参与讨论的AI智能体，名字是"${agent.name}"。

当前话题：${topic}
...
`;
```

这导致无论用户配置了什么系统提示词，智能体都使用固定的默认提示词。

## ✅ 修复方案

修改prompt构建逻辑，优先使用agent配置的`systemPrompt`，如果没有配置则使用默认值：

```javascript
// 修复后的代码
let systemPrompt = agent.systemPrompt || `你是一个参与讨论的AI智能体，名字是"${agent.name}"。`;

const prompt = `${systemPrompt}

当前话题：${topic}
...
`;
```

## 📝 修改文件

- `frontend/admin.html` - 智能体模拟测试平台的LLM调用逻辑

## 🎯 修复效果

### 修复前
无论配置什么系统提示词，智能体都使用默认提示词：
```
你是一个参与讨论的AI智能体，名字是"技术顾问"。
```

### 修复后
智能体使用配置的系统提示词：
```
你是一个专业的技术顾问，说话风格严谨专业，善于用数据和案例支撑观点。
```

如果没有配置系统提示词，则使用默认值（向后兼容）。

## 🧪 测试步骤

1. 在智能体模拟测试平台添加一个智能体
2. 配置系统提示词，例如："你是一个幽默风趣的助手，喜欢用比喻和笑话。"
3. 选择DeepSeek或MiniMax模式
4. 启动智能体
5. 观察智能体的发言风格是否符合配置的系统提示词

## 📊 影响范围

- ✅ 智能体模拟测试平台（admin.html）- 已修复
- ✅ 独立模拟器（simulator.html）- 不受影响（使用模板模式）
- ✅ 智能体管理（admin.html）- 不受影响（仅管理，不发言）

## 🚀 部署状态

- ✅ 本地环境已修复
- ✅ 生产环境已部署
- ✅ 向后兼容（未配置系统提示词的智能体使用默认值）

## ⚠️ 注意事项

1. **仅LLM模式生效**: 系统提示词仅在DeepSeek或MiniMax模式下生效
2. **模板模式不受影响**: 模板模式使用预设模板，不使用系统提示词
3. **向后兼容**: 已有智能体如果没有配置系统提示词，会使用默认值
4. **立即生效**: 修改系统提示词后，下次发言时立即生效

## 🎉 完成时间

2026-02-25 14:45

---

**状态**: ✅ 已修复并部署到生产环境
