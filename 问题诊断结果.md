# 智能体发言后点评功能未运行 - 问题诊断结果

## 问题确认 ✅

已确认问题：**DeepSeek API Key 未配置**

## 根本原因

`.env` 文件中的 `DEEPSEEK_API_KEY` 仍然是占位符：
```
DEEPSEEK_API_KEY=your_deepseek_api_key_here
```

这导致以下三个功能无法正常工作：

1. ❌ **消息评分功能** - 智能体发言后的自动点评
2. ❌ **对话总结功能** - 定期生成对话总结和建议
3. ❌ **自动生成新话题** - 话题关闭后自动创建新话题

## 当前系统状态

| 检查项 | 状态 | 说明 |
|--------|------|------|
| DeepSeek API Key | ❌ 未配置 | 使用占位符 |
| Celery Worker | ✅ 正常运行 | 7个进程 |
| 消息评分记录 | ⚠️ 0条 | API Key未配置导致 |
| 对话总结记录 | ⚠️ 0条 | API Key未配置导致 |
| 话题数量 | ✅ 1个活跃 | 手动创建的话题 |

## 解决方案（3步）

### 第1步：获取 DeepSeek API Key

1. 访问 [DeepSeek 平台](https://platform.deepseek.com/)
2. 注册/登录账号
3. 进入 "API Keys" 页面
4. 点击 "Create API Key"
5. 复制生成的 Key（格式：`sk-xxxxxxxxxxxxxxxxxxxxxxxx`）

### 第2步：配置到 .env 文件

编辑项目根目录的 `.env` 文件：

```bash
# 将这一行：
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# 替换为：
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx  # 你的真实 API Key
```

### 第3步：重启 Celery Worker

```bash
# 停止现有的 Celery 进程
pkill -f 'celery.*worker'
pkill -f 'celery.*beat'

# 重新启动
python quick_start.py
```

## 验证配置

配置完成后，运行以下命令验证：

```bash
# 快速诊断
python test_scoring_issue.py

# 完整功能检查
python check_deepseek_features.py
```

成功的输出应该显示：
```
✅ DeepSeek API Key 已配置
✅ API Key 有效
```

## 测试功能

配置完成后，测试各项功能：

### 1. 测试消息评分

在前端页面或管理后台的智能体模拟器中发送几条消息，然后查询评分记录：

```bash
python -c "
from models.database import SessionLocal
from models.models import MessageRelevanceScore

db = SessionLocal()
scores = db.query(MessageRelevanceScore).order_by(MessageRelevanceScore.evaluated_at.desc()).limit(5).all()

print('最近的评分记录：')
for score in scores:
    print(f'评分: {score.relevance_score}, 评论: {score.evaluation_comment}')

db.close()
"
```

### 2. 测试对话总结

发送足够多的消息（超过 8000 tokens），系统会自动生成总结。

### 3. 测试自动生成新话题

关闭一个话题（双方智能体同意结束），系统会自动生成新话题。

## 成本说明

DeepSeek API 非常经济：

- **每日成本**：约 ¥0.11（假设 100 条消息）
- **每月成本**：约 ¥3.3

详细成本估算请查看 `DeepSeek_API配置指南.md`

## 相关文档

我已经创建了以下文档帮助你：

1. **消息评分功能故障排查报告.md** - 详细的评分功能排查指南
2. **DeepSeek_API配置指南.md** - 完整的配置和使用指南
3. **test_scoring_issue.py** - 快速诊断脚本
4. **check_deepseek_features.py** - 完整功能检查脚本

## 技术细节

### 评分流程

```
消息创建
    ↓
触发 Celery 任务: evaluate_message_relevance.delay()
    ↓
Celery Worker 执行任务
    ↓
调用 MessageScoringService.evaluate_message()
    ↓
调用 DeepSeekClient.evaluate_message_relevance()
    ↓
发送请求到 DeepSeek API ← 这里需要有效的 API Key
    ↓
保存评分到 message_relevance_scores 表
```

### 为什么 Celery Worker 运行但评分不工作？

- Celery Worker 正常运行 ✅
- 评分任务被正确触发 ✅
- 任务代码逻辑正确 ✅
- **但是 DeepSeek API Key 无效** ❌

当 API Key 无效时，`DeepSeekClient.evaluate_message_relevance()` 会：
1. 检测到 API Key 未配置
2. 记录错误日志：`"DeepSeek API key not configured"`
3. 返回 `None`（不抛出异常）
4. 评分任务结束，但没有保存任何评分记录

这是一个**静默失败**的设计，不会影响消息的正常发送和显示。

## 下一步

1. ✅ 问题已诊断：API Key 未配置
2. ⏳ 等待你配置 API Key
3. ⏳ 重启 Celery Worker
4. ⏳ 测试功能是否正常

配置完成后，所有三个功能（消息评分、对话总结、自动生成新话题）都会自动开始工作。

## 需要帮助？

如果配置后仍有问题，请：

1. 运行 `python check_deepseek_features.py` 查看详细状态
2. 查看 Celery Worker 日志
3. 检查数据库中是否有新的评分记录

祝配置顺利！🎉
