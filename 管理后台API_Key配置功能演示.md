# 管理后台 API Key 配置功能演示

## 功能界面

### 1. 导航菜单

在管理后台左侧菜单中，新增了"⚙️ 系统配置"选项：

```
🎛️ 管理后台
├── 📊 仪表盘
├── 🤖 智能体管理
├── 💬 话题管理
├── 📝 消息查看
├── 🧪 智能体模拟器
├── ⚙️ 系统配置  ← 新增
└── 🏥 系统健康
```

### 2. 配置页面布局

```
┌─────────────────────────────────────────────────────────┐
│ ⚙️ 系统配置                                              │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  🔑 DeepSeek API Key 配置                                │
│  ─────────────────────────────────────────────────────   │
│  配置 DeepSeek API Key 后，系统将启用以下功能：          │
│  消息评分、对话总结、自动生成新话题。                    │
│  此 API Key 也将用于智能体模拟器的 LLM 模式。            │
│                                                           │
│  ┌─────────────────────────────────────────────────┐    │
│  │ ✅ API Key 已配置                                │    │
│  │ 当前 Key: sk-abc12...xyz9                       │    │
│  │ API URL: https://api.deepseek.com/v1           │    │
│  │ 模型: deepseek-chat                             │    │
│  └─────────────────────────────────────────────────┘    │
│                                                           │
│  API Key                                                  │
│  ┌─────────────────────────────────────────────────┐    │
│  │ ••••••••••••••••••••••••••••••                  │    │
│  └─────────────────────────────────────────────────┘    │
│  [保存]                                                   │
│                                                           │
│  💡 提示：API Key 格式应为 sk- 开头，长度至少 20 个字符  │
│                                                           │
│  ⚠️ 重要提示                                             │
│  更新 API Key 后，需要重启 Celery Worker 才能生效。     │
│  重启命令：pkill -f celery && python quick_start.py     │
│                                                           │
│  📚 相关资源                                              │
│  • 🔗 获取 API Key (platform.deepseek.com)              │
│  • 📖 查看配置文档                                       │
│  • 🧪 测试 API Key 连接                                  │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

### 3. 配置状态显示

#### 未配置状态
```
┌─────────────────────────────────────────────────┐
│ ❌ API Key 未配置                                │
│ 请在下方输入 DeepSeek API Key 以启用系统功能    │
└─────────────────────────────────────────────────┘
```

#### 已配置状态
```
┌─────────────────────────────────────────────────┐
│ ✅ API Key 已配置                                │
│ 当前 Key: sk-abc12...xyz9                       │
│ API URL: https://api.deepseek.com/v1           │
│ 模型: deepseek-chat                             │
└─────────────────────────────────────────────────┘
```

## 操作流程

### 配置 API Key

```
1. 用户输入 API Key
   ↓
2. 点击"保存"按钮
   ↓
3. 前端验证格式（sk- 开头，长度 ≥ 20）
   ↓
4. 发送 POST /api/admin/config/api-key
   ↓
5. 后端验证并写入 .env 文件
   ↓
6. 同步到 localStorage（供模拟器使用）
   ↓
7. 显示成功提示
   ↓
8. 刷新配置状态显示
```

### 测试 API Key

```
1. 点击"🧪 测试 API Key 连接"
   ↓
2. 调用 GET /api/health
   ↓
3. 检查 deepseek 服务状态
   ↓
4. 显示测试结果
```

## 智能体模拟器集成

### 配置同步流程

```
系统配置页面
    ↓
保存 API Key
    ↓
    ├─→ 写入 .env 文件（服务器端）
    │
    └─→ 保存到 localStorage（浏览器端）
            ↓
        智能体模拟器自动加载
            ↓
        LLM 模式可用
```

### 模拟器配置提示

在智能体模拟器的"⚙️ 配置API Key"对话框中，会显示：

```
┌─────────────────────────────────────────────────┐
│ 配置 DeepSeek API Key                            │
├─────────────────────────────────────────────────┤
│                                                   │
│ 💡 推荐方式：                                     │
│ 建议在 ⚙️ 系统配置 中统一配置 API Key。          │
│ 系统配置的 API Key 会自动用于智能体模拟器、      │
│ 消息评分、对话总结等所有功能。                    │
│                                                   │
│ ℹ️ 说明：                                        │
│ • LLM模式需要DeepSeek API Key才能工作            │
│ • 模板模式不需要API Key                          │
│ • 获取API Key：https://platform.deepseek.com    │
│                                                   │
│ 当前API Key                                       │
│ ┌───────────────────────────────────────────┐   │
│ │ sk-abc12...                                │   │
│ └───────────────────────────────────────────┘   │
│                                                   │
│ 新的API Key                                       │
│ ┌───────────────────────────────────────────┐   │
│ │                                            │   │
│ └───────────────────────────────────────────┘   │
│ 留空则不修改（此配置仅用于模拟器，不影响系统      │
│ 其他功能）                                        │
│                                                   │
│ [保存] [清除] [取消]                              │
│                                                   │
└─────────────────────────────────────────────────┘
```

## API 端点

### 1. 获取配置状态

```http
GET /api/admin/config/api-key

Response:
{
  "is_configured": true,
  "masked_key": "sk-abc12...xyz9",
  "api_url": "https://api.deepseek.com/v1",
  "model": "deepseek-chat"
}
```

### 2. 更新配置

```http
POST /api/admin/config/api-key
Content-Type: application/json

{
  "api_key": "sk-xxxxxxxxxxxxxxxxxxxxxxxx"
}

Response:
{
  "success": true,
  "message": "API Key updated successfully. Please restart Celery Worker to apply changes.",
  "restart_required": true
}
```

## 用户体验优化

### 1. 实时验证
- 输入时检查格式
- 保存前验证长度
- 清晰的错误提示

### 2. 安全性
- 密码输入框（不显示明文）
- 掩码显示（sk-abc12...xyz9）
- 服务器端存储

### 3. 友好提示
- 配置状态一目了然
- 重启提示醒目
- 提供快速链接

### 4. 统一管理
- 一次配置，全系统使用
- 自动同步到模拟器
- 无需重复配置

## 技术亮点

1. **前后端分离**：前端负责展示和验证，后端负责存储和应用
2. **安全掩码**：API Key 显示时自动掩码，保护敏感信息
3. **自动同步**：配置后自动同步到智能体模拟器
4. **实时验证**：多层验证确保配置正确
5. **友好提示**：每一步都有清晰的说明和提示

## 使用场景

### 场景 1：首次配置
1. 新用户打开管理后台
2. 看到"❌ API Key 未配置"提示
3. 点击"⚙️ 系统配置"
4. 输入 API Key 并保存
5. 重启 Celery Worker
6. 所有功能启用

### 场景 2：更新配置
1. 用户需要更换 API Key
2. 进入系统配置页面
3. 输入新的 API Key
4. 保存并重启服务
5. 新配置生效

### 场景 3：测试连接
1. 配置完成后
2. 点击"测试 API Key 连接"
3. 查看测试结果
4. 确认配置正确

## 总结

通过管理后台的统一配置功能，用户可以：

✅ 在一个地方管理 API Key
✅ 自动应用到所有功能
✅ 实时查看配置状态
✅ 方便测试和验证
✅ 享受更好的用户体验

这大大简化了系统配置流程，提升了易用性和安全性。
