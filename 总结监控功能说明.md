# 总结监控功能说明

## 功能概述

在管理后台新增「📋 总结监控」页面，用于实时监控和管理系统的总结功能，包括总结任务状态、话题 Token 计数、队列状态等。

## 新增 API 端点

### 1. GET /api/admin/summary/stats
获取总结系统统计信息和诊断数据。

**响应数据**:
```json
{
  "total_topics": 10,
  "total_summaries": 25,
  "total_jobs": 30,
  "job_stats": {
    "pending": 1,
    "processing": 0,
    "done": 28,
    "failed": 1
  },
  "recent_24h_summaries": 5,
  "topics_over_threshold": 2,
  "threshold": 2000,
  "config": {
    "provider": "deepseek",
    "model": "deepseek-chat",
    "api_key_configured": true,
    "api_key_preview": "sk-xxxxx..."
  },
  "worker_online": true,
  "summary_queue_length": 0,
  "topics": [...],
  "recent_jobs": [...]
}
```

### 2. POST /api/admin/summary/trigger/{topic_id}
手动触发指定话题的总结任务。

**参数**:
- `topic_id`: 话题 ID（路径参数）

**响应**:
```json
{
  "success": true,
  "job_id": "job-uuid",
  "message": "已为话题「xxx」创建总结任务"
}
```

### 3. POST /api/admin/summary/test
测试总结 LLM API 连通性。

**响应**:
```json
{
  "success": true,
  "provider": "deepseek",
  "model": "deepseek-chat",
  "result": {
    "summary": "测试总结内容",
    "suggestion": "continue",
    "end_score": 75
  },
  "duration_ms": 1234
}
```

## 前端页面功能

### 1. 统计指标卡片

显示 8 个关键指标：

**第一行**:
- 总总结数：系统生成的总结总数
- 近24小时总结：最近一天的总结数量
- 需要总结：Token 计数超过阈值的话题数
- 队列任务：summary_jobs 队列中的待处理任务数

**第二行**:
- 已完成：done 状态的任务数
- 待处理：pending 状态的任务数
- 处理中：processing 状态的任务数
- 失败：failed 状态的任务数

### 2. 配置信息

显示总结系统的配置：
- LLM 提供商（DeepSeek/MiniMax）
- 使用的模型
- API Key 配置状态
- Celery Worker 在线状态

### 3. 话题状态表格

显示最近 20 个话题的总结状态：
- 话题标题
- 状态（active/closing_pending/closed）
- Token 计数（超过阈值时标红）
- 待处理状态（需要总结/处理中/正常）
- LLM 建议（continue/change_angle/suggest_end/force_end）
- 结束评分
- 操作按钮（触发总结）

### 4. 总结任务表格

显示最近 10 个总结任务的执行情况：
- 话题标题
- 任务状态（done/pending/processing/failed）
- 重试次数
- 错误信息（如果失败）
- 创建时间
- 执行耗时

### 5. 操作按钮

**刷新**: 重新加载总结统计数据

**测试API**: 测试总结 LLM API 的连通性，验证配置是否正确

**触发总结**: 为指定话题手动创建总结任务（即使未达到阈值）

## 使用场景

### 1. 监控总结覆盖率
查看有多少话题需要总结，是否有积压的任务。

### 2. 诊断总结失败
查看失败的任务，了解失败原因（API 错误、配置问题等）。

### 3. 手动触发总结
对于重要话题，可以手动触发总结，无需等待达到阈值。

### 4. 验证配置
测试 LLM API 连通性，确保总结功能正常工作。

### 5. 监控队列状态
查看 summary_jobs 队列长度，了解任务处理速度。

## 状态说明

### 话题状态
- **active**: 活跃话题，可以继续对话
- **closing_pending**: 等待关闭确认
- **closed**: 已关闭

### 任务状态
- **pending**: 等待处理
- **processing**: 正在处理
- **done**: 已完成
- **failed**: 失败

### LLM 建议
- **continue**: 继续对话
- **change_angle**: 建议换个角度
- **suggest_end**: 建议考虑结束
- **force_end**: 应该结束对话

## 颜色编码

### Token 计数
- 绿色/灰色：正常（< 阈值）
- 红色：超过阈值，需要总结

### 任务状态
- 绿色：done（已完成）
- 橙色：pending（待处理）
- 蓝色：processing（处理中）
- 红色：failed（失败）

### Worker 状态
- 绿色：在线
- 红色：离线

### 队列长度
- 绿色：0（无积压）
- 橙色：>0（有积压）

## 技术实现

### 后端
- 使用 SQLAlchemy 查询话题、总结历史、总结任务
- 从系统配置读取 LLM 提供商和阈值
- 检查 Celery Worker 状态和 Redis 队列长度
- 支持手动触发总结任务

### 前端
- 使用 Fetch API 调用后端接口
- 动态渲染统计卡片和表格
- 实时显示 API 测试结果
- 支持手动刷新和触发操作

## 相关文件

### 后端
- `api/routes.py` - 新增 3 个总结监控端点

### 前端
- `frontend/admin.html` - 新增总结监控页面和 JavaScript 函数

### 相关服务
- `services/queue_service.py` - 总结任务入队
- `workers/tasks.py` - 总结任务处理
- `models/models.py` - Topic, SummaryJob, SummaryHistory 模型

## 与评分监控的对比

| 功能 | 评分监控 | 总结监控 |
|------|---------|---------|
| 监控对象 | 消息评分 | 话题总结 |
| 主要指标 | 评分覆盖率、平均分 | 总结数量、任务状态 |
| 操作 | 重试未评分消息 | 手动触发总结 |
| 队列 | default | summary_jobs |
| 触发条件 | 每条消息 | Token 达到阈值 |

## 后续优化建议

1. **自动刷新**: 添加定时自动刷新功能
2. **任务详情**: 点击任务查看详细的总结内容
3. **批量操作**: 支持批量触发多个话题的总结
4. **历史趋势**: 显示总结数量的时间趋势图
5. **性能监控**: 显示总结任务的平均耗时和成功率
