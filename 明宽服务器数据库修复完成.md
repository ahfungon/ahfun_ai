# 明宽服务器数据库修复完成报告

## 问题描述

明宽服务器监控页面看不到内容，API 返回错误。

## 问题原因

1. **数据库表结构不完整**
   - 缺少 `topic_description` 字段
   - 缺少 `message_relevance_scores` 表

2. **数据未同步**
   - 服务器数据库为空
   - 本地有 30 个话题、95 条消息需要同步

## 解决方案

### 1. 添加缺失的数据库字段和表

```sql
-- 添加 topic_description 字段
ALTER TABLE topics ADD COLUMN IF NOT EXISTS topic_description TEXT;

-- 创建 message_relevance_scores 表
CREATE TABLE IF NOT EXISTS message_relevance_scores (
    id VARCHAR(36) PRIMARY KEY,
    message_id VARCHAR(36) NOT NULL REFERENCES messages(id),
    topic_id VARCHAR(36) NOT NULL REFERENCES topics(id),
    agent_id VARCHAR(36) NOT NULL REFERENCES agents(id),
    relevance_score FLOAT,
    evaluation_comment TEXT,
    evaluated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

### 2. 同步本地数据到服务器

使用 `sync_via_ssh.sh` 脚本：
- 导出本地数据为 JSON 格式
- 通过 SSH 上传到服务器
- 在服务器上使用 Python 导入数据
- 重启所有服务

## 执行步骤

```bash
# 1. 添加数据库字段（已完成）
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'PGPASSWORD=dual_agent_pass psql ...'

# 2. 同步数据（已完成）
./sync_via_ssh.sh

# 3. 验证
curl http://129.211.28.211:8080/api/monitor/topic/active
```

## 验证结果

### 数据库状态
```
智能体: 9
话题: 30
消息: 95
```

### API 测试
```bash
$ curl http://129.211.28.211:8080/api/monitor/topic/active
{
  "topic_id": "e6bbe039-2656-4df7-87af-6547ca457810",
  "title": "量子生物接口：当意识上传遭遇纳米神经编织的伦理边界",
  "status": "active",
  ...
}
```

### 服务状态
- ✓ FastAPI: 运行中
- ✓ Celery Worker: 运行中
- ✓ Celery Beat: 运行中
- ✓ Nginx: 运行中
- ✓ Redis: 运行中
- ✓ PostgreSQL: 运行中

## 访问地址

- **监控页面**: http://129.211.28.211:8080/monitor.html
- **聊天页面**: http://129.211.28.211:8080/index.html
- **管理页面**: http://129.211.28.211:8080/admin.html
- **历史记录**: http://129.211.28.211:8080/history.html
- **API 文档**: http://129.211.28.211:8080/docs

## 创建的工具脚本

1. **sync_via_ssh.sh** - 通过 SSH 同步数据到服务器
   - 导出本地数据为 JSON
   - 上传并在服务器上导入
   - 自动重启服务

2. **fix_mingkuan_database.sh** - 数据库修复脚本（已弃用）
   - 使用 Alembic 创建表结构
   - 尝试导出/导入数据

3. **sync_data_to_mingkuan.py** - Python 数据同步脚本（需要 PostgreSQL 端口开放）

## 后续建议

### 1. 保持代码和数据库结构同步

在本地修改数据库结构后，需要同步到服务器：

```bash
# 同步 Alembic 迁移文件
rsync -avz alembic/ ubuntu@129.211.28.211:~/dual-agent-chat/alembic/

# 在服务器上运行迁移
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
cd ~/dual-agent-chat
source venv/bin/activate
alembic upgrade head
sudo systemctl restart dual-agent-api
ENDSSH
```

### 2. 定期备份数据

```bash
# 在服务器上设置定时备份
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
mkdir -p ~/backups
PGPASSWORD='dual_agent_pass' pg_dump -h localhost -U dual_agent_user -d dual_agent_chat \
    -f ~/backups/dual_agent_chat_$(date +%Y%m%d_%H%M%S).sql
ENDSSH
```

### 3. 使用完整部署脚本

下次部署时使用 `deploy_to_mingkuan.sh`，它会：
- 自动导出本地数据库
- 同步代码和配置
- 在服务器上重建数据库
- 导入数据
- 重启所有服务

```bash
./deploy_to_mingkuan.sh
```

## 问题总结

本次问题的根本原因是：
1. 服务器上的数据库结构与本地不一致（缺少新增字段）
2. 数据库为空，没有导入测试数据
3. 部署脚本没有正确处理数据库迁移和数据导入

已通过手动修复数据库结构和同步数据解决。建议后续使用统一的部署流程，确保代码、数据库结构和数据的一致性。

## 修复时间

2026-02-15 18:00 - 18:30 (30分钟)

## 状态

✅ 已完成 - 监控页面正常显示，所有服务运行正常
