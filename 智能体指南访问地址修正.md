# 智能体指南访问地址修正

## 问题

之前提供的地址 `http://129.211.28.211:8080/ai-agent-guide.html` 会被 Nginx 重定向到监控页面。

## 原因

Nginx 配置中：
```nginx
location / {
    root /home/ubuntu/dual-agent-chat/frontend;
    try_files $uri $uri/ /monitor.html;
    index monitor.html;
}
```

- `root` 指向 `frontend` 目录
- `ai-agent-guide.html` 原本在 `static` 目录
- 找不到文件时会回退到 `monitor.html`

## 解决方案

将 `ai-agent-guide.html` 复制到 `frontend` 目录，这样两个地址都能访问：

1. **推荐地址**（简短）：
   ```
   http://129.211.28.211:8080/ai-agent-guide.html
   ```

2. **备用地址**（完整路径）：
   ```
   http://129.211.28.211:8080/static/ai-agent-guide.html
   ```

## 文件位置

- **本地**：
  - `static/ai-agent-guide.html` （源文件）
  - `frontend/ai-agent-guide.html` （副本）

- **服务器**：
  - `/home/ubuntu/dual-agent-chat/static/ai-agent-guide.html` （源文件）
  - `/home/ubuntu/dual-agent-chat/frontend/ai-agent-guide.html` （副本）

## 验证

```bash
# 测试推荐地址
curl -I http://129.211.28.211:8080/ai-agent-guide.html

# 测试备用地址
curl -I http://129.211.28.211:8080/static/ai-agent-guide.html
```

两个地址都应该返回 `200 OK`。

## 更新的文档

需要更新以下文档中的链接：

- ✓ `智能体对话完整指南.md`
- ✓ `智能体快速参考.md`
- ✓ `智能体使用指南_README.md`
- ✓ `智能体指南HTML更新完成.md`
- ✓ `智能体指南创建完成.md`

## 部署说明

在部署脚本中添加复制命令，确保文件同步到两个位置：

```bash
# 在 deploy_to_mingkuan.sh 中添加
rsync -avz static/ai-agent-guide.html ubuntu@129.211.28.211:~/dual-agent-chat/static/
rsync -avz static/ai-agent-guide.html ubuntu@129.211.28.211:~/dual-agent-chat/frontend/
```

或者在服务器上设置符号链接：

```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 \
  'ln -sf ~/dual-agent-chat/static/ai-agent-guide.html ~/dual-agent-chat/frontend/ai-agent-guide.html'
```

## 正确的访问地址

✅ **http://129.211.28.211:8080/ai-agent-guide.html**

这是推荐使用的地址，简短易记。
