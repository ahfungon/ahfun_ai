# 部署到明宽服务器 - 快速开始

## 三步完成部署

### 第一步：部署前检查

```bash
./pre_deploy_check.sh
```

这个脚本会自动检查：
- ✓ SSH 密钥配置
- ✓ 环境配置文件
- ✓ 必要工具安装（rsync, pg_dump）
- ✓ 服务器连接
- ✓ 服务器环境（PostgreSQL, Redis, Nginx）
- ✓ 本地数据库状态

### 第二步：执行部署

```bash
./deploy_to_mingkuan.sh
```

这个脚本会自动完成：
1. 导出本地数据库 → `dual_agent_chat_data.sql`
2. 同步代码到服务器（排除 venv, .git 等）
3. 复制 `.env` 配置文件
4. 在服务器上安装 Python 依赖
5. 重建数据库并导入数据
6. 重启所有服务（API, Celery Worker, Celery Beat, Nginx）
7. 验证部署状态

### 第三步：验证部署

```bash
./check_mingkuan_status.sh
```

这个脚本会检查：
- ✓ API 健康状态
- ✓ 话题数量
- ✓ 所有服务运行状态
- ✓ 数据库连接
- ✓ 网页可访问性

## 访问地址

部署成功后，通过以下地址访问：

| 页面 | 地址 | 说明 |
|------|------|------|
| 监控页面 | http://129.211.28.211:8080/monitor.html | 实时监控智能体对话 |
| 聊天页面 | http://129.211.28.211:8080/index.html | 用户聊天界面 |
| 管理页面 | http://129.211.28.211:8080/admin.html | 系统管理界面 |
| 历史记录 | http://129.211.28.211:8080/history.html | 查看历史对话 |
| API 文档 | http://129.211.28.211:8080/docs | Swagger API 文档 |

## 常用命令

### 查看实时日志

```bash
# API 日志
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'tail -f ~/dual-agent-chat/logs/api.log'

# Worker 日志
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'tail -f ~/dual-agent-chat/logs/worker.log'

# Beat 日志
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'tail -f ~/dual-agent-chat/logs/beat.log'
```

### 重启服务

```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
sudo systemctl restart dual-agent-api
sudo systemctl restart dual-agent-celery
sudo systemctl restart dual-agent-celery-beat
ENDSSH
```

### 查看服务状态

```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
sudo systemctl status dual-agent-api --no-pager
sudo systemctl status dual-agent-celery --no-pager
sudo systemctl status dual-agent-celery-beat --no-pager
ENDSSH
```

### 查看数据库

```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
PGPASSWORD='dual_agent_pass' psql -h localhost -U dual_agent_user -d dual_agent_chat -c "
SELECT 
    t.id,
    t.title,
    t.status,
    COUNT(m.id) as message_count
FROM topics t
LEFT JOIN messages m ON t.id = m.topic_id
GROUP BY t.id
ORDER BY t.created_at DESC
LIMIT 10;
"
ENDSSH
```

## 故障排查

### 问题 1: 部署脚本执行失败

**症状**: `./deploy_to_mingkuan.sh` 报错

**解决方案**:
1. 先运行 `./pre_deploy_check.sh` 检查环境
2. 确保 SSH 密钥权限正确：`chmod 600 ~/.ssh/mingkuan.pem`
3. 检查网络连接

### 问题 2: 服务无法启动

**症状**: 访问网页显示 502 Bad Gateway

**解决方案**:
```bash
# 查看 API 日志
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'tail -50 ~/dual-agent-chat/logs/api.log'

# 查看系统日志
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'journalctl -u dual-agent-api -n 50'

# 重启服务
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'sudo systemctl restart dual-agent-api'
```

### 问题 3: 数据库连接失败

**症状**: API 日志显示数据库连接错误

**解决方案**:
```bash
# 检查 PostgreSQL 状态
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'sudo systemctl status postgresql'

# 测试数据库连接
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'PGPASSWORD=dual_agent_pass psql -h localhost -U dual_agent_user -d dual_agent_chat -c "SELECT 1;"'

# 重启 PostgreSQL
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'sudo systemctl restart postgresql'
```

### 问题 4: Redis 连接失败

**症状**: Celery Worker 无法启动

**解决方案**:
```bash
# 检查 Redis 状态
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'redis-cli ping'

# 重启 Redis
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'sudo systemctl restart redis-server'
```

## 增量更新

如果只是更新代码，不需要重建数据库：

```bash
# 只同步代码
rsync -avz --progress \
    --exclude 'venv' \
    --exclude '.git' \
    --exclude '__pycache__' \
    --exclude '.hypothesis' \
    --exclude '*.pyc' \
    --exclude 'dual_agent_chat_data.sql' \
    -e "ssh -i ~/.ssh/mingkuan.pem" \
    ./ ubuntu@129.211.28.211:~/dual-agent-chat/

# 重启服务
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
cd ~/dual-agent-chat
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart dual-agent-api
sudo systemctl restart dual-agent-celery
sudo systemctl restart dual-agent-celery-beat
ENDSSH
```

## 备份与恢复

### 备份当前数据

```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
mkdir -p ~/backups
PGPASSWORD='dual_agent_pass' pg_dump -h localhost -U dual_agent_user -d dual_agent_chat \
    -f ~/backups/dual_agent_chat_$(date +%Y%m%d_%H%M%S).sql
echo "备份完成"
ls -lh ~/backups/
ENDSSH
```

### 恢复数据

```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
# 选择要恢复的备份文件
BACKUP_FILE=~/backups/dual_agent_chat_20240215_120000.sql

# 恢复数据库
PGPASSWORD='dual_agent_pass' psql -h localhost -U dual_agent_user -d dual_agent_chat -f "$BACKUP_FILE"

# 重启服务
sudo systemctl restart dual-agent-api
sudo systemctl restart dual-agent-celery
sudo systemctl restart dual-agent-celery-beat
ENDSSH
```

## 性能监控

### 查看系统资源

```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
echo "=== CPU 和内存 ==="
top -bn1 | head -20

echo ""
echo "=== 磁盘使用 ==="
df -h

echo ""
echo "=== 进程状态 ==="
ps aux | grep -E "uvicorn|celery|nginx" | grep -v grep
ENDSSH
```

### 查看 Celery 队列

```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
cd ~/dual-agent-chat
source venv/bin/activate
celery -A workers.celery_app inspect active
celery -A workers.celery_app inspect stats
ENDSSH
```

## 安全建议

1. **定期更新系统**
   ```bash
   ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'sudo apt update && sudo apt upgrade -y'
   ```

2. **定期备份数据库**（建议每天）
   ```bash
   # 可以设置 cron 任务自动备份
   ```

3. **监控日志文件大小**
   ```bash
   ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'du -sh ~/dual-agent-chat/logs/*'
   ```

4. **使用 HTTPS**（生产环境推荐）
   - 配置 SSL 证书
   - 使用 Let's Encrypt 免费证书

## 需要帮助？

查看详细文档：
- `部署到明宽服务器指南.md` - 完整部署指南
- `部署前检查清单.md` - 详细检查清单
- `README.md` - 项目总体说明

查看日志：
```bash
# API 日志
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'tail -100 ~/dual-agent-chat/logs/api.log'

# 系统日志
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'journalctl -u dual-agent-api -n 100'
```
