# 前端看不到发言内容 - 解决方案

## 问题诊断

### 当前状态
✅ 智能体正在运行  
✅ 话题已创建  
✅ 已有 2 条消息  
❌ LLM API 调用失败（401 Unauthorized）  
❌ 使用备用回复："我认为这个话题很有意义，值得深入探讨。"

### 根本原因

**DeepSeek API Key 未设置**

智能体无法调用 LLM API 生成真实的对话内容，只能使用备用回复。

## 解决方案

### 方法 1: 设置环境变量（推荐）

```bash
# 设置 API Key
export DEEPSEEK_API_KEY="your-api-key-here"

# 验证
echo $DEEPSEEK_API_KEY

# 重启智能体
pkill -f 'autonomous_agent.py'
./快速测试关闭协商.sh
```

### 方法 2: 修改配置文件

编辑 `simulation_test/agent_config.yaml`:

```yaml
llm:
  api_key: "your-api-key-here"  # 直接填写 API key
  api_url: "https://api.deepseek.com/v1"
  model: "deepseek-chat"
  temperature: 0.7
  max_tokens: 500
  timeout: 30
```

然后重启智能体：
```bash
pkill -f 'autonomous_agent.py'
./快速测试关闭协商.sh
```

### 方法 3: 使用 .env 文件

创建 `.env` 文件：
```bash
echo "DEEPSEEK_API_KEY=your-api-key-here" > .env
```

然后加载环境变量：
```bash
source .env
./快速测试关闭协商.sh
```

## 验证 LLM 是否工作

### 1. 检查日志

```bash
tail -f simulation_test/logs/agent-alice.log | grep "LLM"
```

**成功的日志:**
```
[时间] 🤔 LLM推理中...
[时间] ✓ 生成回复 (150 tokens)
[时间] 【我的发言】
  "关于人工智能的发展，我认为..."
```

**失败的日志:**
```
[时间] 🤔 LLM推理中...
[时间] ❌ LLM 生成失败: 401 Client Error: Unauthorized
[时间] 【我的发言】
  "我认为这个话题很有意义，值得深入探讨。"
```

### 2. 检查消息内容

```bash
python3 -c "
from models.database import SessionLocal
from models.models import Message, Agent

db = SessionLocal()
messages = db.query(Message).order_by(Message.created_at.desc()).limit(3).all()

for msg in reversed(messages):
    agent = db.query(Agent).filter(Agent.id == msg.agent_id).first()
    print(f'{agent.name if agent else \"Unknown\"}: {msg.content[:100]}')
    print()

db.close()
"
```

**LLM 工作正常的输出:**
- 内容丰富，有具体观点
- 长度通常 150-250 字
- 有互动和回应

**LLM 失败的输出:**
- 内容简短："我认为这个话题很有意义，值得深入探讨。"
- 所有消息都一样

## 前端监控页面

### 为什么前端看不到内容？

前端监控页面通过 API 获取消息，如果：
1. ✅ 数据库中有消息 → 前端应该能看到
2. ❌ 数据库中没有消息 → 前端显示空白

### 检查前端是否正常

1. **打开前端页面:**
   ```
   http://localhost:8080/monitor.html
   ```

2. **打开浏览器控制台** (F12)

3. **查看网络请求:**
   - 应该看到对 `/api/topic/active` 的请求
   - 应该看到对 `/api/topic/{id}/messages` 的请求

4. **检查响应:**
   - 如果返回 200 且有数据 → 前端应该显示
   - 如果返回 404 或错误 → 检查 API 服务

### 手动测试 API

```bash
# 获取活跃话题
curl -s http://localhost:8000/api/topic/active | jq

# 获取消息（替换 topic_id）
curl -s "http://localhost:8000/api/topic/94f69009-0e0b-4067-b6c6-c265863b9c04/messages?limit=10" | jq
```

## 当前状态总结

### 问题
- ❌ DeepSeek API Key 未设置
- ❌ LLM 调用失败
- ❌ 只有备用回复

### 影响
- ✅ 智能体可以发送消息
- ✅ 前端可以看到消息
- ❌ 消息内容不是真实的对话
- ❌ 无法测试关闭协商功能（因为 LLM 不会生成"可以结束了"的内容）

### 解决步骤

1. **设置 API Key:**
   ```bash
   export DEEPSEEK_API_KEY="your-api-key-here"
   ```

2. **重启智能体:**
   ```bash
   pkill -f 'autonomous_agent.py'
   ./快速测试关闭协商.sh
   ```

3. **验证 LLM 工作:**
   ```bash
   tail -f simulation_test/logs/agent-alice.log | grep "LLM"
   ```

4. **查看前端:**
   ```
   http://localhost:8080/monitor.html
   ```

## 临时解决方案（无需 API Key）

如果暂时没有 API Key，可以修改智能体代码使用模拟回复：

编辑 `simulation_test/autonomous_agent.py`，在 `LLMClient.generate` 方法中：

```python
def generate(self, system_prompt: str, user_prompt: str) -> Tuple[str, int]:
    """生成回复"""
    # 临时：使用模拟回复
    import random
    
    responses = [
        "关于人工智能的发展，我认为深度学习和大模型是关键方向。",
        "我同意之前的观点，并且想补充一点：AI 的伦理问题也很重要。",
        "从数据的角度来看，AI 需要更多高质量的训练数据。",
        "我认为这个话题可以结束了，我们已经讨论得很充分。",
        "我同意结束这个话题，感谢大家的参与。"
    ]
    
    content = random.choice(responses)
    tokens = len(content) * 2
    
    return content, tokens
```

但这样无法真正测试智能体的对话能力。

## 推荐方案

**获取 DeepSeek API Key:**

1. 访问 https://platform.deepseek.com/
2. 注册账号
3. 获取 API Key
4. 设置环境变量
5. 重启智能体

这样才能看到真实的智能体对话和测试关闭协商功能。
