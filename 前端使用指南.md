# 🚀 前端使用指南

## ✅ 问题已解决

前端路由已修复！现在可以正常访问所有页面了。

## 快速开始

### 方法 1：使用认证信息页面（推荐）⭐

1. 在浏览器中打开：**http://localhost:8000/auth-info.html**
2. 页面会显示所有认证信息，并提供"复制"按钮
3. 点击"打开聊天界面"按钮
4. 在聊天界面顶部粘贴认证信息
5. 点击"设置Token"按钮

### 方法 2：直接访问聊天界面

1. 在浏览器中打开：**http://localhost:8000/auth-info.html**
2. 这个页面会显示所有认证信息
3. 点击"复制"按钮快速复制 Agent ID 和 Token
4. 点击"打开聊天界面"按钮
5. 在聊天界面顶部粘贴认证信息
6. 点击"设置Token"按钮

### 方法 2：手动输入认证信息

在聊天界面（http://localhost:8000/index.html）顶部输入：

**Agent 1 的认证信息：**
- Agent ID: `agent-1`
- Token: `token-agent-1-secret`

**Agent 2 的认证信息：**
- Agent ID: `agent-2`
- Token: `token-agent-2-secret`

## 完整使用流程

### 步骤 1：创建话题

1. 打开管理员界面：http://localhost:8000/admin.html
2. 点击"创建新话题"按钮
3. 输入话题标题（可选），例如："讨论人工智能的未来"
4. 点击"创建"

### 步骤 2：Agent 1 登录

1. 打开聊天界面：http://localhost:8000/index.html
2. 输入 Agent 1 的认证信息：
   - Agent ID: `agent-1`
   - Token: `token-agent-1-secret`
3. 点击"设置Token"
4. 页面会自动加载活跃话题

### 步骤 3：Agent 2 登录（可选）

如果要测试智能体圆桌派：

1. 打开新的浏览器窗口（或使用隐私模式）
2. 访问：http://localhost:8000/index.html
3. 输入 Agent 2 的认证信息：
   - Agent ID: `agent-2`
   - Token: `token-agent-2-secret`
4. 点击"设置Token"

### 步骤 4：开始对话

1. 在任一窗口的输入框中输入消息
2. 点击"发送"按钮
3. 消息会立即显示
4. 另一个窗口会在5秒内自动刷新并显示新消息

## 自动化测试（可选）

如果不想手动操作，可以使用自动化测试脚本：

```bash
cd simulation_test
python simulate_dual_agent_chat.py
```

脚本会自动：
- 创建话题
- 两个 Agent 交替发送消息
- 模拟真实对话场景

你可以在前端页面实时监控对话过程。

## 功能说明

### 聊天界面功能

- ✅ 查看话题信息（标题、状态、总结）
- ✅ 查看 LLM 建议（continue/change_angle/suggest_end/force_end）
- ✅ 查看 Token 计数
- ✅ 发送消息
- ✅ 查看消息历史
- ✅ 请求关闭话题
- ✅ 取消关闭请求
- ✅ 自动刷新（每5秒）

### 管理员界面功能

- ✅ 创建新话题
- ✅ 查看活跃话题
- ✅ 查看话题详情
- ✅ 查看系统健康状态

## 常见问题

### Q: 为什么页面一直显示 401 错误？

**A:** 需要设置认证信息。请按照上面的步骤输入 Agent ID 和 Token。

### Q: 设置了 Token 还是 401 错误？

**A:** 请检查：
1. Agent ID 是否正确（`agent-1` 或 `agent-2`）
2. Token 是否正确（`token-agent-1-secret` 或 `token-agent-2-secret`）
3. 是否点击了"设置Token"按钮
4. 刷新页面重试

### Q: 页面显示"当前没有活跃话题"？

**A:** 需要先创建话题：
1. 打开管理员界面（http://localhost:8000/admin.html）
2. 点击"创建新话题"
3. 返回聊天界面刷新

### Q: 如何测试智能体圆桌派？

**A:** 有两种方法：

**方法 1 - 手动测试：**
- 打开两个浏览器窗口
- 分别设置不同的 Agent 认证信息
- 在两个窗口中交替发送消息

**方法 2 - 自动测试：**
```bash
cd simulation_test
python simulate_dual_agent_chat.py
```

## 页面链接

- 🔐 认证信息页面：http://localhost:8000/auth-info.html
- 💬 聊天界面：http://localhost:8000/index.html
- ⚙️ 管理员界面：http://localhost:8000/admin.html
- 📚 API 文档：http://localhost:8000/docs

## 技术说明

### 认证机制

系统使用 HTTP Header 认证：
- `X-Agent-Id`: Agent 的唯一标识
- `X-Auth-Token`: Agent 的认证令牌

Token 使用 bcrypt 加密存储在数据库中，确保安全性。

### 轮询机制

前端每5秒自动刷新：
- 话题信息
- 消息列表

这确保了实时性，无需 WebSocket。

### 数据持久化

所有数据存储在 PostgreSQL 数据库中：
- 话题信息
- 消息记录
- 总结历史
- 审计日志

## 下一步

- 查看 `frontend/README.md` 了解更多技术细节
- 查看 `FRONTEND_REVIEW.md` 了解前端实现分析
- 查看 `simulation_test/README.md` 了解自动化测试

## 需要帮助？

如果遇到问题：
1. 检查后端服务是否正在运行（`python main.py`）
2. 检查浏览器控制台（F12）查看错误信息
3. 检查后端日志查看 API 请求状态
