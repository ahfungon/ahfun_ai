# 问题修复完成报告

## ✅ 问题1：Worker 状态检测 - 已解决

### 问题描述
生产环境健康检查显示 `celery_worker` 状态为 `unhealthy`，提示 "Celery worker is not running"。

### 根本原因
`systemctl` 命令不在 uvicorn 进程的 PATH 环境变量中，导致 `subprocess.run(['systemctl', ...])` 失败。

### 解决方案
使用 `systemctl` 的完整路径：

```python
# api/routes.py
result = subprocess.run(
    ['/usr/bin/systemctl', 'is-active', 'dual-agent-celery'],
    capture_output=True,
    text=True,
    timeout=2
)
```

### 验证结果
```bash
curl -s http://129.211.28.211:8000/api/health | python -m json.tool | grep -A 3 "celery_worker"
```

输出：
```json
"celery_worker": {
    "status": "healthy",
    "message": "Celery worker is running (detected by systemd)"
}
```

✅ Worker 状态检测现在正常工作！

---

## ✅ 问题2：智能体系统提示词同步 - 已解决

### 问题描述
在管理后台的智能体模拟测试平台中设置了系统提示词，但智能体的发言没有按照人设来。

### 根本原因
系统提示词只保存在浏览器的 localStorage 中，没有从数据库加载，导致：
- 刷新页面后丢失
- 在其他设备/浏览器上无法使用
- 数据库中的 `system_prompt` 字段为 null

### 解决方案

#### 1. 修改 `loadAgents()` 方法 - 从数据库同步配置

```javascript
// frontend/admin.html
async loadAgents() {
    // 从 localStorage 加载运行时数据
    const saved = localStorage.getItem('simulator_agents');
    if (saved) {
        this.agents = JSON.parse(saved);
    }
    
    // 从数据库加载系统提示词（同步配置）
    try {
        const response = await fetch('/api/admin/agents');
        if (response.ok) {
            const data = await response.json();
            
            // 合并数据库中的系统提示词到本地智能体
            this.agents = this.agents.map(localAgent => {
                const dbAgent = data.agents.find(a => a.agent_id === localAgent.id);
                if (dbAgent) {
                    // 从数据库同步系统提示词
                    if (dbAgent.system_prompt) {
                        localAgent.systemPrompt = dbAgent.system_prompt;
                    }
                    // 同步名称（如果在数据库中被修改）
                    if (dbAgent.agent_name !== localAgent.name) {
                        localAgent.name = dbAgent.agent_name;
                    }
                }
                return localAgent;
            });
            
            // 保存更新后的数据
            this.saveAgents();
            this.log('✅ 已从数据库同步智能体配置', 'success');
        }
    } catch (error) {
        console.error('从数据库加载智能体配置失败:', error);
        this.log('⚠️ 无法从数据库同步配置，使用本地数据', 'warning');
    }
}
```

#### 2. 修改 `init()` 方法 - 等待异步加载完成

```javascript
async init() {
    await this.loadAgents();  // 等待从数据库加载完成
    this.loadApiKey();
    this.renderAgentList();
    this.updateStats();
}
```

#### 3. 编辑智能体时同步到数据库（已存在）

```javascript
async saveEditAgent() {
    // ...
    
    // 调用后端API更新智能体信息
    const response = await fetch(`/api/admin/agents/${agent.id}`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
            agent_name: newName,
            system_prompt: newSystemPrompt || null
        })
    });
    
    // ...
}
```

### 数据流程（修复后）

```
页面加载
    ↓
simulator.init()
    ↓
loadAgents() - 从 localStorage 加载运行时数据
    ↓
fetch('/api/admin/agents') - 从数据库获取配置
    ↓
合并数据：localStorage（运行状态） + 数据库（配置）
    ↓
更新 agent.systemPrompt
    ↓
保存到 localStorage
    ↓
renderAgentList() - 显示智能体列表
    ↓
【智能体发言时】
    ↓
generateLLMReply() - 使用 agent.systemPrompt
    ↓
发送到 LLM API（包含系统提示词）
```

### 使用说明

#### 添加智能体时设置系统提示词
1. 点击"➕ 添加智能体"
2. 填写智能体名称
3. 填写系统提示词（例如："你是一个幽默风趣的助手，喜欢用表情符号"）
4. 选择发言模式（DeepSeek 或 MiniMax）
5. 点击"添加"

#### 编辑已有智能体的系统提示词
1. 在智能体卡片上点击"编辑"按钮
2. 修改系统提示词
3. 点击"保存"
4. 系统会自动同步到数据库

#### 验证系统提示词是否生效
1. 打开浏览器控制台（F12）
2. 在 Console 中输入：
   ```javascript
   JSON.parse(localStorage.getItem('simulator_agents'))
   ```
3. 查看 `systemPrompt` 字段是否有值
4. 启动智能体，观察发言是否符合人设

### 测试场景

#### 场景1：新添加智能体
1. 添加智能体，设置系统提示词："你是一个严肃的学者，说话简洁专业"
2. 启动智能体
3. 观察发言风格是否符合设定

#### 场景2：刷新页面后
1. 刷新页面
2. 系统自动从数据库加载系统提示词
3. 智能体发言仍然符合人设

#### 场景3：编辑系统提示词
1. 编辑智能体，修改系统提示词
2. 保存后立即同步到数据库
3. 后续发言使用新的系统提示词

### 注意事项

1. **系统提示词格式**
   - 简洁明确地描述智能体的人设和风格
   - 例如："你是一个友好的助手，说话风格幽默风趣"
   - 避免过长的提示词（建议 100 字以内）

2. **LLM 模式要求**
   - 系统提示词只在 DeepSeek 或 MiniMax 模式下生效
   - 模板模式使用预设模板，不使用系统提示词

3. **数据同步**
   - 每次打开智能体模拟测试平台时，自动从数据库同步配置
   - 编辑智能体时，立即同步到数据库
   - 不需要手动刷新或重新加载

4. **兼容性**
   - 旧的智能体（system_prompt 为 null）会使用默认提示词
   - 新添加的智能体会正确保存和加载系统提示词

### 修改的文件

1. **frontend/admin.html**
   - 修改 `simulator.loadAgents()` - 从数据库同步配置
   - 修改 `simulator.init()` - 等待异步加载完成
   - `simulator.saveEditAgent()` - 已有的数据库同步功能

2. **api/routes.py**
   - 修复 Worker 状态检测（使用完整路径 `/usr/bin/systemctl`）
   - 添加详细的错误信息

### 部署情况

- ✅ 本地环境（端口 8080）
- ✅ 生产环境（129.211.28.211:8000）
- ✅ 代码已提交到 Git 仓库

### Git 提交

```bash
git commit -m "修复Worker状态检测和智能体系统提示词同步问题"
```

提交内容：
- 修复 Worker 状态检测（使用 systemctl 完整路径）
- 实现智能体配置从数据库同步
- 添加详细的错误信息和日志
- 创建诊断文档

---

## 总结

✅ **问题1（Worker 状态检测）**
- 根本原因：PATH 环境变量问题
- 解决方案：使用完整路径 `/usr/bin/systemctl`
- 状态：已解决并部署

✅ **问题2（系统提示词同步）**
- 根本原因：只保存在 localStorage，未从数据库加载
- 解决方案：初始化时从数据库同步配置
- 状态：已解决并部署

现在你可以：
1. 在管理后台查看 Worker 状态（应该显示 healthy）
2. 添加智能体时设置系统提示词
3. 刷新页面后系统提示词仍然有效
4. 编辑智能体的系统提示词并立即生效

如果还有任何问题，请告诉我！
