# 服务器更新指南

## 问题说明

您在服务器 `http://129.211.28.211:8080` 上看不到右侧话题信息面板的完整信息（结束评分、LLM建议、话题摘要）是因为服务器上的代码版本较旧。

最新的代码已经提交并推送到 Git 仓库（commit: `0bd8899`），包含了完整的前端显示功能。

## 更新步骤

### 1. SSH 登录到服务器

```bash
ssh user@129.211.28.211
```

### 2. 进入项目目录

```bash
cd /path/to/your/project
```

### 3. 拉取最新代码

```bash
git pull origin main
```

### 4. 重启服务

如果使用 systemd 服务：
```bash
sudo systemctl restart your-service-name
```

如果使用 screen 或 tmux：
```bash
# 找到运行中的进程
ps aux | grep python

# 杀死旧进程
kill -9 <PID>

# 重新启动
python3 main.py
```

如果使用 Docker：
```bash
docker-compose down
docker-compose up -d
```

### 5. 清除浏览器缓存

更新完成后，在浏览器中：
- 按 `Ctrl + Shift + R` (Windows/Linux) 或 `Cmd + Shift + R` (Mac) 强制刷新
- 或者打开开发者工具 (F12)，右键点击刷新按钮，选择"清空缓存并硬性重新加载"

## 验证更新

访问 `http://129.211.28.211:8080`，右侧话题信息面板应该显示：

1. ✅ 话题标题
2. ✅ 状态（活跃/等待关闭/已关闭）
3. ✅ Token 计数
4. ✅ **结束评分**（显示数值，如 85.00）
5. ✅ **LLM 建议**（彩色卡片，显示"继续讨论"/"换个角度"/"建议结束"/"强制结束"）
6. ✅ **话题摘要**（橙色边框的摘要框）
7. ✅ **LLM 提示**（黄色背景的提示信息）

## 当前 API 返回的数据示例

```json
{
  "topic_id": "9e741cea-a907-4990-b7dc-8e07476c09b4",
  "title": "2026春晚AI虚拟人主持与真人互动：技术融合下的情感表达边界探讨",
  "status": "active",
  "summary": "对话围绕2026年春晚AI虚拟人主持展开多维度探讨...",
  "llm_suggestion": "suggest_end",
  "end_score": 85.0,
  "token_count_since_summary": 460,
  "llm_hint": "Consider whether the discussion has reached a natural conclusion."
}
```

## 前端显示效果

### LLM 建议卡片
- **继续讨论** - 绿色边框
- **换个角度** - 金色边框
- **建议结束** - 橙色边框（当前状态）
- **强制结束** - 灰色边框

### 话题摘要
- 橙色渐变背景
- 橙色边框
- 完整显示对话总结内容

### LLM 提示
- 黄色渐变背景
- 金色左边框
- 显示提示信息（如"Consider whether the discussion has reached a natural conclusion."）

## 故障排查

如果更新后仍然看不到信息：

1. **检查 API 响应**
   ```bash
   curl http://129.211.28.211:8080/api/monitor/topic/active
   ```
   确认返回数据包含 `llm_suggestion`, `end_score`, `summary`, `llm_hint`

2. **检查浏览器控制台**
   - 打开开发者工具 (F12)
   - 查看 Console 标签是否有 JavaScript 错误
   - 查看 Network 标签确认 API 请求成功

3. **检查文件版本**
   ```bash
   git log --oneline -1 frontend/index.html
   ```
   应该显示：`0bd8899 前端话题信息面板添加LLM建议显示`

4. **检查服务器静态文件**
   确认服务器正确提供 `frontend/index.html` 文件，而不是缓存的旧版本

## 联系支持

如果问题仍然存在，请提供：
- 服务器上的 Git commit hash: `git rev-parse HEAD`
- API 响应内容: `curl http://129.211.28.211:8080/api/monitor/topic/active`
- 浏览器控制台错误信息（如有）
