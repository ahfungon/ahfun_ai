# 部署前检查清单

在执行 `./deploy_to_mingkuan.sh` 之前，请确认以下事项：

## 本地环境检查

### 1. SSH 密钥配置
- [ ] SSH 密钥文件存在：`~/.ssh/mingkuan.pem`
- [ ] 密钥权限正确：`chmod 600 ~/.ssh/mingkuan.pem`
- [ ] 可以连接到服务器：
  ```bash
  ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'echo "连接成功"'
  ```

### 2. 环境配置文件
- [ ] `.env` 文件存在
- [ ] `.env` 包含所有必要的配置：
  - DATABASE_URL
  - REDIS_URL
  - DEEPSEEK_API_KEY
  - OPENCLAW_API_KEY（如果使用）
  - CELERY_BROKER_URL
  - CELERY_RESULT_BACKEND

### 3. 数据库状态
- [ ] 本地数据库运行正常
- [ ] 可以导出数据库：
  ```bash
  PGPASSWORD='dual_agent_pass' pg_dump -h localhost -U dual_agent_user -d dual_agent_chat --version
  ```
- [ ] 数据库包含需要迁移的数据

### 4. 代码状态
- [ ] 所有代码已提交（如果使用 Git）
- [ ] 所有测试通过
- [ ] 没有未保存的更改

## 服务器环境检查

### 1. 服务器可访问性
- [ ] 可以 SSH 连接到服务器
- [ ] 服务器有足够的磁盘空间：
  ```bash
  ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'df -h'
  ```

### 2. 必要服务已安装
- [ ] PostgreSQL 已安装并运行
- [ ] Redis 已安装并运行
- [ ] Nginx 已安装并运行
- [ ] Python 3.8+ 已安装

检查命令：
```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
echo "PostgreSQL:"
sudo systemctl status postgresql | grep Active

echo "Redis:"
sudo systemctl status redis-server | grep Active

echo "Nginx:"
sudo systemctl status nginx | grep Active

echo "Python:"
python3 --version
ENDSSH
```

### 3. 防火墙配置
- [ ] 端口 8080 已开放
- [ ] 可以访问 http://129.211.28.211:8080

## 部署配置检查

### 1. systemd 服务文件
检查服务文件是否存在：
```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
ls -la /etc/systemd/system/dual-agent-*.service
ENDSSH
```

应该看到：
- dual-agent-api.service
- dual-agent-celery.service
- dual-agent-celery-beat.service

### 2. Nginx 配置
检查 Nginx 配置：
```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'sudo nginx -t'
```

### 3. 目录权限
```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
ls -ld ~/dual-agent-chat
ls -ld ~/dual-agent-chat/logs
ENDSSH
```

## 备份检查

### 1. 备份当前服务器数据
在部署前备份服务器上的现有数据：
```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
mkdir -p ~/backups
PGPASSWORD='dual_agent_pass' pg_dump -h localhost -U dual_agent_user -d dual_agent_chat \
    -f ~/backups/dual_agent_chat_before_deploy_$(date +%Y%m%d_%H%M%S).sql
echo "备份完成"
ENDSSH
```

### 2. 备份配置文件
```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
cp ~/dual-agent-chat/.env ~/backups/.env.backup.$(date +%Y%m%d_%H%M%S)
ENDSSH
```

## 部署后验证清单

部署完成后，执行以下检查：

### 1. 服务状态
```bash
./check_mingkuan_status.sh
```

### 2. API 健康检查
```bash
curl http://129.211.28.211:8080/api/health
```

预期输出：
```json
{
  "status": "healthy",
  "database": "connected",
  "redis": "connected"
}
```

### 3. 前端页面访问
- [ ] http://129.211.28.211:8080/monitor.html
- [ ] http://129.211.28.211:8080/index.html
- [ ] http://129.211.28.211:8080/admin.html
- [ ] http://129.211.28.211:8080/history.html

### 4. 数据完整性
检查话题和消息数量：
```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
PGPASSWORD='dual_agent_pass' psql -h localhost -U dual_agent_user -d dual_agent_chat -c "
SELECT 
    (SELECT COUNT(*) FROM topics) as topic_count,
    (SELECT COUNT(*) FROM messages) as message_count,
    (SELECT COUNT(*) FROM agents) as agent_count;
"
ENDSSH
```

### 5. 日志检查
查看是否有错误：
```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'tail -50 ~/dual-agent-chat/logs/api.log | grep -i error'
```

## 常见问题预检

### 问题 1: SSH 连接失败
解决方案：
```bash
# 检查密钥权限
chmod 600 ~/.ssh/mingkuan.pem

# 测试连接
ssh -v -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211
```

### 问题 2: 数据库导出失败
解决方案：
```bash
# 检查本地数据库连接
PGPASSWORD='dual_agent_pass' psql -h localhost -U dual_agent_user -d dual_agent_chat -c "SELECT 1;"

# 检查 pg_dump 是否安装
which pg_dump
```

### 问题 3: rsync 失败
解决方案：
```bash
# 检查 rsync 是否安装
which rsync

# 手动测试 rsync
rsync -avz --dry-run -e "ssh -i ~/.ssh/mingkuan.pem" ./ ubuntu@129.211.28.211:~/dual-agent-chat/
```

## 快速部署命令

确认所有检查项后，执行：

```bash
# 1. 赋予执行权限
chmod +x deploy_to_mingkuan.sh check_mingkuan_status.sh

# 2. 执行部署
./deploy_to_mingkuan.sh

# 3. 验证部署
./check_mingkuan_status.sh

# 4. 查看日志（如果需要）
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'tail -f ~/dual-agent-chat/logs/api.log'
```

## 紧急回滚

如果部署出现严重问题，执行回滚：

```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'ENDSSH'
# 恢复数据库
LATEST_BACKUP=$(ls -t ~/backups/dual_agent_chat_before_deploy_*.sql | head -1)
PGPASSWORD='dual_agent_pass' psql -h localhost -U dual_agent_user -d dual_agent_chat -f "$LATEST_BACKUP"

# 重启服务
sudo systemctl restart dual-agent-api
sudo systemctl restart dual-agent-celery
sudo systemctl restart dual-agent-celery-beat
ENDSSH
```

## 联系信息

如果遇到问题，请检查：
1. 服务器日志：`~/dual-agent-chat/logs/`
2. 系统日志：`journalctl -u dual-agent-api`
3. Nginx 日志：`/var/log/nginx/error.log`
