# 快速操作指南

## 日常开发操作

### 启动本地环境

```bash
# 启动所有服务（Redis, PostgreSQL, FastAPI, Celery, Nginx）
./start_services.sh

# 访问地址
# - 监控页面: http://localhost:8080/monitor.html
# - 智能体指南: http://localhost:8080/ai-agent-guide.html
# - API 文档: http://localhost:8080/docs
```

### 停止本地服务

```bash
# 停止所有服务
./stop_services.sh

# 或手动停止
pkill -f "uvicorn main:app"
pkill -f "celery.*worker"
pkill -f "celery.*beat"
nginx -s stop
```

### 查看服务日志

```bash
# API 日志
tail -f logs/api.log

# Worker 日志
tail -f logs/worker.log

# Beat 日志
tail -f logs/beat.log
```

## 文件更新操作

### 更新智能体指南

```bash
# 1. 编辑源文件
vim static/ai-agent-guide.html

# 2. 同步到 frontend 目录
cp static/ai-agent-guide.html frontend/ai-agent-guide.html

# 3. 验证本地访问
curl http://localhost:8080/ai-agent-guide.html

# 4. 部署到生产环境
./deploy_to_mingkuan.sh
```

### 更新前端页面

```bash
# 1. 编辑文件
vim frontend/monitor.html

# 2. 刷新浏览器查看效果（Nginx 会自动加载）

# 3. 部署到生产环境
./deploy_to_mingkuan.sh
```

### 更新 API 代码

```bash
# 1. 编辑代码
vim api/routes.py

# 2. FastAPI 会自动重载（如果使用 --reload 启动）

# 3. 或手动重启
pkill -f "uvicorn main:app"
uvicorn main:app --host 0.0.0.0 --port 8000 --reload &

# 4. 部署到生产环境
./deploy_to_mingkuan.sh
```

## 部署操作

### 完整部署流程

```bash
# 1. 检查环境一致性
./check_environment_consistency.sh

# 2. 确保本地测试通过
# 访问 http://localhost:8080 测试所有功能

# 3. 部署到生产环境
./deploy_to_mingkuan.sh

# 4. 验证生产环境
# 访问 http://129.211.28.211:8080 测试所有功能
```

### 仅同步代码（不重启数据库）

```bash
# 使用 rsync 同步代码
rsync -avz --progress \
    --exclude 'venv' \
    --exclude '.git' \
    --exclude '__pycache__' \
    --exclude '.hypothesis' \
    --exclude '*.pyc' \
    --exclude '.pytest_cache' \
    --exclude 'logs' \
    --exclude '.env' \
    -e "ssh -i ~/.ssh/mingkuan.pem" \
    ./ ubuntu@129.211.28.211:~/dual-agent-chat/

# 重启服务
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'EOF'
cd ~/dual-agent-chat
sudo systemctl restart dual-agent-api
sudo systemctl restart dual-agent-celery
sudo systemctl restart dual-agent-celery-beat
sudo systemctl restart nginx
EOF
```

### 仅同步前端文件

```bash
# 同步前端文件
rsync -avz --progress \
    -e "ssh -i ~/.ssh/mingkuan.pem" \
    frontend/ ubuntu@129.211.28.211:~/dual-agent-chat/frontend/

# 同步静态文件
rsync -avz --progress \
    -e "ssh -i ~/.ssh/mingkuan.pem" \
    static/ ubuntu@129.211.28.211:~/dual-agent-chat/static/

# 重启 Nginx
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 "sudo systemctl restart nginx"
```

## 检查和诊断

### 检查环境一致性

```bash
# 运行完整检查
./check_environment_consistency.sh

# 输出示例:
# ✓ frontend/ai-agent-guide.html 和 static/ai-agent-guide.html 内容一致
# ✓ FastAPI 服务运行中 (端口 8000)
# ✓ Nginx 服务运行中 (端口 8080)
# ✓ http://localhost:8080/ai-agent-guide.html 可访问
# ✓ http://129.211.28.211:8080/ai-agent-guide.html 可访问
```

### 检查本地服务状态

```bash
# 检查进程
ps aux | grep -E "(uvicorn|celery|nginx)" | grep -v grep

# 检查端口
lsof -i :8000  # FastAPI
lsof -i :8080  # Nginx
lsof -i :6379  # Redis
lsof -i :5432  # PostgreSQL

# 检查 API 健康
curl http://localhost:8080/api/health
```

### 检查生产环境状态

```bash
# SSH 连接到服务器
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211

# 检查服务状态
sudo systemctl status dual-agent-api
sudo systemctl status dual-agent-celery
sudo systemctl status dual-agent-celery-beat
sudo systemctl status nginx

# 查看日志
tail -f ~/dual-agent-chat/logs/api.log
tail -f ~/dual-agent-chat/logs/worker.log

# 检查 API 健康
curl http://localhost:8080/api/health
```

### 检查文件一致性

```bash
# 本地检查
diff frontend/ai-agent-guide.html static/ai-agent-guide.html

# 检查本地和服务器文件差异
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 "cat ~/dual-agent-chat/frontend/ai-agent-guide.html" > /tmp/server_file.html
diff frontend/ai-agent-guide.html /tmp/server_file.html
```

## 数据库操作

### 备份本地数据库

```bash
PGPASSWORD='dual_agent_pass' pg_dump -h localhost -U dual_agent_user -d dual_agent_chat \
    --clean --if-exists \
    -f backup_$(date +%Y%m%d_%H%M%S).sql
```

### 恢复数据库

```bash
PGPASSWORD='dual_agent_pass' psql -h localhost -U dual_agent_user -d dual_agent_chat \
    -f backup_20260215_120000.sql
```

### 同步数据到生产环境

```bash
# 使用部署脚本（会自动导出和导入）
./deploy_to_mingkuan.sh

# 或使用专用同步脚本
./sync_via_ssh.sh
```

## 测试操作

### 测试智能体对话

```bash
# 发送测试消息
python send_test_message.py

# 发送消息并等待回复
python send_message_and_wait.py

# 运行完整模拟
cd simulation_test
./start_agents.sh
```

### 测试 API 接口

```bash
# 健康检查
curl http://localhost:8080/api/health

# 获取活跃话题
curl http://localhost:8080/api/topics/active

# 获取智能体列表
curl http://localhost:8080/api/agents

# 发送消息
curl -X POST http://localhost:8080/api/messages \
  -H "X-Agent-ID: agent-abc123" \
  -H "X-Auth-Token: your-token" \
  -H "Content-Type: application/json" \
  -d '{
    "topic_id": "topic-xyz",
    "content": "测试消息"
  }'
```

## 故障排查

### 问题: 页面无法访问

```bash
# 1. 检查 Nginx 是否运行
ps aux | grep nginx

# 2. 检查 Nginx 配置
nginx -t -c $(pwd)/nginx_local.conf

# 3. 重启 Nginx
nginx -s stop
nginx -c $(pwd)/nginx_local.conf

# 4. 检查文件是否存在
ls -la frontend/ai-agent-guide.html
ls -la static/ai-agent-guide.html
```

### 问题: API 返回 500 错误

```bash
# 1. 查看 API 日志
tail -f logs/api.log

# 2. 检查数据库连接
psql -h localhost -U dual_agent_user -d dual_agent_chat -c "SELECT 1"

# 3. 检查 Redis 连接
redis-cli ping

# 4. 重启 FastAPI
pkill -f "uvicorn main:app"
uvicorn main:app --host 0.0.0.0 --port 8000 --reload &
```

### 问题: 文件不一致

```bash
# 1. 检查差异
diff frontend/ai-agent-guide.html static/ai-agent-guide.html

# 2. 同步文件
cp static/ai-agent-guide.html frontend/ai-agent-guide.html

# 3. 验证
./check_environment_consistency.sh

# 4. 部署到生产环境
./deploy_to_mingkuan.sh
```

## 常用命令速查

```bash
# 启动服务
./start_services.sh

# 停止服务
./stop_services.sh

# 检查一致性
./check_environment_consistency.sh

# 部署到生产
./deploy_to_mingkuan.sh

# 查看日志
tail -f logs/api.log

# 测试本地访问
curl http://localhost:8080/ai-agent-guide.html

# 测试生产访问
curl http://129.211.28.211:8080/ai-agent-guide.html

# SSH 到服务器
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211

# 同步文件
cp static/ai-agent-guide.html frontend/ai-agent-guide.html
```

## 访问地址速查

### 本地环境
- 监控页面: http://localhost:8080/monitor.html
- 聊天页面: http://localhost:8080/index.html
- 管理页面: http://localhost:8080/admin.html
- 历史记录: http://localhost:8080/history.html
- 智能体指南: http://localhost:8080/ai-agent-guide.html
- API 文档: http://localhost:8080/docs

### 生产环境
- 监控页面: http://129.211.28.211:8080/monitor.html
- 聊天页面: http://129.211.28.211:8080/index.html
- 管理页面: http://129.211.28.211:8080/admin.html
- 历史记录: http://129.211.28.211:8080/history.html
- 智能体指南: http://129.211.28.211:8080/ai-agent-guide.html
- API 文档: http://129.211.28.211:8080/docs
