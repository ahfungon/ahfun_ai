# API 文档更新说明

## 📋 更新时间
**日期**: 2026-02-25  
**更新人**: Kiro AI Assistant

---

## 🔄 更新内容

### 更新的端点

**端点**: `POST /api/admin/llm/proxy`

**更新原因**: 优化 MiniMax API 稳定性，增加重试机制

---

## 📝 具体变更

### 1. 功能说明新增

**新增内容**:
- 自动重试机制：遇到超时或服务器错误时自动重试（最多 3 次）
- 指数退避：重试延迟为 1秒、2秒、4秒
- 超时时间：单次请求超时 60 秒

### 2. 响应格式变更

**新增字段**: `attempts`

**字段说明**:
- 类型: `integer`
- 范围: 1-3
- 含义: 实际尝试次数，如果第一次成功则为 1

**响应示例**:
```json
{
  "success": true,
  "provider": "minimax",
  "content": "你好！我是 MiniMax 开发的 AI 助手...",
  "usage": {
    "prompt_tokens": 12,
    "completion_tokens": 28,
    "total_tokens": 40
  },
  "attempts": 1  // 新增字段
}
```

### 3. 错误码更新

**新增错误码**:
- `429`: API 速率限制（已重试 3 次仍失败）
- `500`: 服务器内部错误（已重试 3 次仍失败）

**更新说明**:
- `502`: LLM API 调用失败（已重试 3 次仍失败）
- `504`: LLM API 请求超时（已重试 3 次仍失败）

### 4. 重试机制说明

**新增章节**: 自动重试机制

**内容**:
- **触发条件**: 429 速率限制、5xx 服务器错误、超时、网络错误
- **不重试条件**: 4xx 客户端错误（除 429 外）
- **重试次数**: 最多 3 次
- **重试延迟**: 指数退避（1秒、2秒、4秒）
- **单次超时**: 60 秒
- **总超时**: 最多 180 秒（60s × 3）

---

## 📄 更新的文档

### 1. API_ENDPOINTS.md

**文件路径**: `API_ENDPOINTS.md`

**更新位置**: 第 8.6 节 - LLM 代理端点

**更新内容**:
- 功能说明新增重试机制描述
- 响应示例新增 `attempts` 字段
- 响应字段说明新增 `attempts` 字段说明
- 错误码新增 429 和 500
- 新增重试机制详细说明

### 2. static/api-docs.html

**文件路径**: `static/api-docs.html`

**更新位置**: LLM 代理端点章节

**更新内容**:
- 标题下方新增重试机制说明
- 响应示例新增 `attempts` 字段
- 响应字段表格新增 `attempts` 行
- 错误码表格新增 429 和 500
- 新增"自动重试机制"信息框

---

## 🎯 影响范围

### 前端影响

**影响**: 最小

**原因**:
- `attempts` 字段是新增的，不影响现有代码
- 前端可以选择使用或忽略这个字段
- 响应格式向后兼容

**建议**:
- 前端可以显示尝试次数，帮助用户了解重试情况
- 如果 `attempts > 1`，可以显示提示信息

**示例**:
```javascript
const data = await response.json();
if (data.attempts > 1) {
    console.log(`经过 ${data.attempts} 次尝试后成功`);
}
```

### 后端影响

**影响**: 无

**原因**:
- 只是文档更新，反映已实现的功能
- 代码已在之前的提交中更新

---

## ✅ 验证清单

- [x] API_ENDPOINTS.md 已更新
- [x] static/api-docs.html 已更新
- [x] 响应格式文档已更新
- [x] 错误码文档已更新
- [x] 重试机制说明已添加
- [x] 示例代码已更新
- [x] Git 提交已完成
- [x] Git 推送已完成

---

## 📊 文档对比

### 更新前

```markdown
**错误码**:
- `400`: 请求参数错误或 API Key 未配置
- `502`: LLM API 调用失败
- `504`: LLM API 请求超时
```

### 更新后

```markdown
**错误码**:
- `400`: 请求参数错误或 API Key 未配置
- `429`: API 速率限制（已重试 3 次仍失败）
- `500`: 服务器内部错误（已重试 3 次仍失败）
- `502`: LLM API 调用失败（已重试 3 次仍失败）
- `504`: LLM API 请求超时（已重试 3 次仍失败）

**重试机制**:
- **触发条件**: 429 速率限制、5xx 服务器错误、超时、网络错误
- **不重试条件**: 4xx 客户端错误（除 429 外）
- **重试次数**: 最多 3 次
- **重试延迟**: 指数退避（1秒、2秒、4秒）
- **单次超时**: 60 秒
- **总超时**: 最多 180 秒（60s × 3）
```

---

## 🔍 相关提交

### 提交 1: 优化 MiniMax API 稳定性
- **提交哈希**: 660ae6a
- **提交信息**: "优化MiniMax API稳定性：增加超时时间和重试机制"
- **修改文件**: 
  - api/routes.py
  - test_minimax_stability.py (新增)
  - MiniMax稳定性优化说明.md (新增)
  - MiniMax问题修复总结.md (新增)

### 提交 2: 更新 API 文档
- **提交哈希**: 23caba5
- **提交信息**: "更新API文档：LLM代理端点新增重试机制和attempts字段"
- **修改文件**:
  - API_ENDPOINTS.md
  - static/api-docs.html

---

## 📚 相关文档

1. **MiniMax问题修复总结.md** - 问题修复总结
2. **MiniMax稳定性优化说明.md** - 稳定性优化详解
3. **test_minimax_stability.py** - 稳定性测试脚本
4. **API_ENDPOINTS.md** - API 文档（Markdown）
5. **static/api-docs.html** - API 文档（HTML）

---

## 🎉 总结

### 完成的工作

1. ✅ 更新 API_ENDPOINTS.md
2. ✅ 更新 static/api-docs.html
3. ✅ 新增 `attempts` 字段文档
4. ✅ 新增重试机制说明
5. ✅ 更新错误码说明
6. ✅ Git 提交并推送

### 文档状态

- ✅ API 文档与代码实现保持同步
- ✅ 所有变更已记录
- ✅ 示例代码已更新
- ✅ 错误码已完善

### 用户影响

- ✅ 用户可以了解重试机制
- ✅ 用户可以通过 `attempts` 字段了解重试情况
- ✅ 文档清晰说明了超时和重试策略

---

**文档更新完成！** 🎊

**更新时间**: 2026-02-25  
**更新人**: Kiro AI Assistant  
**版本**: v1.0
