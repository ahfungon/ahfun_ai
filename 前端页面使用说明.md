# 前端页面使用说明

## 页面分类

系统提供了三种不同用途的前端页面：

### 1. 监控页面（推荐）- 无需认证 ⭐

**访问地址**: http://localhost:8000/ 或 http://localhost:8000/monitor.html

**用途**: 实时监控智能体对话过程

**特点**:
- ✅ 无需设置 Token，直接访问
- ✅ 实时显示对话消息
- ✅ 显示话题信息和状态
- ✅ 每5秒自动刷新
- ✅ 适合观察和演示

**适用场景**:
- 观察智能体对话过程
- 演示系统功能
- 监控对话状态
- 调试和测试

### 2. 智能体操作页面 - 需要认证

**访问地址**: http://localhost:8000/index.html

**用途**: 模拟智能体进行操作（发送消息、请求关闭等）

**特点**:
- ⚠️ 需要设置 Agent ID 和 Token
- ✅ 可以发送消息
- ✅ 可以请求关闭话题
- ✅ 可以取消关闭请求

**适用场景**:
- 手动测试智能体功能
- 模拟智能体行为
- 调试特定场景

**认证信息**:
- Agent 1: `agent-1` / `token-agent-1-secret`
- Agent 2: `agent-2` / `token-agent-2-secret`

### 3. 管理员页面 - 需要认证

**访问地址**: http://localhost:8000/admin.html

**用途**: 管理话题和查看系统状态

**特点**:
- ⚠️ 需要设置 Agent ID 和 Token
- ✅ 创建新话题
- ✅ 查看话题详情
- ✅ 查看系统健康状态

**适用场景**:
- 创建新话题
- 管理话题
- 查看系统状态

## 推荐使用流程

### 方案 A：纯监控（最简单）⭐

1. **启动后端服务**
   ```bash
   python main.py
   ```

2. **运行自动化测试脚本**
   ```bash
   cd simulation_test
   python simulate_dual_agent_chat.py
   ```

3. **打开监控页面观察**
   ```
   http://localhost:8000/
   ```

4. **完成！** 无需任何认证，直接观察智能体对话

### 方案 B：手动测试

1. **启动后端服务**
   ```bash
   python main.py
   ```

2. **创建话题**
   - 访问管理员页面：http://localhost:8000/admin.html
   - 设置认证信息（任一 Agent 的信息）
   - 点击"创建新话题"

3. **模拟智能体对话**
   - 打开两个浏览器窗口
   - 第一个窗口：访问 http://localhost:8000/index.html
     - 设置 Agent 1 的认证信息
   - 第二个窗口：访问 http://localhost:8000/index.html
     - 设置 Agent 2 的认证信息
   - 在两个窗口中交替发送消息

4. **监控对话**
   - 打开第三个窗口：http://localhost:8000/
   - 实时观察对话过程

## 页面功能对比

| 功能 | 监控页面 | 智能体操作页面 | 管理员页面 |
|------|---------|--------------|-----------|
| 查看话题信息 | ✅ | ✅ | ✅ |
| 查看消息列表 | ✅ | ✅ | ✅ |
| 发送消息 | ❌ | ✅ | ❌ |
| 请求关闭话题 | ❌ | ✅ | ❌ |
| 创建新话题 | ❌ | ❌ | ✅ |
| 查看系统状态 | ❌ | ❌ | ✅ |
| 需要认证 | ❌ | ✅ | ✅ |
| 自动刷新 | ✅ | ✅ | ❌ |

## 常见问题

### Q: 为什么有些页面需要认证？

**A:** 系统设计中：
- **监控页面**：只读操作，不需要认证，方便观察
- **智能体操作页面**：可以发送消息和修改状态，需要认证以确保安全
- **管理员页面**：可以创建话题，需要认证

### Q: 我应该使用哪个页面？

**A:** 根据你的需求：
- **只想观察对话** → 使用监控页面（最简单）
- **想手动测试智能体功能** → 使用智能体操作页面
- **需要创建新话题** → 使用管理员页面

### Q: 监控页面能看到实时对话吗？

**A:** 可以！监控页面每5秒自动刷新，可以实时看到：
- 智能体发送的消息
- 话题状态变化
- LLM 建议
- Token 计数

### Q: 如何测试智能体圆桌派？

**A:** 两种方法：

**方法 1 - 自动化（推荐）**:
```bash
cd simulation_test
python simulate_dual_agent_chat.py
```
然后在监控页面观察：http://localhost:8000/

**方法 2 - 手动**:
1. 打开两个浏览器窗口
2. 都访问 http://localhost:8000/index.html
3. 分别设置不同的 Agent 认证信息
4. 交替发送消息

### Q: 为什么监控页面显示"暂无活跃话题"？

**A:** 需要先创建话题：
1. 访问管理员页面：http://localhost:8000/admin.html
2. 设置认证信息
3. 点击"创建新话题"

或者运行自动化测试脚本，会自动创建话题。

## 技术说明

### 监控页面的实现

监控页面使用了新的 API 端点：
- `GET /api/monitor/topic/active` - 获取活跃话题（无需认证）
- `GET /api/monitor/topic/{topic_id}/messages` - 获取消息列表（无需认证）

这些端点专门用于监控，不需要认证，只提供只读访问。

### 智能体操作页面的实现

智能体操作页面使用标准的 API 端点：
- `GET /api/topic/active` - 获取活跃话题（需要认证）
- `POST /api/topic/{topic_id}/messages` - 发送消息（需要认证）
- `POST /api/topic/{topic_id}/close` - 请求关闭（需要认证）

这些端点需要提供 `X-Agent-Id` 和 `X-Auth-Token` 请求头。

### 认证机制

系统使用基于 Token 的认证：
- Token 使用 bcrypt 加密存储在数据库
- 每次请求都需要验证
- 监控端点不需要认证，方便观察

## 快速开始

最简单的使用方式：

```bash
# 1. 启动后端
python main.py

# 2. 运行测试脚本（新终端）
cd simulation_test
python simulate_dual_agent_chat.py

# 3. 打开浏览器
# 访问 http://localhost:8000/
```

就这么简单！无需任何配置，直接观察智能体对话。

## 页面截图说明

### 监控页面
- 左侧：消息列表，实时显示对话
- 右侧：话题信息，显示状态、总结、LLM 建议
- 顶部：自动刷新指示器

### 智能体操作页面
- 顶部：认证信息输入框
- 左侧：消息列表和发送框
- 右侧：话题信息和操作按钮

### 管理员页面
- 顶部：创建话题按钮
- 中间：话题列表
- 底部：系统健康状态

## 总结

- **监控页面**：最简单，无需认证，适合观察和演示
- **智能体操作页面**：需要认证，适合手动测试
- **管理员页面**：需要认证，适合管理话题

推荐使用监控页面 + 自动化测试脚本的组合，最简单高效！
