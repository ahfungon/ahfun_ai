# 系统健康检查增强完成报告

## 任务概述

增强管理后台的系统健康检查功能，添加以下监控项：
1. Celery Worker 进程状态检查
2. LLM 配置状态检查（DeepSeek 和 MiniMax API Key）
3. 系统资源监控（CPU、内存、磁盘使用率）

## 完成内容

### 1. 后端增强 (`api/routes.py`)

#### 添加的导入
```python
import subprocess  # 用于执行系统命令检查 Worker 状态
```

#### Worker 状态检查（三种检测方法）

**方法1: systemctl（生产环境）**
```python
result = subprocess.run(
    ['systemctl', 'is-active', 'dual-agent-celery'],
    capture_output=True,
    text=True,
    timeout=2
)
if result.returncode == 0 and result.stdout.strip() == 'active':
    worker_running = True
    detection_method = "systemd"
```

**方法2: pgrep（备用方法）**
```python
pgrep_path = shutil.which('pgrep')
if pgrep_path:
    result = subprocess.run(
        [pgrep_path, '-f', 'celery.*worker'],
        capture_output=True,
        timeout=2
    )
    if result.returncode == 0 and result.stdout.strip():
        worker_running = True
        detection_method = "pgrep"
```

**方法3: Celery inspect（最可靠但较慢）**
```python
from celery_app import celery_app
inspect = celery_app.control.inspect(timeout=2.0)
active_workers = inspect.active()
if active_workers:
    worker_running = True
    detection_method = "celery_inspect"
```

#### LLM 配置检查
```python
config_service = SystemConfigService(db)
deepseek_key = config_service.get_config_value('deepseek_api_key', '')
minimax_key = config_service.get_config_value('minimax_api_key', '')

llm_status = []
if deepseek_key:
    llm_status.append("DeepSeek")
if minimax_key:
    llm_status.append("MiniMax")

if llm_status:
    health_status["services"]["llm_config"] = {
        "status": "healthy",
        "message": f"Configured: {', '.join(llm_status)}"
    }
```

#### 系统资源监控
```python
import psutil

cpu_percent = psutil.cpu_percent(interval=0.1)
memory = psutil.virtual_memory()
disk = psutil.disk_usage('/')

health_status["system"]["cpu_usage"] = f"{cpu_percent}%"
health_status["system"]["memory_usage"] = f"{memory.percent}%"
health_status["system"]["disk_usage"] = f"{disk.percent}%"

# 资源使用率超过 90% 时标记为 degraded
if cpu_percent > 90 or memory.percent > 90 or disk.percent > 90:
    health_status["status"] = "degraded"
```

### 2. 前端增强 (`frontend/admin.html`)

#### 更新 `loadHealth()` 函数

**支持三种状态徽章**
- `healthy` → 绿色
- `warning` → 黄色
- `unhealthy` → 红色

**添加系统资源显示**
```javascript
// 根据使用率显示不同颜色
const getResourceBadge = (usage) => {
    const percent = parseFloat(usage);
    if (percent > 90) return 'badge-danger';    // 红色
    if (percent > 75) return 'badge-warning';   // 黄色
    return 'badge-success';                     // 绿色
};

// 显示 CPU、内存、磁盘使用率
systemResourcesHtml = `
    <div style="margin-top: 30px;">
        <h3>📊 系统资源</h3>
        <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 15px;">
            <div>CPU 使用率: ${data.system.cpu_usage}</div>
            <div>内存使用率: ${data.system.memory_usage}</div>
            <div>磁盘使用率: ${data.system.disk_usage}</div>
        </div>
    </div>
`;
```

### 3. 依赖更新 (`requirements.txt`)

添加 psutil 库用于系统资源监控：
```
psutil==5.9.8
```

## API 响应示例

### 健康状态（所有服务正常）
```json
{
    "status": "ok",
    "timestamp": "2026-02-25T05:42:27.542505",
    "services": {
        "database": {
            "status": "healthy",
            "message": "Database connection successful"
        },
        "redis": {
            "status": "healthy",
            "message": "Redis connection successful"
        },
        "celery_worker": {
            "status": "healthy",
            "message": "Celery worker is running (detected by pgrep)"
        },
        "llm_config": {
            "status": "healthy",
            "message": "Configured: DeepSeek, MiniMax"
        }
    },
    "system": {
        "cpu_usage": "9.1%",
        "memory_usage": "54.5%",
        "disk_usage": "1.1%"
    }
}
```

### 降级状态（Worker 未运行）
```json
{
    "status": "degraded",
    "timestamp": "2026-02-25T05:47:52.844576",
    "services": {
        "database": {
            "status": "healthy",
            "message": "Database connection successful"
        },
        "redis": {
            "status": "healthy",
            "message": "Redis connection successful"
        },
        "celery_worker": {
            "status": "unhealthy",
            "message": "Celery worker is not running"
        },
        "llm_config": {
            "status": "healthy",
            "message": "Configured: DeepSeek, MiniMax"
        }
    },
    "system": {
        "cpu_usage": "0.0%",
        "memory_usage": "49.2%",
        "disk_usage": "35.5%"
    }
}
```

## 部署情况

### 本地环境 ✅
- 端口：8080
- 状态：已部署并测试通过
- Worker 检测：正常（使用 pgrep 方法）
- 系统资源：正常显示

### 生产环境 ✅
- 服务器：129.211.28.211
- 端口：8000
- 状态：已部署
- 文件已更新：
  - `api/routes.py`
  - `frontend/admin.html`
  - `requirements.txt`
- psutil 已安装
- API 服务已重启

## 测试验证

### 本地测试
```bash
curl -s http://localhost:8080/api/health | python -m json.tool
```

### 生产测试
```bash
curl -s http://129.211.28.211:8000/api/health | python -m json.tool
```

### Worker 检测测试
在服务器上验证三种检测方法：
```bash
# systemctl 方法
systemctl is-active dual-agent-celery
# 输出: active ✅

# pgrep 方法
pgrep -f 'celery.*worker'
# 输出: 进程ID列表 ✅

# Celery inspect 方法
python3 -c "from celery_app import celery_app; print(celery_app.control.inspect().active())"
```

## 功能特点

### 1. 多层次检测
- 优先使用 systemctl（生产环境标准）
- 备用 pgrep（跨平台兼容）
- 最后使用 Celery inspect（最准确但较慢）

### 2. 容错设计
- 每种检测方法都有超时保护（2秒）
- 检测失败不会导致整个健康检查失败
- 提供详细的错误信息

### 3. 可视化展示
- 服务状态用颜色徽章区分
- 系统资源用进度条和颜色标识
- 自动刷新功能

### 4. 性能优化
- 使用 `psutil.cpu_percent(interval=0.1)` 快速采样
- 所有检测都有超时限制
- 异步执行不阻塞其他请求

## 使用说明

### 访问健康检查页面
1. 打开管理后台：`http://your-domain/admin.html`
2. 点击左侧菜单"🏥 系统健康"
3. 点击"刷新"按钮查看最新状态

### 监控指标说明

**服务状态**
- `database`: 数据库连接状态
- `redis`: Redis 连接状态
- `celery_worker`: Worker 进程运行状态
- `llm_config`: LLM API Key 配置状态

**系统资源**
- `cpu_usage`: CPU 使用率（>90% 警告）
- `memory_usage`: 内存使用率（>90% 警告）
- `disk_usage`: 磁盘使用率（>90% 警告）

**整体状态**
- `ok`: 所有服务正常
- `degraded`: 部分服务异常或资源使用率过高
- `unhealthy`: 关键服务不可用

## SSH 连接问题解决

### 问题
之前使用 `mingkuan.pem` 可以连接，突然无法连接。

### 原因
scp 命令没有指定密钥文件路径。

### 解决方案
使用 `-i` 参数指定密钥文件：
```bash
scp -i ~/.ssh/mingkuan.pem file.txt ubuntu@129.211.28.211:/path/
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 "command"
```

### 密钥文件位置
```
~/.ssh/mingkuan.pem
权限: 400 (r--------)
```

## 后续优化建议

1. **添加历史趋势**
   - 记录系统资源使用率历史
   - 绘制趋势图表

2. **告警功能**
   - Worker 停止时发送通知
   - 资源使用率超过阈值时告警

3. **更多监控项**
   - 数据库连接池状态
   - Redis 内存使用情况
   - API 响应时间统计

4. **自动恢复**
   - Worker 停止时自动重启
   - 服务异常时自动修复

## 总结

✅ 成功增强了系统健康检查功能
✅ 添加了 Worker 状态、LLM 配置、系统资源监控
✅ 本地和生产环境都已部署
✅ 前端界面美观易用
✅ 解决了 SSH 连接问题

系统健康检查现在可以全面监控平台运行状态，帮助及时发现和解决问题。
