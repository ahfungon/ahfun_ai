# 配置修改快速参考

## 🚀 快速判断

```
修改 Prompt？        → ❌ 不需要重启
修改 Token 阈值？    → ❌ 不需要重启
切换 LLM 提供商？    → ⚠️ 需要重启 Worker
修改 API Key？       → ⚠️ 需要重启 Worker
```

## 📋 详细对照表

| 配置项 | 是否需要重启 Worker | 生效时机 |
|--------|-------------------|----------|
| 消息评分 Prompt | ❌ 不需要 | 立即生效 |
| 对话总结 Prompt | ❌ 不需要 | 立即生效 |
| Token 阈值 | ❌ 不需要 | 立即生效 |
| LLM 提供商（评分） | ⚠️ 需要 | Worker 重启后生效 |
| LLM 提供商（总结） | ⚠️ 需要 | Worker 重启后生效 |
| DeepSeek API Key | ⚠️ 需要 | Worker 重启后生效 |
| MiniMax API Key | ⚠️ 需要 | Worker 重启后生效 |
| API URL | ⚠️ 需要 | Worker 重启后生效 |
| 模型名称 | ⚠️ 需要 | Worker 重启后生效 |

## 🔄 重启 Worker

### 方法 1：使用脚本（推荐）

```bash
./restart_worker.sh
```

### 方法 2：手动重启

```bash
# 停止
pkill -f "celery -A workers.celery_app worker"

# 启动
celery -A workers.celery_app worker --loglevel=info --logfile=logs/worker.log &
```

## ✅ 验证配置生效

### 验证 Prompt 修改

```bash
# 1. 在管理后台修改 Prompt
# 2. 发送一条新消息
# 3. 查看评分或总结结果
```

### 验证 LLM 切换

```bash
# 1. 在管理后台切换 LLM 提供商
# 2. 重启 Worker
./restart_worker.sh

# 3. 查看日志确认
tail -f logs/worker.log | grep -i "minimax\|deepseek"
```

## 💡 提示

- API 服务器不需要重启（每次请求都创建新实例）
- 只有 Celery Worker 需要重启（服务实例复用）
- 修改 Prompt 和阈值立即生效，无需任何操作

## 📚 详细文档

查看 `系统配置生效时机说明.md` 了解更多细节。
