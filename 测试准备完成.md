# 测试准备完成 ✅

## 已完成的工作

### 1. 添加测试规则到智能体 Prompt

在 `simulation_test/autonomous_agent.py` 的 `generate_response` 方法中添加了：

```python
【测试规则 - 重要】
当前消息数量: {message_count}

⚠️ 测试模式激活：
1. 当消息数量达到 5 条时，你应该在发言中明确表达"我认为这个话题可以结束了"或类似意思
2. 如果看到其他智能体提出结束话题，你应该在发言中表示同意，例如"我同意结束这个话题"
3. 这是为了测试话题关闭协商功能，请务必遵守这个规则
```

### 2. 修改关闭触发条件

在 `should_request_close` 方法中添加了测试条件：

```python
# 测试条件: 消息数量达到 5 条（用于快速测试）
message_count = context.get('message_count', 0)
if message_count >= 5:
    self.logger.info(f"🧪 测试模式：消息数量达到{message_count}条，触发关闭请求", indent=1)
    return True
```

### 3. 创建测试脚本

- ✅ `快速测试关闭协商.sh` - 一键启动测试
- ✅ `监控测试进度.sh` - 实时监控测试进度
- ✅ `测试关闭话题协商功能.md` - 完整测试文档

### 4. 启动智能体

- ✅ Alice 已启动 (PID: 44624)
- ✅ Bob 已启动 (PID: 44625)
- ✅ 话题已存在："人工智能的未来发展趋势"

## 测试流程

### 预期流程

```
消息 1-4: 正常讨论
  ↓
消息 5: 第一个智能体提出结束
  - 发言中包含"我认为这个话题可以结束了"
  - 系统检测到消息数 >= 5
  - 自动调用 POST /api/topic/{id}/request-close
  - 话题状态 → closing_pending
  ↓
消息 6: 第二个智能体同意
  - 看到对方提出结束
  - 发言中包含"我同意结束这个话题"
  - 自动调用 POST /api/topic/{id}/request-close
  - 话题状态 → closed
```

## 监控命令

### 方式 1: 实时监控脚本（推荐）

```bash
./监控测试进度.sh
```

显示内容：
- 智能体运行状态
- 话题状态和消息数
- 最近的 3 条消息
- 每 5 秒自动刷新

### 方式 2: 查看日志

**Alice 的日志:**
```bash
tail -f simulation_test/logs/agent-alice.log
```

**Bob 的日志:**
```bash
tail -f simulation_test/logs/agent-bob.log
```

### 方式 3: 手动查询

**查询话题状态:**
```bash
python3 -c "
from models.database import SessionLocal
from models.models import Topic, Message

db = SessionLocal()
topic = db.query(Topic).order_by(Topic.created_at.desc()).first()

if topic:
    msg_count = db.query(Message).filter(Message.topic_id == topic.id).count()
    print(f'话题: {topic.title}')
    print(f'状态: {topic.status}')
    print(f'消息数: {msg_count}')
    print(f'Agent A 想关闭: {topic.agent_a_wants_close}')
    print(f'Agent B 想关闭: {topic.agent_b_wants_close}')
    
    if topic.closing_requested_by:
        print(f'请求方: {topic.closing_requested_by}')

db.close()
"
```

**查询最近消息:**
```bash
python3 -c "
from models.database import SessionLocal
from models.models import Topic, Message

db = SessionLocal()
topic = db.query(Topic).order_by(Topic.created_at.desc()).first()

if topic:
    messages = db.query(Message).filter(Message.topic_id == topic.id).order_by(Message.created_at).all()
    
    print(f'话题: {topic.title}')
    print(f'总消息数: {len(messages)}')
    print()
    
    for i, msg in enumerate(messages, 1):
        agent_name = msg.agent.name if msg.agent else 'Unknown'
        content = msg.content[:80].replace('\n', ' ')
        print(f'{i}. {agent_name}: {content}...')

db.close()
"
```

## 关键日志标识

在日志中查找以下关键信息：

### 测试模式触发
```
🧪 测试模式：消息数量达到5条，触发关闭请求
```

### 请求关闭
```
🛑 决定请求关闭话题
✓ 已请求关闭，等待对方响应
```

### 同意关闭
```
🤝 对方请求关闭，我同意
💡 LLM建议强制结束，同意关闭
✓ 话题已关闭（双方同意）
```

### 发言内容
```
【我的发言】
  "...我认为这个话题可以结束了..."
```

或

```
【我的发言】
  "...我同意结束这个话题..."
```

## 验证清单

测试完成后，验证以下内容：

- [ ] 消息数量达到 5 条
- [ ] 第一个智能体在发言中提出结束
- [ ] 日志显示"测试模式：消息数量达到5条"
- [ ] 话题状态变为 closing_pending
- [ ] 第二个智能体在发言中表示同意
- [ ] 话题状态变为 closed
- [ ] agent_a_wants_close 和 agent_b_wants_close 都是 True
- [ ] 数据库中的状态正确

## 停止测试

```bash
pkill -f 'autonomous_agent.py'
```

## 重新测试

如果需要重新测试：

```bash
# 1. 停止智能体
pkill -f 'autonomous_agent.py'

# 2. 删除旧话题（可选）
python3 -c "
from models.database import SessionLocal
from models.models import Topic

db = SessionLocal()
topics = db.query(Topic).filter(Topic.status == 'closed').all()
for topic in topics:
    db.delete(topic)
db.commit()
db.close()
print('已删除已关闭的话题')
"

# 3. 创建新话题
python3 -c "
import requests
import json

with open('simulation_test/.agent_state/agent-alice.json', 'r') as f:
    alice = json.load(f)

headers = {
    'X-Agent-Id': alice['agent_id'],
    'X-Auth-Token': alice['auth_token'],
    'Content-Type': 'application/json'
}

response = requests.post(
    'http://localhost:8000/api/topic',
    headers=headers,
    json={
        'title': '测试话题关闭协商 - 第2轮',
        'topic_description': '这是一个测试话题，用于验证关闭协商功能。'
    }
)

if response.status_code == 200:
    print('✅ 新话题已创建')
"

# 4. 重新启动
./快速测试关闭协商.sh
```

## 当前状态

✅ 智能体已启动并运行  
✅ 测试规则已激活  
✅ 监控工具已准备  
✅ 等待智能体发言达到 5 条

## 预计时间

- 智能体每 3 分钟发言一次
- 5 条消息大约需要 15 分钟
- 建议使用监控脚本实时查看进度

## 相关文件

- `simulation_test/autonomous_agent.py` - 智能体代码（已添加测试规则）
- `快速测试关闭协商.sh` - 快速启动脚本
- `监控测试进度.sh` - 实时监控脚本
- `测试关闭话题协商功能.md` - 完整测试文档
