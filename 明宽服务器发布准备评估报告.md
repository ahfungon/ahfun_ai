# 明宽服务器发布准备评估报告

**评估时间**: 2026-02-25  
**当前版本**: 76fc222 (main)  
**评估人**: Kiro AI Assistant

---

## 📋 执行摘要

### 结论
✅ **可以发布，但需要执行数据库迁移**

### 关键发现
1. ✅ 代码已同步到 Git，工作区干净
2. ⚠️ **需要执行 2 个数据库迁移**
3. ✅ Python 依赖无变更
4. ⚠️ 需要重启 Worker 和 API 服务器
5. ⚠️ 需要在系统配置中设置 LLM API Key

---

## 🗄️ 数据库变更评估

### ⚠️ 必须执行的迁移

服务器数据库需要升级，包含以下变更：

#### 迁移 1: 消息评分功能 ✅

**文件**: `migrations/add_message_relevance_scoring.sql`

**变更内容**:
1. 在 `topics` 表添加 `topic_description` 字段
2. 创建 `message_relevance_scores` 表
3. 创建 3 个索引

**SQL 预览**:
```sql
-- 1. 添加字段
ALTER TABLE topics ADD COLUMN IF NOT EXISTS topic_description TEXT;

-- 2. 创建评分表
CREATE TABLE IF NOT EXISTS message_relevance_scores (
    id VARCHAR(36) PRIMARY KEY,
    message_id VARCHAR(36) NOT NULL UNIQUE,
    topic_id VARCHAR(36) NOT NULL,
    agent_id VARCHAR(36) NOT NULL,
    relevance_score FLOAT NOT NULL CHECK (relevance_score >= 0 AND relevance_score <= 100),
    evaluation_comment TEXT,
    evaluated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (message_id) REFERENCES messages(id) ON DELETE CASCADE,
    FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE
);

-- 3. 创建索引
CREATE INDEX IF NOT EXISTS idx_message_scores_message ON message_relevance_scores(message_id);
CREATE INDEX IF NOT EXISTS idx_message_scores_topic ON message_relevance_scores(topic_id);
CREATE INDEX IF NOT EXISTS idx_message_scores_agent ON message_relevance_scores(agent_id, evaluated_at DESC);
```

**影响**:
- ✅ 使用 `IF NOT EXISTS`，安全可重复执行
- ✅ 不影响现有数据
- ✅ 不会锁表（PostgreSQL）

#### 迁移 2: 系统配置功能 ✅

**文件**: `migrations/create_system_config_table.sql`

**变更内容**:
1. 创建 `system_configs` 表
2. 创建 2 个索引

**SQL 预览**:
```sql
CREATE TABLE IF NOT EXISTS system_configs (
    key VARCHAR(100) PRIMARY KEY,
    value TEXT NOT NULL,
    config_type VARCHAR(20) NOT NULL DEFAULT 'text',
    category VARCHAR(50) NOT NULL DEFAULT 'general',
    display_name VARCHAR(200) NOT NULL,
    description TEXT,
    default_value TEXT,
    validation TEXT,
    options TEXT,
    display_order INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_system_configs_category ON system_configs(category);
CREATE INDEX IF NOT EXISTS idx_system_configs_display_order ON system_configs(display_order);
```

**影响**:
- ✅ 使用 `IF NOT EXISTS`，安全可重复执行
- ✅ 独立表，不影响现有功能
- ✅ 不会锁表

### 迁移执行方式

**方式 1: 使用迁移脚本（推荐）**
```bash
# 在服务器上执行
cd /path/to/project

# 执行消息评分迁移
python3 run_migration.py

# 初始化系统配置表和默认值
python3 init_system_config.py
```

**方式 2: 手动执行 SQL**
```bash
# 连接数据库
psql -U username -d dual_agent_chat

# 执行迁移 1
\i migrations/add_message_relevance_scoring.sql

# 执行迁移 2
\i migrations/create_system_config_table.sql

# 初始化系统配置
# 需要运行 Python 脚本
python3 init_system_config.py
```

---

## 📦 代码同步评估

### Git 状态

**本地状态**:
```
On branch main
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean
```

**最新提交**:
```
76fc222 修复总结触发端点：添加缺失的 desc 导入
```

**提交历史** (最近 20 个):
- 76fc222 - 修复总结触发端点
- a27fe08 - 添加明宽服务器发布评估报告
- 0dd1857 - 优化总结Prompt
- 0b2f958 - 添加总结监控功能最终完成报告
- b6dc514 - 添加API服务器重启脚本
- d220c6e - 完成总结监控功能
- ... (更多提交)

### ✅ 代码同步方案

**在服务器上执行**:
```bash
# 1. 备份当前代码
cd /path/to/project
tar -czf backup_$(date +%Y%m%d_%H%M%S).tar.gz .

# 2. 拉取最新代码
git fetch origin
git status
git pull origin main

# 3. 验证代码版本
git log --oneline -1
# 应显示：76fc222 修复总结触发端点：添加缺失的 desc 导入
```

---

## 🐍 Python 依赖评估

### 依赖状态

**当前依赖** (requirements.txt):
```
fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
celery==5.3.6
redis==5.0.1
httpx==0.26.0
openai==1.10.0
requests==2.31.0
python-dotenv==1.0.0
... (其他依赖)
```

### ✅ 依赖评估结果

**结论**: 无需更新依赖

**原因**:
- 所有依赖版本未变更
- 服务器应该已经安装了这些依赖
- 如果不确定，可以重新安装确保一致性

**可选操作**:
```bash
# 在服务器上重新安装依赖（可选）
pip3 install -r requirements.txt --upgrade
```

---

## ⚙️ 配置变更评估

### 环境变量 (.env)

**重要变更**: LLM 配置已迁移到系统配置表

#### 必需配置（基础设施）
```bash
# 数据库
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Redis
REDIS_URL=redis://host:6379/0

# Celery
CELERY_BROKER_URL=redis://host:6379/0
CELERY_RESULT_BACKEND=redis://host:6379/0

# API 服务器
API_HOST=0.0.0.0
API_PORT=8080
```

#### 可选配置（作为默认值）
```bash
# LLM 配置（优先从系统配置读取）
DEEPSEEK_API_KEY=
DEEPSEEK_API_URL=https://api.deepseek.com/v1
SUMMARY_THRESHOLD=8000
```

### ⚠️ 配置优先级
```
系统配置表 > .env 文件 > 代码默认值
```

**建议**: 在管理后台配置 LLM，.env 文件中可以留空

---

## 🔄 服务重启评估

### 需要重启的服务

#### 1. Celery Worker ⚠️ 必须重启

**原因**:
- 新增了 `scoring_queue` 队列
- 代码逻辑有更新
- 需要加载新的系统配置

**重启命令**:
```bash
# 查找并停止旧的 Worker
ps aux | grep celery
kill -9 <PID>

# 启动新的 Worker（包含新队列）
celery -A workers.celery_app worker \
  --loglevel=info \
  --concurrency=5 \
  --queues=summary_queue,scoring_queue \
  --max-tasks-per-child=100 \
  --logfile=logs/worker.log &
```

**或使用提供的脚本**:
```bash
chmod +x restart_worker.sh
./restart_worker.sh
```

#### 2. API 服务器 ⚠️ 必须重启

**原因**:
- 新增了多个 API 端点
- 代码逻辑有更新

**重启命令**:
```bash
# 如果使用 systemd
sudo systemctl restart your-service-name

# 如果使用 screen/tmux
ps aux | grep "python.*main.py"
kill -9 <PID>
python3 main.py &

# 如果使用 uvicorn
uvicorn main:app --host 0.0.0.0 --port 8080 --reload &
```

---

## 🧪 验证清单

### 部署后验证步骤

#### 1. 数据库验证
```bash
# 检查表是否创建成功
psql -U username -d dual_agent_chat -c "\dt"

# 应该看到：
# - message_relevance_scores
# - system_configs

# 检查字段是否添加成功
psql -U username -d dual_agent_chat -c "\d topics"

# 应该看到 topic_description 字段
```

#### 2. 服务验证
```bash
# 检查 API 服务器
curl http://localhost:8080/api/health

# 检查系统配置 API
curl http://localhost:8080/api/admin/config/system

# 检查 Worker 状态
celery -A workers.celery_app inspect active
```

#### 3. 功能验证

**访问前端页面**:
- http://服务器IP:8080/ - 主页
- http://服务器IP:8080/system-config.html - 系统配置
- http://服务器IP:8080/admin.html - 管理后台
- http://服务器IP:8080/simulator.html - 智能体模拟器

**测试核心功能**:
1. 创建新话题
2. 发送消息
3. 查看消息评分（需要配置 LLM）
4. 查看对话总结（需要配置 LLM）

---

## 📝 发布步骤清单

### 完整发布流程

#### 准备阶段（5 分钟）

- [ ] 1. 备份服务器数据库
  ```bash
  pg_dump -U user -d dual_agent_chat > backup_$(date +%Y%m%d_%H%M%S).sql
  ```

- [ ] 2. 备份服务器代码
  ```bash
  cd /path/to/project
  tar -czf backup_$(date +%Y%m%d_%H%M%S).tar.gz .
  ```

#### 停止服务（1 分钟）

- [ ] 3. 停止 Celery Worker
  ```bash
  pkill -f "celery.*worker"
  ```

- [ ] 4. 停止 API 服务器
  ```bash
  pkill -f "python.*main.py"
  # 或
  sudo systemctl stop your-service-name
  ```

#### 代码更新（2 分钟）

- [ ] 5. 拉取最新代码
  ```bash
  git fetch origin
  git pull origin main
  ```

- [ ] 6. 验证代码版本
  ```bash
  git log --oneline -1
  # 应显示：76fc222
  ```

#### 数据库迁移（3 分钟）

- [ ] 7. 执行消息评分迁移
  ```bash
  python3 run_migration.py
  ```

- [ ] 8. 初始化系统配置
  ```bash
  python3 init_system_config.py
  ```

- [ ] 9. 验证表创建
  ```bash
  psql -U user -d dual_agent_chat -c "\dt" | grep -E "message_relevance_scores|system_configs"
  ```

#### 启动服务（2 分钟）

- [ ] 10. 启动 Celery Worker
  ```bash
  celery -A workers.celery_app worker \
    --loglevel=info \
    --concurrency=5 \
    --queues=summary_queue,scoring_queue \
    --logfile=logs/worker.log &
  ```

- [ ] 11. 启动 API 服务器
  ```bash
  python3 main.py &
  # 或
  sudo systemctl start your-service-name
  ```

#### 验证（5 分钟）

- [ ] 12. 检查服务状态
  ```bash
  # API 服务器
  curl http://localhost:8080/api/health
  
  # Worker
  celery -A workers.celery_app inspect active
  ```

- [ ] 13. 访问前端页面
  - http://服务器IP:8080/
  - http://服务器IP:8080/system-config.html

- [ ] 14. 配置 LLM API Key
  - 访问系统配置页面
  - 设置 DeepSeek 或 MiniMax API Key
  - 保存配置
  - 重启 Worker（如果需要）

- [ ] 15. 测试核心功能
  - 创建话题
  - 发送消息
  - 查看评分
  - 查看总结

---

## ⚠️ 风险评估

### 高风险项

#### 1. 数据库迁移 ⚠️

**风险**: 迁移失败可能导致服务不可用

**缓解措施**:
- ✅ 迁移前备份数据库
- ✅ 使用 `IF NOT EXISTS` 避免重复创建
- ✅ 在测试环境先验证
- ✅ 准备回滚方案

#### 2. 配置迁移 ⚠️

**风险**: .env 中的 LLM 配置需要迁移到系统配置表

**缓解措施**:
- ✅ 保留 .env 中的配置作为默认值
- ✅ 在管理后台重新配置 API Key
- ✅ 验证配置生效

### 中风险项

#### 3. Celery 队列变更 ⚠️

**风险**: 新增 `scoring_queue`，旧 Worker 不监听

**缓解措施**:
- ✅ 重启 Worker 时指定新队列
- ✅ 使用提供的 `restart_worker.sh` 脚本

#### 4. 前端缓存 ⚠️

**风险**: 浏览器缓存旧版本前端文件

**缓解措施**:
- ✅ 强制刷新（Ctrl+Shift+R）
- ✅ 清空浏览器缓存

### 低风险项

#### 5. 新增功能

**风险**: 消息评分、模拟器等新功能不影响现有功能

**缓解措施**:
- ✅ 可以逐步启用和测试

---

## 🔙 回滚方案

### 代码回滚

```bash
# 回滚到上一个版本
git log --oneline -5
git reset --hard <previous_commit_hash>

# 重启服务
sudo systemctl restart your-service-name
```

### 数据库回滚

```bash
# 删除新表（会丢失数据）
psql -U user -d dual_agent_chat

DROP TABLE IF EXISTS message_relevance_scores CASCADE;
DROP TABLE IF EXISTS system_configs CASCADE;
ALTER TABLE topics DROP COLUMN IF EXISTS topic_description;
```

**注意**: 数据库回滚会丢失新功能的数据，建议只在严重问题时使用

### 配置回滚

```bash
# 恢复旧的 .env 文件
cp .env.backup .env
```

---

## 📊 预计停机时间

| 阶段 | 预计时间 |
|------|---------|
| 备份 | 5 分钟 |
| 停止服务 | 1 分钟 |
| 代码更新 | 2 分钟 |
| 数据库迁移 | 3 分钟 |
| 启动服务 | 2 分钟 |
| 验证 | 5 分钟 |
| **总计** | **18 分钟** |

**建议发布时间**: 业务低峰期（如凌晨或周末）

---

## 📞 支持资源

### 文档

- **发布评估报告**: `明宽服务器发布评估报告.md`（之前的版本）
- **系统配置说明**: `系统配置管理功能说明.md`
- **Worker 管理**: `Worker管理功能完成总结.md`
- **MiniMax 集成**: `MiniMax完整集成总结.md`

### 诊断工具

```bash
# 检查 LLM 配置
python3 diagnose_llm_provider.py

# 检查 Celery 状态
python3 check_celery_status.py

# 检查系统配置
python3 verify_config_usage.py
```

### 联系方式

如有问题，请提供：
- Git commit hash: `git rev-parse HEAD`
- 错误日志：`tail -100 logs/app.log`
- 数据库状态：检查表是否存在
- Celery 状态：`celery -A workers.celery_app inspect active`

---

## ✅ 最终建议

### 发布决策

**✅ 建议发布**

**理由**:
1. 代码已同步到 Git，工作区干净
2. 数据库迁移脚本完善，使用 `IF NOT EXISTS` 安全可重复执行
3. 提供了完整的回滚方案
4. 预计停机时间短（18 分钟）
5. 新功能不影响现有功能

### 关键注意事项

1. **必须执行数据库迁移** - 2 个迁移脚本
2. **必须重启 Worker** - 监听新队列
3. **必须重启 API 服务器** - 加载新代码
4. **必须配置 LLM API Key** - 在系统配置页面
5. **建议在低峰期发布** - 减少用户影响

### 发布前最后检查

- [ ] 已备份数据库
- [ ] 已备份代码
- [ ] 已准备回滚方案
- [ ] 已通知相关人员
- [ ] 已选择合适的发布时间窗口

---

**报告生成时间**: 2026-02-25  
**评估人员**: Kiro AI Assistant  
**项目版本**: 76fc222 (main)  
**评估状态**: ✅ 可以发布

