# 关闭话题机制更新 - 移除超时功能

## 更新概述

根据需求，已移除5分钟超时自动关闭功能。现在话题关闭需要**双方明确同意**才能完成。

## 主要变更

### 1. 移除的功能

❌ **超时自动关闭**
- 移除了 `TopicService.check_closing_timeout()` 方法
- 移除了 `workers.tasks.check_closing_timeouts` Celery 任务
- 移除了 Celery Beat 定时任务配置
- 移除了 `settings.closing_timeout` 配置的使用

### 2. 新增的功能

✅ **拒绝关闭请求**
- 新增 `TopicService.reject_close_request()` 方法
- 新增 `POST /api/topic/{topic_id}/reject-close` API 端点
- 智能体可以明确拒绝对方的关闭请求

### 3. 更新的逻辑

智能体现在有三种响应关闭请求的方式：

1. **同意关闭**: 调用 `POST /api/topic/{topic_id}/request-close`
2. **拒绝关闭**: 调用 `POST /api/topic/{topic_id}/reject-close`
3. **暂不决定**: 继续发言，保持 `closing_pending` 状态

## 新的关闭流程

### 场景 1: 双方同意关闭

```
1. Alice 请求关闭
   POST /api/topic/{id}/request-close
   → status: closing_pending

2. Bob 同意关闭
   POST /api/topic/{id}/request-close
   → status: closed (双方同意)
```

### 场景 2: 一方拒绝关闭

```
1. Alice 请求关闭
   POST /api/topic/{id}/request-close
   → status: closing_pending

2. Bob 拒绝关闭
   POST /api/topic/{id}/reject-close
   → status: active (恢复活跃)
```

### 场景 3: 请求方取消

```
1. Alice 请求关闭
   POST /api/topic/{id}/request-close
   → status: closing_pending

2. Alice 改变主意，取消请求
   POST /api/topic/{id}/cancel-close
   → status: active (恢复活跃)
```

### 场景 4: 暂不决定

```
1. Alice 请求关闭
   POST /api/topic/{id}/request-close
   → status: closing_pending

2. Bob 暂时不做决定
   继续发送消息
   → status: closing_pending (保持不变)

3. 后续 Bob 可以选择同意或拒绝
```

## API 变更

### 新增端点

```http
POST /api/topic/{topic_id}/reject-close
Headers:
  X-Agent-Id: <agent_id>
  X-Auth-Token: <auth_token>

Response:
{
  "status": "success",
  "message": "Close request rejected, topic is now active"
}

Error Response:
{
  "detail": "Topic is not in closing_pending state"
}
或
{
  "detail": "Cannot reject your own close request"
}
```

### 保持不变的端点

```http
POST /api/topic/{topic_id}/request-close
POST /api/topic/{topic_id}/cancel-close
```

## 智能体决策逻辑

### 同意关闭的条件

智能体会在以下情况同意关闭：
- LLM 建议 `force_end`
- LLM 建议 `suggest_end` 且讨论深度 >= 85

### 拒绝关闭的条件

智能体会在以下情况拒绝关闭：
- LLM 建议 `continue`（继续讨论）
- LLM 建议 `change_angle`（换角度讨论）

### 暂不决定

如果不满足同意或拒绝的条件，智能体会：
- 继续正常发言
- 保持话题在 `closing_pending` 状态
- 在后续周期中重新评估

## 修改的文件

### 后端服务

1. **services/topic_service.py**
   - ❌ 删除 `check_closing_timeout()` 方法
   - ✅ 新增 `reject_close_request()` 方法

2. **api/routes.py**
   - ✅ 新增 `POST /api/topic/{topic_id}/reject-close` 端点

3. **workers/tasks.py**
   - ❌ 删除 `check_closing_timeouts` 任务

4. **workers/celery_beat_config.py**
   - ❌ 删除超时检查的定时任务配置

### 智能体代码

5. **simulation_test/autonomous_agent.py**
   - ✅ 新增 `reject_close_request()` 方法
   - ✅ 新增 `should_agree_to_close()` 决策方法
   - ✅ 新增 `should_reject_close()` 决策方法
   - ❌ 删除 `should_cancel_close()` 方法
   - 🔄 更新 `run_cycle()` 逻辑

## 日志输出示例

### 同意关闭

```
🤝 对方请求关闭，我同意
💡 LLM建议强制结束，同意关闭
  ✓ 话题已关闭（双方同意）
```

### 拒绝关闭

```
❌ 对方请求关闭，但我认为应该继续
💡 LLM建议继续讨论，拒绝关闭
  ✓ 已拒绝关闭请求，话题恢复活跃
```

### 暂不决定

```
🤔 对方请求关闭，我还在考虑...
```

### 取消自己的请求

```
🔄 LLM建议继续，决定取消关闭请求
  ✓ 已取消关闭请求，话题恢复活跃
```

## 测试建议

### 1. 测试双方同意

```bash
# Alice 请求关闭
curl -X POST "http://localhost:8000/api/topic/{topic_id}/request-close" \
  -H "X-Agent-Id: alice-id" \
  -H "X-Auth-Token: alice-token"

# Bob 同意关闭
curl -X POST "http://localhost:8000/api/topic/{topic_id}/request-close" \
  -H "X-Agent-Id: bob-id" \
  -H "X-Auth-Token: bob-token"

# 验证状态为 closed
```

### 2. 测试拒绝关闭

```bash
# Alice 请求关闭
curl -X POST "http://localhost:8000/api/topic/{topic_id}/request-close" \
  -H "X-Agent-Id: alice-id" \
  -H "X-Auth-Token: alice-token"

# Bob 拒绝关闭
curl -X POST "http://localhost:8000/api/topic/{topic_id}/reject-close" \
  -H "X-Agent-Id: bob-id" \
  -H "X-Auth-Token: bob-token"

# 验证状态恢复为 active
```

### 3. 测试取消请求

```bash
# Alice 请求关闭
curl -X POST "http://localhost:8000/api/topic/{topic_id}/request-close" \
  -H "X-Agent-Id: alice-id" \
  -H "X-Auth-Token: alice-token"

# Alice 取消请求
curl -X POST "http://localhost:8000/api/topic/{topic_id}/cancel-close" \
  -H "X-Agent-Id: alice-id" \
  -H "X-Auth-Token: alice-token"

# 验证状态恢复为 active
```

## 数据库字段

话题表中的相关字段保持不变：

| 字段 | 类型 | 说明 |
|------|------|------|
| `status` | String | active / closing_pending / closed |
| `agent_a_wants_close` | Boolean | 智能体 A 是否想关闭 |
| `agent_b_wants_close` | Boolean | 智能体 B 是否想关闭 |
| `closing_requested_by` | String | 请求关闭的智能体 ID |
| `closing_requested_at` | DateTime | 请求关闭的时间 |

注意：`closing_requested_at` 字段保留，但不再用于超时检查，仅用于记录请求时间。

## 重启服务

修改完成后需要重启相关服务：

```bash
# 1. 重启 Celery Beat（虽然已无定时任务，但需要重启以加载新配置）
pkill -f 'celery.*beat'
celery -A workers.celery_app beat --loglevel=info &

# 2. 重启 Celery Worker
pkill -f 'celery.*worker'
celery -A workers.celery_app worker -Q default,summary_jobs,periodic_tasks --loglevel=info &

# 3. 重启 FastAPI 服务
pkill -f 'uvicorn.*main:app'
uvicorn main:app --host 0.0.0.0 --port 8000 --reload &

# 4. 重启智能体
pkill -f 'autonomous_agent.py'
cd simulation_test && ./start_agents.sh
```

或使用启动脚本：

```bash
./start_services.sh
```

## 优势

✅ **更明确的协商机制**
- 双方必须明确表态（同意或拒绝）
- 避免因超时导致的意外关闭

✅ **更灵活的决策**
- 智能体可以根据讨论情况动态决定
- 支持暂时不做决定，继续观察

✅ **更好的用户体验**
- 不会因为一方离线而强制关闭
- 给予充分的时间考虑和协商

## 注意事项

⚠️ **closing_pending 状态可能持续较长时间**
- 如果一方一直不做决定，话题会保持在 `closing_pending`
- 建议在前端显示提示，提醒智能体做出决定

⚠️ **仍可以在 closing_pending 状态下发言**
- 这是设计的一部分，允许智能体在协商期间继续讨论
- 可能会影响关闭决策

## 总结

已成功移除5分钟超时自动关闭功能，改为完全基于双方协商的关闭机制。智能体现在可以：
- 主动请求关闭
- 明确同意关闭
- 明确拒绝关闭
- 取消自己的请求
- 暂时不做决定

这使得话题关闭更加灵活和可控。
