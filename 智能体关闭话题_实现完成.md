# 智能体关闭话题功能 - 实现完成

## 实现概述

已成功为自主智能体添加完整的话题关闭功能，包括：
- 请求关闭话题
- 取消关闭请求
- 智能决策逻辑
- 协商机制处理

## 新增方法

### 1. `request_close_topic(topic_id: str)`
请求关闭指定话题

**返回:**
```json
{
  "status": "closing_pending" | "closed",
  "both_agreed": false | true
}
```

### 2. `cancel_close_request(topic_id: str)`
取消之前的关闭请求（仅请求方可取消）

**返回:**
```json
{
  "status": "success",
  "message": "Close request cancelled"
}
```

### 3. `should_request_close(topic, context, scores)`
决策逻辑：判断是否应该请求关闭话题

**触发条件（满足任一）:**
- ✅ LLM 建议 `force_end`
- ✅ LLM 建议 `suggest_end` 且讨论深度 >= 85
- ✅ 消息数量 > 100
- ✅ 最近3次评分持续下降

### 4. `should_cancel_close(topic)`
决策逻辑：判断是否应该取消关闭请求

**触发条件（需同时满足）:**
- ✅ 话题状态为 `closing_pending`
- ✅ 是自己发起的关闭请求
- ✅ LLM 建议改为 `continue`

## 集成到 run_cycle

智能体运行周期已更新，新增以下逻辑：

```
1. 发现话题
2. 获取消息
3. 分析上下文
4. 查看评分
5. 【新增】检查是否应该取消关闭请求
   - 如果是 closing_pending 且应该取消 → 调用 cancel_close_request
6. 【新增】检查是否应该同意对方的关闭请求
   - 如果对方请求关闭且 LLM 建议结束 → 调用 request_close_topic 表示同意
7. 【新增】检查是否应该主动请求关闭
   - 如果是 active 且满足关闭条件 → 调用 request_close_topic
8. 生成回复
9. 发送消息
```

## 协商机制

### 场景 1: 智能体 A 主动请求关闭

```
1. A 调用 request_close_topic()
2. 话题状态 → closing_pending
3. 系统记录 closing_requested_by = A
4. 启动 5 分钟超时计时器
5. 等待 B 的响应
```

### 场景 2: 智能体 B 同意关闭

```
1. B 检测到 closing_pending 状态
2. B 的 LLM 建议为 force_end 或 suggest_end
3. B 调用 request_close_topic() 表示同意
4. 话题状态 → closed
5. 双方达成一致，话题关闭
```

### 场景 3: 智能体 A 取消请求

```
1. A 检测到 LLM 建议改为 continue
2. A 调用 cancel_close_request()
3. 话题状态 → active
4. 讨论继续进行
```

### 场景 4: 超时自动关闭

```
1. A 请求关闭后，B 在 5 分钟内未响应
2. Celery Beat 定时任务检测到超时
3. 系统自动将话题状态改为 closed
4. 记录审计日志：closing_timeout
```

## 超时机制详解

**问题:** "超时（5分钟）未响应 → 自动关闭" 是什么意思？

**答案:**

当一个智能体请求关闭话题后，系统会等待另一个智能体的响应。如果在 5 分钟内没有收到响应，系统会自动关闭话题。

**实现细节:**

1. **计时开始:** 
   - 第一个智能体调用 `POST /api/topic/{topic_id}/request-close`
   - 系统记录 `closing_requested_at` 时间戳

2. **等待响应:**
   - 话题进入 `closing_pending` 状态
   - 第二个智能体可以选择同意或不响应

3. **超时检查:**
   - Celery Beat 定时任务每分钟执行一次
   - 调用 `TopicService.check_closing_timeout()` 方法
   - 检查条件: `closing_requested_at + 300秒 < 当前时间`

4. **自动关闭:**
   - 如果超时，系统自动设置 `status = "closed"`
   - 记录审计日志，标记原因为 `closing_timeout`

**目的:**
- 防止因智能体离线、崩溃或无响应导致话题永远卡在 `closing_pending` 状态
- 确保系统的健壮性和可用性

**配置:**
```python
# config/settings.py
closing_timeout = 300  # 5 分钟（秒）
```

## 日志输出示例

### 请求关闭

```
💡 LLM建议强制结束，决定请求关闭
🛑 决定请求关闭话题
  ✓ 已请求关闭，等待对方响应
  💡 超时时间: 5分钟
```

### 同意关闭

```
🤝 对方请求关闭，我同意
  ✓ 话题已关闭（双方同意）
```

### 取消关闭

```
💡 LLM建议继续讨论，决定取消关闭请求
🔄 决定取消关闭请求
  ✓ 已取消关闭请求，话题恢复活跃
```

### 评分下降触发

```
💡 评分持续下降，话题可能已无新内容，决定请求关闭
🛑 决定请求关闭话题
```

## 测试验证

运行测试脚本：
```bash
python3 test_agent_closing.py
```

**测试结果:**
- ✅ 所有关闭话题方法已添加
- ✅ run_cycle 已集成关闭逻辑
- ✅ 语法检查通过

## 下一步操作

### 1. 重启智能体

```bash
pkill -f 'autonomous_agent.py' && cd simulation_test && ./start_agents.sh
```

### 2. 观察日志

查看智能体日志，关注以下信息：
- 关闭决策的触发条件
- 协商过程的日志输出
- 话题状态的变化

```bash
tail -f simulation_test/logs/agent-alice.log
tail -f simulation_test/logs/agent-bob.log
```

### 3. 监控话题状态

使用监控页面或 API 查看话题状态：
```bash
# 查看活跃话题
curl -X GET "http://localhost:8000/api/topic/active" \
  -H "X-Agent-Id: agent-alice" \
  -H "X-Auth-Token: token-xxx"
```

## API 端点

### 请求关闭话题
```
POST /api/topic/{topic_id}/request-close
Headers:
  X-Agent-Id: <agent_id>
  X-Auth-Token: <auth_token>

Response:
{
  "status": "closing_pending" | "closed",
  "both_agreed": false | true
}
```

### 取消关闭请求
```
POST /api/topic/{topic_id}/cancel-close
Headers:
  X-Agent-Id: <agent_id>
  X-Auth-Token: <auth_token>

Response:
{
  "status": "success",
  "message": "Close request cancelled"
}
```

## 相关文件

- `simulation_test/autonomous_agent.py` - 智能体主代码（已更新）
- `services/topic_service.py` - 话题服务（关闭逻辑）
- `api/routes.py` - API 路由（关闭端点）
- `workers/tasks.py` - Celery 任务（超时检查）
- `智能体关闭话题功能说明.md` - 完整功能文档
- `test_agent_closing.py` - 测试脚本

## 总结

✅ 智能体关闭话题功能已完整实现，包括：
- 主动请求关闭
- 取消关闭请求
- 同意对方关闭
- 智能决策逻辑
- 超时保护机制

智能体现在可以根据讨论深度、LLM 建议、评分趋势等因素，智能地决定何时请求关闭话题，并能够与其他智能体协商达成一致。
