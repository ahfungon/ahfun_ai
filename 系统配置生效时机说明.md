# 系统配置生效时机说明

## ❓ 问题

在系统配置里修改 prompt 和大模型选择，是不是要触发重启 Worker？

## ✅ 答案总结

| 配置项 | 是否需要重启 Worker | 说明 |
|--------|-------------------|------|
| **Prompt 配置** | ❌ 不需要 | 每次调用时实时读取 |
| **大模型选择** | ⚠️ 建议重启 | 服务初始化时读取 |
| **Token 阈值** | ❌ 不需要 | 每次检查时实时读取 |
| **API Key** | ⚠️ 建议重启 | 服务初始化时读取 |

## 📋 详细说明

### 1️⃣ Prompt 配置（不需要重启）

**配置项**：
- `scoring_prompt` - 消息评分 Prompt
- `summary_prompt` - 对话总结 Prompt

**生效时机**：✅ 立即生效（下次调用时）

**原因**：
```python
# services/message_scoring_service.py
def _build_evaluation_prompt(self, ...):
    # 每次构建 prompt 时都会重新读取配置
    custom_prompt = self.config_service.get_config_value('scoring_prompt', None)
    
    if custom_prompt:
        return custom_prompt.format(...)
```

**测试方法**：
1. 修改 Prompt
2. 发送一条新消息
3. 查看评分结果，应该使用新的 Prompt

**结论**：❌ 不需要重启 Worker

---

### 2️⃣ 大模型选择（建议重启）

**配置项**：
- `llm_provider_scoring` - 消息评分 LLM
- `llm_provider_summary` - 对话总结 LLM

**生效时机**：⚠️ 服务初始化时读取

**原因**：
```python
# services/message_scoring_service.py
def __init__(self, db: Session):
    self.db = db
    self.config_service = SystemConfigService(db)
    
    # 初始化时读取 LLM 提供商
    provider = self.config_service.get_config_value('llm_provider_scoring', 'deepseek')
    
    if provider == 'minimax':
        self.llm_client = MiniMaxClient(...)
    else:
        self.llm_client = DeepSeekClient(...)
```

**问题**：
- MessageScoringService 和 SummaryService 在初始化时读取配置
- 已经创建的服务实例不会自动更新
- Worker 进程中的服务实例会一直存在

**影响范围**：
- ✅ API 服务器：每次请求都会创建新的服务实例，立即生效
- ⚠️ Celery Worker：服务实例在进程启动时创建，需要重启

**结论**：⚠️ 建议重启 Worker（API 服务器不需要）

---

### 3️⃣ Token 阈值（不需要重启）

**配置项**：
- `summary_threshold` - 总结触发阈值

**生效时机**：✅ 立即生效（下次检查时）

**原因**：
```python
# services/message_service.py
def create_message(self, ...):
    # 每次创建消息时都会重新读取阈值
    if topic.summary_threshold:
        threshold = topic.summary_threshold
    else:
        threshold = self.config_service.get_config_value('summary_threshold', settings.summary_threshold)
```

**测试方法**：
1. 修改阈值（如从 8000 改为 2000）
2. 发送消息
3. 观察是否在新阈值时触发总结

**结论**：❌ 不需要重启 Worker

---

### 4️⃣ API Key 配置（建议重启）

**配置项**：
- `deepseek_api_key`
- `minimax_api_key`
- `deepseek_api_url`
- `minimax_api_url`
- `deepseek_model`
- `minimax_model`

**生效时机**：⚠️ 服务初始化时读取

**原因**：
```python
# services/message_scoring_service.py
def __init__(self, db: Session):
    # 初始化时读取 API Key
    api_key = self.config_service.get_config_value('minimax_api_key', '')
    api_url = self.config_service.get_config_value('minimax_api_url', '...')
    
    self.llm_client = MiniMaxClient(
        api_key=api_key,
        api_url=api_url,
        ...
    )
```

**结论**：⚠️ 建议重启 Worker

---

## 🔄 何时需要重启 Worker

### 需要重启的情况

1. **切换 LLM 提供商**
   - 从 DeepSeek 切换到 MiniMax
   - 从 MiniMax 切换到 DeepSeek

2. **修改 API Key**
   - 更换 DeepSeek API Key
   - 更换 MiniMax API Key

3. **修改 API URL**
   - 更改 API 端点地址

4. **修改模型名称**
   - 更改使用的模型

### 不需要重启的情况

1. **修改 Prompt**
   - 修改评分 Prompt
   - 修改总结 Prompt

2. **修改阈值**
   - 修改 Token 阈值

---

## 🛠️ 如何重启 Worker

### 方法 1：手动重启（推荐）

```bash
# 停止 Worker
pkill -f "celery -A workers.celery_app worker"

# 启动 Worker
celery -A workers.celery_app worker --loglevel=info --logfile=logs/worker.log &
```

### 方法 2：使用脚本

创建一个重启脚本 `restart_worker.sh`：

```bash
#!/bin/bash
echo "停止 Celery Worker..."
pkill -f "celery -A workers.celery_app worker"

echo "等待进程结束..."
sleep 2

echo "启动 Celery Worker..."
celery -A workers.celery_app worker --loglevel=info --logfile=logs/worker.log &

echo "✅ Worker 已重启"
```

使用：
```bash
chmod +x restart_worker.sh
./restart_worker.sh
```

### 方法 3：检查 Worker 状态

```bash
# 检查 Worker 是否运行
ps aux | grep "celery.*worker"

# 查看 Worker 日志
tail -f logs/worker.log
```

---

## 🎯 最佳实践

### 配置修改流程

1. **修改 Prompt**
   ```
   修改配置 → 保存 → 立即生效 ✅
   ```

2. **修改 Token 阈值**
   ```
   修改配置 → 保存 → 立即生效 ✅
   ```

3. **切换 LLM 提供商**
   ```
   修改配置 → 保存 → 重启 Worker ⚠️
   ```

4. **修改 API Key**
   ```
   修改配置 → 保存 → 重启 Worker ⚠️
   ```

### 验证配置是否生效

#### 验证 Prompt 修改

```bash
# 1. 修改 Prompt
# 2. 发送一条消息
# 3. 查看评分或总结结果
```

#### 验证 LLM 切换

```bash
# 1. 切换 LLM 提供商
# 2. 重启 Worker
# 3. 运行测试脚本
python3 test_minimax_integration.py

# 4. 查看日志
tail -f logs/worker.log | grep -i "minimax\|deepseek"
```

---

## 📊 配置生效时机对比表

| 配置类型 | 读取时机 | 生效范围 | 是否需要重启 |
|---------|---------|---------|-------------|
| Prompt | 每次调用时 | 立即生效 | ❌ 不需要 |
| Token 阈值 | 每次检查时 | 立即生效 | ❌ 不需要 |
| LLM 提供商 | 服务初始化时 | API 立即生效<br>Worker 需重启 | ⚠️ Worker 需要 |
| API Key | 服务初始化时 | API 立即生效<br>Worker 需重启 | ⚠️ Worker 需要 |
| API URL | 服务初始化时 | API 立即生效<br>Worker 需重启 | ⚠️ Worker 需要 |
| 模型名称 | 服务初始化时 | API 立即生效<br>Worker 需重启 | ⚠️ Worker 需要 |

---

## 🔍 为什么 API 服务器不需要重启？

### API 服务器的工作方式

```python
# api/routes.py
@app.post("/api/messages")
async def create_message(...):
    db = SessionLocal()
    try:
        # 每次请求都创建新的服务实例
        message_service = MessageService(db)
        scoring_service = MessageScoringService(db)  # 重新读取配置
        
        # 使用服务
        message = message_service.create_message(...)
        score = scoring_service.evaluate_message(...)
    finally:
        db.close()
```

**特点**：
- 每次 HTTP 请求都创建新的服务实例
- 每次都会重新读取系统配置
- 配置修改立即生效

### Celery Worker 的工作方式

```python
# workers/tasks.py
@celery_app.task
def process_summary_job(job_id: str):
    db = SessionLocal()
    try:
        # Worker 启动时创建服务实例，一直复用
        summary_service = SummaryService(db)  # 只在启动时读取配置
        
        # 使用服务
        result = summary_service.generate_summary(...)
    finally:
        db.close()
```

**特点**：
- Worker 进程启动时创建服务实例
- 服务实例在整个进程生命周期中复用
- 配置修改不会影响已创建的实例

---

## 💡 改进建议（未来优化）

### 方案 1：每次任务都重新创建服务

```python
@celery_app.task
def process_summary_job(job_id: str):
    db = SessionLocal()
    try:
        # 每次任务都创建新的服务实例
        summary_service = SummaryService(db)  # 重新读取配置
        result = summary_service.generate_summary(...)
    finally:
        db.close()
```

**优点**：配置立即生效，不需要重启
**缺点**：每次都初始化客户端，性能略有影响

### 方案 2：添加配置热重载

```python
class MessageScoringService:
    def __init__(self, db: Session):
        self.db = db
        self.config_service = SystemConfigService(db)
        self._llm_client = None
        self._last_provider = None
    
    @property
    def llm_client(self):
        # 每次使用时检查配置是否变化
        current_provider = self.config_service.get_config_value('llm_provider_scoring')
        
        if self._last_provider != current_provider:
            # 配置变化，重新创建客户端
            self._llm_client = self._create_client(current_provider)
            self._last_provider = current_provider
        
        return self._llm_client
```

**优点**：配置自动热重载
**缺点**：实现复杂，需要测试

---

## ✨ 总结

### 你的问题
> 我在系统配置里，修改 prompt 和大模型选择，是不是要触发重启 Worker？

### 答案

**Prompt 修改**：❌ 不需要重启
- 每次调用时实时读取
- 立即生效

**大模型选择**：⚠️ 建议重启 Worker
- 服务初始化时读取
- API 服务器立即生效（每次请求创建新实例）
- Celery Worker 需要重启（服务实例复用）

### 重启命令

```bash
pkill -f "celery -A workers.celery_app worker"
celery -A workers.celery_app worker --loglevel=info --logfile=logs/worker.log &
```

### 快速判断

- 修改 Prompt、阈值 → 不需要重启
- 切换 LLM、修改 API Key → 建议重启 Worker
