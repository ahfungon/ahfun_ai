# 后端服务 502 错误修复说明

## 📋 问题描述

**时间**: 2026-02-25

**现象**: 访问系统配置页面 http://localhost:8080/system-config.html 时出现 502 错误

**错误信息**:
```
api/admin/config/system:1 Failed to load resource: the server responded with a status of 502 (Bad Gateway)
system-config.html:384 加载配置失败: Error: HTTP 502
api/admin/worker/status:1 Failed to load resource: the server responded with a status of 502 (Bad Gateway)
system-config.html:627 检查 Worker 状态失败: SyntaxError: Unexpected token '<', "<html><h"... is not valid JSON
```

---

## 🔍 问题诊断

### 1. 检查后端服务状态

```bash
ps aux | grep uvicorn | grep -v grep
```

**结果**: 没有输出，说明后端服务没有运行

### 2. 检查日志

```bash
tail -50 logs/api.log
```

**发现问题**:
```
ERROR: [Errno 48] error while attempting to bind on address ('0.0.0.0', 8080): address already in use
```

**原因**: 端口 8080 被占用

### 3. 检查端口占用

```bash
lsof -i :8080
```

**结果**:
```
COMMAND     PID     USER   FD   TYPE             DEVICE SIZE/OFF NODE NAME
nginx     60646 mingkuan    6u  IPv4 ...      0t0  TCP *:http-alt (LISTEN)
nginx     60647 mingkuan    3u  IPv4 ...      0t0  TCP localhost:http-alt->localhost:59628 (ESTABLISHED)
```

**原因**: nginx 占用了 8080 端口

---

## ✅ 解决方案

### 步骤 1: 停止 nginx

```bash
pkill -f nginx
```

### 步骤 2: 重启后端服务

```bash
nohup uvicorn main:app --host 0.0.0.0 --port 8080 > logs/api.log 2>&1 &
```

### 步骤 3: 验证服务状态

```bash
# 检查健康状态
curl http://localhost:8080/api/health

# 检查系统配置 API
curl http://localhost:8080/api/admin/config/system

# 检查 Worker 状态 API
curl http://localhost:8080/api/admin/worker/status
```

**结果**: ✅ 所有 API 正常工作

---

## 🔧 预防措施

### 1. 避免端口冲突

**方法 1**: 使用不同的端口

修改后端服务端口：
```bash
uvicorn main:app --host 0.0.0.0 --port 8000
```

修改前端配置中的 API 地址：
```javascript
const API_BASE_URL = 'http://localhost:8000';
```

**方法 2**: 停止 nginx

如果不需要 nginx，可以停止它：
```bash
pkill -f nginx
```

### 2. 启动脚本

创建启动脚本 `start_backend.sh`：
```bash
#!/bin/bash

# 检查端口是否被占用
if lsof -i :8080 > /dev/null 2>&1; then
    echo "端口 8080 被占用，尝试停止占用进程..."
    pkill -f "uvicorn main:app"
    pkill -f nginx
    sleep 2
fi

# 启动后端服务
echo "启动后端服务..."
nohup uvicorn main:app --host 0.0.0.0 --port 8080 > logs/api.log 2>&1 &

# 等待服务启动
sleep 3

# 检查服务状态
if curl -s http://localhost:8080/api/health > /dev/null; then
    echo "✅ 后端服务启动成功"
else
    echo "❌ 后端服务启动失败"
    exit 1
fi
```

使用方法：
```bash
chmod +x start_backend.sh
./start_backend.sh
```

### 3. 监控脚本

创建监控脚本 `check_backend.sh`：
```bash
#!/bin/bash

# 检查后端服务是否运行
if ! ps aux | grep "uvicorn main:app" | grep -v grep > /dev/null; then
    echo "❌ 后端服务未运行"
    exit 1
fi

# 检查健康状态
if ! curl -s http://localhost:8080/api/health > /dev/null; then
    echo "❌ 后端服务健康检查失败"
    exit 1
fi

echo "✅ 后端服务运行正常"
```

---

## 📊 服务状态检查清单

### 快速检查命令

```bash
# 1. 检查后端进程
ps aux | grep uvicorn | grep -v grep

# 2. 检查端口占用
lsof -i :8080

# 3. 检查健康状态
curl http://localhost:8080/api/health

# 4. 检查 Worker 状态
curl http://localhost:8080/api/admin/worker/status

# 5. 查看最新日志
tail -50 logs/api.log
```

### 完整检查脚本

```bash
#!/bin/bash

echo "=========================================="
echo "后端服务状态检查"
echo "=========================================="

# 1. 检查进程
echo -e "\n1. 检查后端进程:"
if ps aux | grep "uvicorn main:app" | grep -v grep > /dev/null; then
    echo "✅ 后端进程运行中"
    ps aux | grep "uvicorn main:app" | grep -v grep
else
    echo "❌ 后端进程未运行"
fi

# 2. 检查端口
echo -e "\n2. 检查端口 8080:"
if lsof -i :8080 > /dev/null 2>&1; then
    echo "✅ 端口 8080 已被占用"
    lsof -i :8080 | head -5
else
    echo "❌ 端口 8080 未被占用"
fi

# 3. 检查健康状态
echo -e "\n3. 检查健康状态:"
if curl -s http://localhost:8080/api/health > /dev/null; then
    echo "✅ 健康检查通过"
    curl -s http://localhost:8080/api/health | python -m json.tool | head -10
else
    echo "❌ 健康检查失败"
fi

# 4. 检查 Worker
echo -e "\n4. 检查 Worker 状态:"
if curl -s http://localhost:8080/api/admin/worker/status > /dev/null; then
    echo "✅ Worker 状态检查通过"
    curl -s http://localhost:8080/api/admin/worker/status | python -m json.tool
else
    echo "❌ Worker 状态检查失败"
fi

echo -e "\n=========================================="
echo "检查完成"
echo "=========================================="
```

---

## 🚨 常见问题

### 问题 1: 端口被占用

**现象**: `address already in use`

**解决**:
```bash
# 查看占用进程
lsof -i :8080

# 停止占用进程
pkill -f nginx
# 或
pkill -f "uvicorn main:app"
```

### 问题 2: 服务启动失败

**现象**: 启动后立即退出

**解决**:
```bash
# 查看日志
tail -50 logs/api.log

# 检查 Python 环境
which python
python --version

# 检查依赖
pip list | grep uvicorn
pip list | grep fastapi
```

### 问题 3: 502 Bad Gateway

**现象**: API 返回 502 错误

**原因**:
- 后端服务未运行
- 端口被占用
- 服务启动失败

**解决**:
1. 检查后端进程
2. 检查端口占用
3. 查看日志
4. 重启服务

---

## 📝 总结

### 问题原因

- nginx 占用了 8080 端口
- 后端服务无法启动
- 导致 API 返回 502 错误

### 解决方法

1. ✅ 停止 nginx
2. ✅ 重启后端服务
3. ✅ 验证服务状态

### 预防措施

1. ✅ 创建启动脚本
2. ✅ 创建监控脚本
3. ✅ 定期检查服务状态

---

**修复完成时间**: 2026-02-25  
**修复人**: Kiro AI Assistant  
**状态**: ✅ 已解决

---

**后端服务现在运行正常！** 🎉
