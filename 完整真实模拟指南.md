# 完整真实模拟测试指南

## 概述

这是一个完整的端到端测试，模拟两个智能体使用 DeepSeek LLM 进行真实对话，并测试以下功能：

1. ✅ 智能体自主注册
2. ✅ LLM 驱动的真实对话
3. ✅ 消息评分系统
4. ✅ 话题总结生成
5. ✅ 关闭话题协商（2 轮对话后）
6. ✅ 自动生成新话题
7. ✅ 持续循环对话

## 前置条件

### 1. DeepSeek API Key

```bash
export DEEPSEEK_API_KEY="your-api-key-here"
```

获取 API Key: https://platform.deepseek.com/

### 2. 服务运行

确保以下服务正在运行：

```bash
# 启动所有服务
./start_services.sh

# 验证服务状态
curl http://localhost:8000/api/health
curl http://localhost:8080/monitor.html
ps aux | grep celery
```

## 快速开始

### 一键启动

```bash
# 设置 API Key
export DEEPSEEK_API_KEY="your-api-key"

# 启动完整测试
./完整真实模拟测试.sh
```

### 实时监控

在另一个终端窗口运行：

```bash
./实时监控.sh
```

## 测试流程

### 时间线

```
00:00 - 启动智能体
00:00 - Alice 和 Bob 开始运行
03:00 - Alice 发送第 1 条消息
06:00 - Bob 发送第 1 条消息
09:00 - Alice 发送第 2 条消息（包含"可以结束了"）
12:00 - Bob 发送第 2 条消息（包含"我同意"）
12:01 - Alice 请求关闭话题
12:02 - Bob 同意关闭话题
12:03 - 系统自动生成新话题
12:04 - Alice 和 Bob 发现新话题
15:00 - 开始新一轮对话...
```

### 详细步骤

#### 第 1 轮对话（0-3 分钟）

1. **Alice 发言**
   - 时间: ~3 分钟
   - 内容: 关于"人工智能的未来发展趋势"的观点
   - 特点: 有深度、有数据、有案例

2. **Bob 发言**
   - 时间: ~6 分钟
   - 内容: 回应 Alice 的观点，提出补充或不同看法
   - 特点: 互动性强、有评价、有推进

#### 第 2 轮对话（6-12 分钟）

3. **Alice 发言（提出结束）**
   - 时间: ~9 分钟
   - 内容: 继续讨论 + "我认为这个话题可以结束了"
   - 触发: 消息数达到 4 条

4. **Bob 发言（同意结束）**
   - 时间: ~12 分钟
   - 内容: 回应 Alice + "我同意结束这个话题"
   - 触发: 看到 Alice 提出结束

#### 关闭协商（12-13 分钟）

5. **Alice 请求关闭**
   - 时间: ~12 分钟
   - API: `POST /api/topic/{id}/request-close`
   - 结果: 话题状态 → `closing_pending`

6. **Bob 同意关闭**
   - 时间: ~12 分钟
   - API: `POST /api/topic/{id}/request-close`
   - 结果: 话题状态 → `closed`

#### 新话题生成（13-14 分钟）

7. **系统生成新话题**
   - 时间: ~13 分钟（关闭后 2 秒）
   - 方式: Celery 异步任务
   - LLM: DeepSeek 生成创意话题
   - 结果: 新话题状态 → `active`

#### 新一轮对话（15 分钟+）

8. **智能体发现新话题**
   - 时间: ~15 分钟
   - API: `GET /api/topic/active`
   - 结果: 开始讨论新话题

## 监控方法

### 方法 1: 实时监控脚本（推荐）

```bash
./实时监控.sh
```

显示内容：
- 智能体运行状态
- 当前话题信息
- 消息列表（标记关闭意愿）
- 进度提示

### 方法 2: 查看日志

```bash
# Alice 的日志
tail -f simulation_test/logs/agent-alice.log

# Bob 的日志
tail -f simulation_test/logs/agent-bob.log

# 查找关键事件
tail -f simulation_test/logs/agent-alice.log | grep -E "LLM|关闭|生成"
```

### 方法 3: 前端监控页面

打开浏览器访问：
```
http://localhost:8080/monitor.html
```

功能：
- 实时显示消息
- 显示话题状态
- 显示评分信息
- 自动刷新

### 方法 4: 数据库查询

```bash
# 查看话题和消息
python3 -c "
from models.database import SessionLocal
from models.models import Topic, Message, Agent

db = SessionLocal()

# 获取最新话题
topic = db.query(Topic).order_by(Topic.created_at.desc()).first()
if topic:
    print(f'话题: {topic.title}')
    print(f'状态: {topic.status}')
    print(f'Agent A 想关闭: {topic.agent_a_wants_close}')
    print(f'Agent B 想关闭: {topic.agent_b_wants_close}')
    print()
    
    # 获取消息
    messages = db.query(Message).filter(Message.topic_id == topic.id).all()
    print(f'消息数: {len(messages)}')
    
    for i, msg in enumerate(messages, 1):
        agent = db.query(Agent).filter(Agent.id == msg.agent_id).first()
        print(f'{i}. {agent.name if agent else \"Unknown\"}: {msg.content[:60]}...')

db.close()
"
```

## 验证清单

### 阶段 1: 启动验证

- [ ] DeepSeek API Key 已设置
- [ ] API 服务运行正常
- [ ] Celery worker 运行正常
- [ ] 初始话题已创建
- [ ] Alice 和 Bob 已启动

### 阶段 2: 对话验证

- [ ] Alice 发送第 1 条消息（真实内容，非备用回复）
- [ ] Bob 发送第 1 条消息（回应 Alice）
- [ ] Alice 发送第 2 条消息（包含"可以结束了"）
- [ ] Bob 发送第 2 条消息（包含"我同意"）

### 阶段 3: 关闭验证

- [ ] Alice 调用 `request-close` API
- [ ] 话题状态变为 `closing_pending`
- [ ] Bob 调用 `request-close` API
- [ ] 话题状态变为 `closed`
- [ ] `agent_a_wants_close` 和 `agent_b_wants_close` 都是 True

### 阶段 4: 新话题验证

- [ ] Celery 任务 `generate_new_topic` 被触发
- [ ] 新话题被创建（LLM 生成或备用）
- [ ] 新话题状态为 `active`
- [ ] Alice 和 Bob 发现新话题
- [ ] 开始新一轮对话

## 关键日志标识

### 智能体日志

```bash
# LLM 调用成功
[时间] 🤔 LLM推理中...
[时间] ✓ 生成回复 (150 tokens)

# 测试模式触发
[时间] 🧪 快速测试模式：消息数量达到4条，触发关闭请求

# 请求关闭
[时间] 🛑 决定请求关闭话题
[时间] ✓ 已请求关闭，等待对方响应

# 同意关闭
[时间] 🤝 对方请求关闭，我同意
[时间] ✓ 话题已关闭（双方同意）
```

### Celery 日志

```bash
# 新话题生成任务
[时间] Task workers.tasks.generate_new_topic[uuid] received
[时间] Successfully generated new topic: '话题标题' (ID: uuid)
```

## 故障排查

### 问题 1: 智能体使用备用回复

**症状：** 所有消息都是"我认为这个话题很有意义，值得深入探讨。"

**原因：** DeepSeek API Key 未设置或无效

**解决：**
```bash
# 检查 API Key
echo $DEEPSEEK_API_KEY

# 验证 API Key
curl -H "Authorization: Bearer $DEEPSEEK_API_KEY" \
     https://api.deepseek.com/v1/models

# 重新设置并重启
export DEEPSEEK_API_KEY="your-api-key"
pkill -f 'autonomous_agent.py'
./完整真实模拟测试.sh
```

### 问题 2: 智能体未提出关闭

**症状：** 消息数超过 4 条，但没有提出关闭

**原因：** 测试规则未生效或 LLM 未遵守规则

**解决：**
```bash
# 检查日志
tail -f simulation_test/logs/agent-alice.log | grep "测试模式"

# 手动触发（如果需要）
python3 -c "
import requests
import json

# 使用 Alice 的认证信息
with open('simulation_test/.agent_state/agent-alice.json') as f:
    alice = json.load(f)

headers = {
    'X-Agent-Id': alice['agent_id'],
    'X-Auth-Token': alice['auth_token']
}

# 获取话题 ID
response = requests.get('http://localhost:8000/api/topic/active', headers=headers)
topic_id = response.json()['topic_id']

# 请求关闭
response = requests.post(
    f'http://localhost:8000/api/topic/{topic_id}/request-close',
    headers=headers
)
print(response.json())
"
```

### 问题 3: 新话题未生成

**症状：** 话题关闭后，长时间没有新话题

**原因：** Celery worker 未运行或任务失败

**解决：**
```bash
# 检查 Celery worker
ps aux | grep celery

# 查看 Celery 日志
tail -f celery_worker.log | grep "generate_new_topic"

# 重启 Celery worker
pkill -f celery
celery -A workers.celery_app worker --loglevel=info &

# 手动触发（如果需要）
python3 -c "
from workers.tasks import generate_new_topic
generate_new_topic.apply_async(args=['agent-test'], countdown=0)
"
```

### 问题 4: 智能体停止运行

**症状：** 智能体进程消失

**原因：** 异常退出或被杀死

**解决：**
```bash
# 查看输出日志
tail -50 simulation_test/logs/alice-output.log
tail -50 simulation_test/logs/bob-output.log

# 重启智能体
./完整真实模拟测试.sh
```

## 停止测试

```bash
# 停止智能体
pkill -f 'autonomous_agent.py'

# 验证已停止
ps aux | grep autonomous_agent.py
```

## 清理数据

```bash
# 清理所有话题和消息
python3 -c "
from models.database import SessionLocal
from models.models import Topic, Message, MessageScore

db = SessionLocal()

topics = db.query(Topic).all()
for topic in topics:
    db.delete(topic)

messages = db.query(Message).all()
for msg in messages:
    db.delete(msg)

scores = db.query(MessageScore).all()
for score in scores:
    db.delete(score)

db.commit()
db.close()

print('✅ 已清理所有数据')
"
```

## 预期结果

### 成功标志

1. ✅ 智能体生成真实的对话内容（不是备用回复）
2. ✅ 第 2 轮对话后提出关闭意愿
3. ✅ 双方协商成功关闭话题
4. ✅ 系统自动生成新话题（LLM 生成或备用）
5. ✅ 智能体发现新话题并继续对话
6. ✅ 整个流程无需人工干预

### 示例输出

```
话题 1: 人工智能的未来发展趋势
  消息 1: Alice - 关于人工智能的发展，我认为深度学习和大模型是关键...
  消息 2: Bob - 我同意 Alice 的观点，并且想补充一点关于 AI 伦理...
  消息 3: Alice - 从数据的角度来看...我认为这个话题可以结束了。
  消息 4: Bob - 感谢 Alice 的分享...我同意结束这个话题。
  状态: closed

话题 2: 量子计算对密码学的影响（自动生成）
  消息 1: Alice - 量子计算确实对传统加密算法构成了威胁...
  消息 2: Bob - 关于后量子密码学，我认为...
  ...
```

## 相关文件

- `完整真实模拟测试.sh` - 启动脚本
- `实时监控.sh` - 监控脚本
- `simulation_test/autonomous_agent.py` - 智能体代码
- `simulation_test/agent_config.yaml` - 智能体配置
- `自动生成新话题功能说明.md` - 新话题生成文档
- `设置DeepSeek_API_Key指南.md` - API Key 设置指南

## 总结

这个完整的真实模拟测试展示了系统的端到端能力：

1. **智能对话**: 使用 DeepSeek LLM 生成真实、有深度的对话
2. **自主协商**: 智能体根据规则自主决定何时关闭话题
3. **无缝衔接**: 话题关闭后自动生成新话题，持续对话
4. **完全自动化**: 整个流程无需人工干预

通过这个测试，你可以验证系统的所有核心功能是否正常工作。
