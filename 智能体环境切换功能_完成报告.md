# 智能体环境切换功能 - 完成报告

## 📋 任务概述

为自主智能体脚本添加环境切换功能，支持通过命令行参数在本地和服务器环境之间切换，无需修改配置文件。

---

## ✅ 完成内容

### 1. 修改 `autonomous_agent.py`

**新增参数**：
- `--env` 参数，支持 `local` 和 `server` 两个选项
- 默认值为 `local`（本地环境）

**功能实现**：
```python
parser.add_argument(
    "--env",
    choices=["local", "server"],
    default="local",
    help="运行环境：local=本地(localhost:8000), server=明宽服务器(129.211.28.211:8080)"
)
```

**环境切换逻辑**：
```python
# 根据环境参数设置 API base_url
if args.env == "server":
    config['api']['base_url'] = "http://129.211.28.211:8080"
    print(f"🌐 连接到明宽服务器: {config['api']['base_url']}")
else:
    config['api']['base_url'] = "http://localhost:8000"
    print(f"🏠 连接到本地服务: {config['api']['base_url']}")
```

---

### 2. 更新 `start_agents.sh`

**新增功能**：
- 支持 `--env` 参数传递给启动脚本
- 自动将环境参数传递给所有智能体
- 显示当前连接的环境信息

**使用示例**：
```bash
# 本地环境
./simulation_test/start_agents.sh --env local

# 服务器环境
./simulation_test/start_agents.sh --env server
```

**输出示例**：
```
==========================================
自主智能体启动
🌐 目标环境: 明宽服务器 (129.211.28.211:8080)
==========================================
```

---

### 3. 创建使用说明文档

**文件**：`simulation_test/环境切换使用说明.md`

**内容包括**：
- 功能说明
- 使用方法（单个智能体 + 批量启动）
- 参数说明
- 使用场景（本地开发、服务器测试、对比测试）
- 环境对比表
- 故障排查
- 完整测试流程示例

---

### 4. 创建测试脚本

**文件**：`simulation_test/test_env_switch.sh`

**功能**：
- 测试帮助信息显示
- 测试本地环境参数
- 测试服务器环境参数
- 验证功能正常工作

---

## 🎯 使用方法

### 单个智能体

#### 本地环境（默认）
```bash
# 方式1：不指定环境（默认 local）
python3 simulation_test/autonomous_agent.py --agent alice

# 方式2：明确指定 local
python3 simulation_test/autonomous_agent.py --agent alice --env local
```

#### 服务器环境
```bash
# 连接明宽服务器
python3 simulation_test/autonomous_agent.py --agent alice --env server
```

---

### 批量启动（推荐）

#### 本地环境
```bash
# 启动 Alice 和 Bob（连接本地）
./simulation_test/start_agents.sh --env local
```

#### 服务器环境
```bash
# 启动 Alice 和 Bob（连接服务器）
./simulation_test/start_agents.sh --env server
```

---

## 📊 环境对比

| 特性 | 本地环境 (local) | 服务器环境 (server) |
|------|-----------------|-------------------|
| **API地址** | http://localhost:8000 | http://129.211.28.211:8080 |
| **用途** | 开发测试 | 生产测试 |
| **数据** | 本地数据库 | 生产数据库 |
| **网络延迟** | 极低 | 取决于网络 |
| **适用场景** | 功能开发、调试 | 压力测试、演示 |

---

## 🔍 测试验证

### 测试1：帮助信息
```bash
$ python3 simulation_test/autonomous_agent.py --help

usage: autonomous_agent.py [-h] --agent AGENT [--config CONFIG] [--env {local,server}]

自主智能体模拟系统

optional arguments:
  --agent AGENT         智能体标识 (alice, bob, carol)
  --config CONFIG       配置文件路径
  --env {local,server}  运行环境：local=本地(localhost:8000),
                        server=明宽服务器(129.211.28.211:8080)
```

### 测试2：本地环境
```bash
$ python3 simulation_test/autonomous_agent.py --agent alice --env local

🏠 连接到本地服务: http://localhost:8000
==========================================
自主智能体启动: Agent-Alice
性格: analytical
描述: 注重数据和证据的分析型智能体
==========================================
```

### 测试3：服务器环境
```bash
$ python3 simulation_test/autonomous_agent.py --agent alice --env server

🌐 连接到明宽服务器: http://129.211.28.211:8080
==========================================
自主智能体启动: Agent-Alice
性格: analytical
描述: 注重数据和证据的分析型智能体
==========================================
```

---

## 💡 使用场景

### 场景1：本地开发测试
```bash
# 1. 启动本地服务
./start_services.sh

# 2. 启动智能体（连接本地）
./simulation_test/start_agents.sh --env local

# 3. 查看日志
tail -f simulation_test/logs/alice.log
```

### 场景2：服务器压力测试
```bash
# 直接连接服务器，无需启动本地服务
./simulation_test/start_agents.sh --env server

# 在浏览器查看监控页面
open http://129.211.28.211:8080/monitor.html
```

### 场景3：对比测试
```bash
# 终端1：本地环境
python3 simulation_test/autonomous_agent.py --agent alice --env local

# 终端2：服务器环境
python3 simulation_test/autonomous_agent.py --agent bob --env server
```

---

## 📁 修改的文件

1. **simulation_test/autonomous_agent.py**
   - 新增 `--env` 参数
   - 添加环境切换逻辑
   - 显示连接的环境信息

2. **simulation_test/start_agents.sh**
   - 支持 `--env` 参数
   - 传递环境参数给智能体
   - 显示环境信息

3. **simulation_test/环境切换使用说明.md** (新建)
   - 完整的使用说明
   - 场景示例
   - 故障排查

4. **simulation_test/test_env_switch.sh** (新建)
   - 功能测试脚本
   - 验证环境切换

---

## ⚠️ 注意事项

1. **环境变量**
   - 确保 `.env` 文件中配置了 `DEEPSEEK_API_KEY`
   - 智能体会自动加载环境变量

2. **服务器连接**
   - 连接服务器前，确保服务器服务正常运行
   - 可以先访问 http://129.211.28.211:8080/api/health 检查

3. **状态文件**
   - 智能体状态保存在 `simulation_test/.agent_state/`
   - 本地和服务器使用相同的状态文件
   - 如需重新注册，删除对应的状态文件

4. **并发测试**
   - 可以同时运行多个智能体连接不同环境
   - 注意不要使用相同的智能体标识

---

## 🎉 优势

### 1. 灵活性
- 无需修改配置文件
- 命令行参数即可切换
- 支持同时测试多个环境

### 2. 便捷性
- 一键切换本地/服务器
- 批量启动脚本支持
- 清晰的环境提示

### 3. 可维护性
- 配置集中管理
- 环境信息明确显示
- 易于调试和排查

---

## 📝 快速命令参考

```bash
# 查看帮助
python3 simulation_test/autonomous_agent.py --help

# 本地单个智能体
python3 simulation_test/autonomous_agent.py --agent alice --env local

# 服务器单个智能体
python3 simulation_test/autonomous_agent.py --agent alice --env server

# 本地批量启动
./simulation_test/start_agents.sh --env local

# 服务器批量启动
./simulation_test/start_agents.sh --env server

# 查看日志
tail -f simulation_test/logs/alice.log

# 停止所有智能体
pkill -f autonomous_agent

# 测试功能
./simulation_test/test_env_switch.sh
```

---

## 🔗 相关文档

- `simulation_test/环境切换使用说明.md` - 详细使用说明
- `simulation_test/README_AUTONOMOUS.md` - 自主智能体总体说明
- `simulation_test/AUTONOMOUS_AGENT_GUIDE.md` - 智能体开发指南
- `API_ENDPOINTS.md` - API 接口文档

---

**完成时间**: 2026-02-15 20:30 (UTC+8)
**功能状态**: ✅ 已完成并测试
**版本**: 1.0
