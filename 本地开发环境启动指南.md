# 本地开发环境启动指南

## 🚀 快速启动

### 一键启动所有服务

```bash
./start_services.sh
```

这个脚本会自动：
1. ✅ 检查并启动 Redis
2. ✅ 检查并启动 PostgreSQL
3. ✅ 激活 Python 虚拟环境
4. ✅ 安装依赖
5. ✅ 启动 FastAPI (端口 8000)
6. ✅ 启动 Celery Worker
7. ✅ 启动 Celery Beat
8. ✅ 配置并启动 Nginx (端口 8080)

### 停止所有服务

```bash
./stop_services.sh
```

## 📋 前置要求

### macOS

```bash
# 安装 Homebrew (如果未安装)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 安装依赖
brew install python@3.9 postgresql@14 redis nginx

# 启动数据库服务
brew services start postgresql@14
brew services start redis
```

### Ubuntu/Linux

```bash
# 更新包列表
sudo apt update

# 安装依赖
sudo apt install python3 python3-venv python3-pip postgresql redis-server nginx

# 启动服务
sudo systemctl start postgresql
sudo systemctl start redis
```

## 🌐 访问地址

启动成功后，可以访问以下地址：

| 服务 | 地址 | 说明 |
|------|------|------|
| 🌐 前端监控 (新版) | http://localhost:8080 | AI 科技风格监控界面 |
| 🌐 前端监控 (旧版) | http://localhost:8080/monitor.html | 原版监控界面 |
| 💬 聊天界面 (新版) | http://localhost:8080/index_v2.html | AI 科技风格聊天界面 |
| 💬 聊天界面 (旧版) | http://localhost:8080/index.html | 原版聊天界面 |
| 🔧 管理后台 | http://localhost:8080/admin.html | 管理员界面 |
| 📚 API 文档 | http://localhost:8080/docs | Swagger UI |
| 📖 API 文档 (ReDoc) | http://localhost:8080/redoc | ReDoc 风格 |

## 🏗️ 架构说明

### 端口配置

```
用户浏览器
    ↓
Nginx (8080) ← 前端入口
    ↓
    ├─→ 静态文件 (frontend/, static/)
    └─→ API 代理 → FastAPI (8000)
                        ↓
                    PostgreSQL (5432)
                    Redis (6379)
                    Celery Worker
                    Celery Beat
```

### 与生产环境一致

本地开发环境完全模拟明宽生产服务器的配置：

- ✅ 使用 8080 端口作为前端入口
- ✅ Nginx 反向代理到后端 8000 端口
- ✅ 相同的服务架构（FastAPI + Celery + Redis + PostgreSQL）
- ✅ 相同的 Nginx 配置规则

## 📊 服务管理

### 查看服务状态

```bash
# 查看所有进程
ps aux | grep -E "uvicorn|celery|nginx"

# 查看端口占用
lsof -i :8080  # Nginx
lsof -i :8000  # FastAPI
lsof -i :5432  # PostgreSQL
lsof -i :6379  # Redis
```

### 查看日志

```bash
# 实时查看 API 日志
tail -f logs/api.log

# 实时查看 Worker 日志
tail -f logs/worker.log

# 实时查看 Beat 日志
tail -f logs/beat.log

# 查看所有日志
tail -f logs/*.log
```

### 重启单个服务

```bash
# 重启 FastAPI
pkill -f "uvicorn main:app"
nohup uvicorn main:app --host 0.0.0.0 --port 8000 --reload > logs/api.log 2>&1 &

# 重启 Celery Worker
pkill -f "celery.*worker"
nohup celery -A workers.celery_app worker --loglevel=info > logs/worker.log 2>&1 &

# 重启 Nginx
nginx -s reload
```

## 🔧 配置文件

### Nginx 配置

启动脚本会自动生成 `nginx_local.conf`，配置内容：

- 监听 8080 端口
- 前端文件从 `frontend/` 目录提供
- API 请求代理到 `http://127.0.0.1:8000`
- 默认首页为 `monitor_v2.html` (新版监控界面)

### 环境变量

编辑 `.env` 文件配置：

```bash
# 数据库
DATABASE_URL=postgresql://dual_agent_user:dual_agent_pass@localhost:5432/dual_agent_chat

# Redis
REDIS_URL=redis://localhost:6379/0

# API 配置
API_HOST=0.0.0.0
API_PORT=8000

# LLM API Keys
OPENCLAW_API_KEY=your_key_here
DEEPSEEK_API_KEY=your_key_here
```

## 🐛 故障排查

### Nginx 启动失败

```bash
# 检查端口占用
lsof -i :8080

# 如果端口被占用，停止占用进程
kill -9 <PID>

# 或者修改 nginx_local.conf 中的端口号
```

### FastAPI 启动失败

```bash
# 查看详细错误
cat logs/api.log

# 常见问题：
# 1. 端口 8000 被占用
lsof -i :8000
kill -9 <PID>

# 2. 数据库连接失败
psql -h localhost -U dual_agent_user -d dual_agent_chat
```

### Celery 启动失败

```bash
# 查看 Worker 日志
cat logs/worker.log

# 查看 Beat 日志
cat logs/beat.log

# 常见问题：Redis 未启动
redis-cli ping
```

### 数据库问题

```bash
# 检查 PostgreSQL 状态
pg_isready -h localhost -p 5432

# 重置数据库
psql -h localhost -U dual_agent_user -d dual_agent_chat -f dual_agent_chat_data.sql
```

## 📦 数据库初始化

如果需要重新初始化数据库：

```bash
# 1. 删除并重建数据库
sudo -u postgres psql -c "DROP DATABASE IF EXISTS dual_agent_chat;"
sudo -u postgres psql -c "CREATE DATABASE dual_agent_chat OWNER dual_agent_user;"

# 2. 运行迁移
source venv/bin/activate
alembic upgrade head

# 3. 导入测试数据（如果有）
PGPASSWORD='dual_agent_pass' psql -h localhost -U dual_agent_user -d dual_agent_chat -f dual_agent_chat_data.sql
```

## 🎨 前端页面说明

### 新版 AI 科技风格页面

- `frontend/index_v2.html` - 主聊天界面
- `frontend/monitor_v2.html` - 监控界面

特点：
- 深色主题 + 霓虹光效
- 浮动毛玻璃导航栏
- 平滑动画和过渡
- 响应式设计
- 符合 WCAG AAA 无障碍标准

### 原版页面

- `frontend/index.html` - 原版聊天界面
- `frontend/monitor.html` - 原版监控界面
- `frontend/admin.html` - 管理后台

## 🚀 开发工作流

### 1. 启动开发环境

```bash
./start_services.sh
```

### 2. 修改代码

- 后端代码修改会自动重载（uvicorn --reload）
- 前端代码修改后刷新浏览器即可

### 3. 查看日志

```bash
tail -f logs/api.log
```

### 4. 测试 API

访问 http://localhost:8080/docs 使用 Swagger UI 测试

### 5. 停止服务

```bash
./stop_services.sh
```

## 📝 注意事项

1. **首次启动**：确保已安装所有依赖（PostgreSQL, Redis, Nginx）
2. **端口冲突**：如果 8080 或 8000 端口被占用，需要先停止占用进程
3. **数据库权限**：确保 PostgreSQL 用户 `dual_agent_user` 有正确的权限
4. **虚拟环境**：所有 Python 命令都在虚拟环境中执行
5. **日志文件**：定期清理 `logs/` 目录避免占用过多空间

## 🔗 相关文档

- [API 端点文档](API_ENDPOINTS.md)
- [前端页面使用说明](前端页面使用说明.md)
- [前端 AI 科技风格优化报告](前端AI科技风格优化报告.md)
- [快速开始指南](QUICKSTART.md)

## 💡 提示

- 使用新版 AI 科技风格界面获得更好的视觉体验
- 开发时建议开启多个终端窗口分别查看不同服务的日志
- 修改 Nginx 配置后需要重启 Nginx: `nginx -s reload`
