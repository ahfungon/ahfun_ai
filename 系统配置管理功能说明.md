# 系统配置管理功能说明

## 功能概述

系统配置管理功能允许管理员通过Web界面动态配置系统运行参数，无需重启服务即可生效。

## 已实现功能

### 1. 数据库模型 ✅
- 文件：`models/system_config.py`
- 支持的配置类型：text, number, textarea, password, select
- 支持配置分类、验证规则、默认值等

### 2. 配置服务 ✅
- 文件：`services/system_config_service.py`
- 功能：
  - 初始化默认配置
  - 获取单个/多个配置
  - 更新配置（单个/批量）
  - 重置配置到默认值
  - 导出/导入配置
  - 配置值验证

### 3. API端点 ✅
- 文件：`api/routes.py`
- 端点列表：
  - `GET /api/admin/config/system` - 获取所有配置（可按分类筛选）
  - `GET /api/admin/config/system/export` - 导出所有配置
  - `GET /api/admin/config/system/{key}` - 获取单个配置
  - `PUT /api/admin/config/system/{key}` - 更新单个配置
  - `POST /api/admin/config/system/batch` - 批量更新配置
  - `POST /api/admin/config/system/{key}/reset` - 重置配置到默认值

### 4. 前端管理页面 ✅
- 文件：`frontend/system-config.html`
- 功能：
  - 按分类展示配置项
  - 支持不同类型的输入控件（文本、数字、文本域、密码、下拉选择）
  - 单个保存和批量保存
  - 重置到默认值
  - 实时验证
  - 友好的用户界面

### 5. 数据库迁移 ✅
- 文件：`migrations/create_system_config_table.sql`
- 表：`system_configs`
- 已执行并初始化默认配置

### 6. 管理后台集成 ✅
- 文件：`frontend/admin.html`
- 在侧边栏添加"⚙️ 系统配置"入口
- 点击后跳转到独立配置页面

## 默认配置项

### 总结配置 (summary)
1. **summary_threshold** - 总结触发阈值
   - 类型：number
   - 默认值：8000
   - 验证：1000-50000
   - 说明：Token计数达到此阈值时触发总结任务

### Prompt模板 (prompts)
2. **scoring_prompt** - 消息评分Prompt
   - 类型：textarea
   - 说明：用于评估每条消息质量的Prompt模板
   - 包含评分标准和JSON格式要求

3. **summary_prompt** - 对话总结Prompt
   - 类型：textarea
   - 说明：用于生成对话总结和建议的Prompt模板
   - 包含评估标准和建议类型

### LLM配置 (llm)
4. **llm_provider_scoring** - 消息评分LLM
   - 类型：select
   - 选项：DeepSeek, MiniMax
   - 默认值：deepseek

5. **llm_provider_summary** - 对话总结LLM
   - 类型：select
   - 选项：DeepSeek, MiniMax
   - 默认值：deepseek

6. **deepseek_api_key** - DeepSeek API Key
   - 类型：password
   - 说明：DeepSeek API密钥

7. **deepseek_api_url** - DeepSeek API URL
   - 类型：text
   - 默认值：https://api.deepseek.com/v1

8. **deepseek_model** - DeepSeek模型
   - 类型：text
   - 默认值：deepseek-chat

9. **minimax_api_key** - MiniMax API Key
   - 类型：password
   - 说明：MiniMax API密钥

10. **minimax_api_url** - MiniMax API URL
    - 类型：text
    - 默认值：https://api.minimax.chat/v1

11. **minimax_model** - MiniMax模型
    - 类型：text
    - 默认值：abab6.5-chat

## 使用方法

### 访问配置页面
1. 打开管理后台：http://localhost:8080/admin.html
2. 点击侧边栏"⚙️ 系统配置"
3. 或直接访问：http://localhost:8080/system-config.html

### 修改配置
1. 在配置页面找到要修改的配置项
2. 修改配置值
3. 点击"保存"按钮（单个保存）或"保存所有配置"（批量保存）
4. 配置立即生效，无需重启服务

### 重置配置
1. 点击配置项旁边的"重置"按钮
2. 配置将恢复到默认值

### API调用示例

#### 获取所有配置
```bash
curl http://localhost:8080/api/admin/config/system
```

#### 更新单个配置
```bash
curl -X PUT http://localhost:8080/api/admin/config/system/summary_threshold \
  -H "Content-Type: application/json" \
  -d '{"value": "10000"}'
```

#### 批量更新配置
```bash
curl -X POST http://localhost:8080/api/admin/config/system/batch \
  -H "Content-Type: application/json" \
  -d '{"configs": {"summary_threshold": "9000", "llm_provider_scoring": "deepseek"}}'
```

#### 重置配置
```bash
curl -X POST http://localhost:8080/api/admin/config/system/summary_threshold/reset
```

#### 导出配置
```bash
curl http://localhost:8080/api/admin/config/system/export
```

## 测试

运行测试脚本验证功能：
```bash
python3 test_system_config.py
```

测试覆盖：
- ✅ 获取所有配置
- ✅ 按分类获取配置
- ✅ 获取单个配置
- ✅ 更新配置
- ✅ 批量更新
- ✅ 重置配置
- ✅ 配置验证
- ✅ 前端页面访问
- ✅ 导出配置

## 技术特点

1. **动态生效**：配置修改后立即生效，无需重启服务
2. **类型安全**：支持多种配置类型，自动类型转换和验证
3. **分类管理**：配置按功能分类，便于管理
4. **默认值**：所有配置都有默认值，支持一键重置
5. **验证规则**：数字类型支持最小值/最大值验证
6. **友好界面**：直观的Web界面，支持不同类型的输入控件
7. **批量操作**：支持批量更新配置，提高效率
8. **导出功能**：支持导出所有配置，便于备份和迁移

## 下一步计划

### 集成到业务逻辑
1. **消息评分服务** - 使用配置的Prompt和LLM提供商
   - 文件：`services/message_scoring_service.py`
   - 读取：`scoring_prompt`, `llm_provider_scoring`

2. **对话总结服务** - 使用配置的阈值、Prompt和LLM提供商
   - 文件：`services/summary_service.py`
   - 读取：`summary_threshold`, `summary_prompt`, `llm_provider_summary`

3. **LLM客户端** - 根据配置选择不同的LLM提供商
   - 文件：`services/llm_clients/`
   - 读取：`deepseek_*`, `minimax_*`

### 实现步骤
```python
# 在服务中使用配置
from services.system_config_service import SystemConfigService

config_service = SystemConfigService(db)

# 获取配置值
threshold = config_service.get_config_value('summary_threshold', default=8000)
prompt = config_service.get_config_value('scoring_prompt')
provider = config_service.get_config_value('llm_provider_scoring', default='deepseek')
```

## 文件清单

- ✅ `models/system_config.py` - 配置数据模型
- ✅ `services/system_config_service.py` - 配置服务
- ✅ `migrations/create_system_config_table.sql` - 数据库迁移
- ✅ `init_system_config.py` - 初始化脚本
- ✅ `api/routes.py` - API端点（新增配置相关端点）
- ✅ `frontend/system-config.html` - 配置管理页面
- ✅ `frontend/admin.html` - 管理后台（添加配置入口）
- ✅ `test_system_config.py` - 测试脚本
- ✅ `系统配置管理功能说明.md` - 本文档

## 状态

✅ **已完成** - 所有功能已实现并测试通过

测试结果：9/9 tests passed 🎉
