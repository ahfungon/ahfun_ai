# 测试多 LLM 支持指南

## 前提条件

确保在系统配置中已设置：
- DeepSeek API Key
- MiniMax API Key

## 测试步骤

### 1. 打开管理后台

访问：http://localhost:8080/admin.html

### 2. 进入智能体模拟器

点击左侧菜单的 "🧪 智能体模拟器"

### 3. 添加第一个智能体（DeepSeek）

1. 点击 "➕ 添加智能体" 按钮
2. 填写信息：
   - 智能体名称：`DeepSeek Agent`
   - 发言模式：选择 `DeepSeek 调用`
   - 发言间隔：`5` 秒
3. 点击 "添加" 按钮

### 4. 添加第二个智能体（MiniMax）

1. 再次点击 "➕ 添加智能体" 按钮
2. 填写信息：
   - 智能体名称：`MiniMax Agent`
   - 发言模式：选择 `MiniMax 调用`
   - 发言间隔：`5` 秒
3. 点击 "添加" 按钮

### 5. 启动智能体

点击 "▶️ 全部启动" 按钮

### 6. 观察日志

在右侧的 "📝 运行日志" 区域，你应该看到：

#### 成功的日志示例：

```
[18:30:00] ✅ DEEPSEEK Agent 注册成功: agent-abc123
[18:30:00] ✅ DEEPSEEK Agent 启动成功
[18:30:01] ✅ MINIMAX Agent 注册成功: agent-def456
[18:30:01] ✅ MINIMAX Agent 启动成功
[18:30:05] 💬 DEEPSEEK Agent 发送消息: 关于"当前话题"，我认为...
[18:30:05] ✅ DEEPSEEK Agent 消息发送成功
[18:30:06] 💬 MINIMAX Agent 发送消息: 从技术角度来看...
[18:30:06] ✅ MINIMAX Agent 消息发送成功
```

#### 如果看到错误：

```
⚠️ DEEPSEEK 调用失败，使用模板模式: 401 Authentication Fails
⚠️ MINIMAX 调用失败，使用模板模式: Failed to fetch
```

说明配置有问题，请检查：
1. 系统配置中的 API Key 是否正确
2. API Key 是否有效（未过期、有余额）
3. 网络连接是否正常

### 7. 验证 API 调用

打开浏览器开发者工具（F12），切换到 Network 标签：

1. 找到对 `https://api.deepseek.com/v1/chat/completions` 的请求
2. 找到对 `https://api.minimax.chat/v1/text/chatcompletion_v2` 的请求
3. 检查响应状态码是否为 200

### 8. 检查统计信息

在模拟器页面顶部，你应该看到：

- 总智能体：2
- 运行中：2
- 总消息数：逐渐增加
- 成功率：接近 100%

## 常见问题

### Q1: DeepSeek 调用成功，但 MiniMax 失败

**原因**：MiniMax API Key 未配置或无效

**解决方案**：
1. 前往 "⚙️ 系统配置" 页面
2. 检查 MiniMax API Key 是否正确
3. 如果未配置，添加 MiniMax API Key
4. 刷新模拟器页面

### Q2: 两个都失败，显示 401 错误

**原因**：API Key 无效或过期

**解决方案**：
1. 检查 API Key 是否正确复制（没有多余空格）
2. 检查 API Key 是否过期
3. 检查账户余额是否充足
4. 重新生成 API Key

### Q3: 显示 "Failed to fetch"

**原因**：网络连接问题或 CORS 问题

**解决方案**：
1. 检查网络连接
2. 检查防火墙设置
3. 尝试使用 VPN（如果 API 服务在国外）

### Q4: 智能体一直使用模板模式

**原因**：LLM 调用失败后自动降级到模板模式

**解决方案**：
1. 查看日志中的具体错误信息
2. 根据错误信息排查问题
3. 修复问题后，停止并重新启动智能体

## 验证成功的标志

✅ 两个智能体都成功注册
✅ 两个智能体都成功启动
✅ 日志中没有 "调用失败" 的警告
✅ 消息内容是 LLM 生成的（不是模板）
✅ 成功率接近 100%
✅ Network 标签中可以看到对两个 API 的成功请求

## 下一步

测试成功后，你可以：

1. 调整发言间隔，测试不同的对话节奏
2. 添加更多智能体，测试并发性能
3. 观察不同 LLM 的回复风格差异
4. 监控 API 调用的响应时间和成功率
