# 服务器与本地差异对比报告

生成时间: 2026-02-25

## 一、数据库对比

### 服务器数据库 (129.211.28.211)
```
表数量: 8 个
- agents
- alembic_version
- audit_logs
- message_relevance_scores ✓ (已存在)
- messages
- summary_history
- summary_jobs
- topics (包含 topic_description 字段 ✓)
```

### 本地数据库
```
表数量: 9 个
- agents
- alembic_version
- audit_logs
- message_relevance_scores ✓
- messages
- summary_history
- summary_jobs
- system_configs ⚠️ (服务器缺失)
- topics (包含 topic_description 字段 ✓)
```

### 关键发现

#### ✅ 已完成的迁移
1. **message_relevance_scores 表** - 服务器已存在
2. **topics.topic_description 字段** - 服务器已存在

#### ⚠️ 需要执行的迁移
1. **system_configs 表** - 服务器缺失，需要创建

### 修正后的迁移方案

原评估报告中提到需要执行两个迁移：
- ❌ `migrations/add_message_relevance_scoring.sql` - **不需要执行**（已存在）
- ✅ `migrations/create_system_config_table.sql` - **需要执行**（缺失）

## 二、代码版本对比

### 服务器代码版本
```
当前 commit: 3f492b4 "。。"
提交时间: 较早（约94个提交之前）
```

### 本地代码版本
```
当前 commit: 0387760 "添加明宽服务器发布准备评估报告"
提交时间: 最新
```

### 版本差距
- **落后提交数**: 94 个 commits
- **主要更新内容**:
  - 总结监控功能
  - 系统配置管理功能
  - MiniMax LLM 集成
  - Worker 管理功能
  - 前端优化和多项功能增强

## 三、修正后的部署方案

### 步骤 1: 备份（必需）
```bash
# 1.1 备份数据库
ssh mingkuan "PGPASSWORD=dual_agent_pass /usr/local/pgsql/src/bin/psql/psql -h localhost -U dual_agent_user -d dual_agent_chat -c '\copy (SELECT * FROM topics) TO /tmp/topics_backup.csv CSV HEADER'"
ssh mingkuan "PGPASSWORD=dual_agent_pass /usr/local/pgsql/src/bin/psql/psql -h localhost -U dual_agent_user -d dual_agent_chat -c '\copy (SELECT * FROM messages) TO /tmp/messages_backup.csv CSV HEADER'"
ssh mingkuan "PGPASSWORD=dual_agent_pass /usr/local/pgsql/src/bin/psql/psql -h localhost -U dual_agent_user -d dual_agent_chat -c '\copy (SELECT * FROM agents) TO /tmp/agents_backup.csv CSV HEADER'"

# 1.2 备份代码
ssh mingkuan "cd /home/ubuntu/dual-agent-chat && tar -czf /tmp/dual-agent-chat-backup-$(date +%Y%m%d-%H%M%S).tar.gz ."
```

### 步骤 2: 停止服务
```bash
ssh mingkuan "cd /home/ubuntu/dual-agent-chat && pkill -f 'celery.*worker' && pkill -f 'uvicorn.*main:app'"
```

### 步骤 3: 更新代码
```bash
ssh mingkuan "cd /home/ubuntu/dual-agent-chat && git fetch origin && git reset --hard origin/main"
```

### 步骤 4: 执行数据库迁移（仅1个）
```bash
# 只需要执行 system_configs 表创建
ssh mingkuan "cd /home/ubuntu/dual-agent-chat && python3 -c '
from models.database import engine
from sqlalchemy import text

with open(\"migrations/create_system_config_table.sql\", \"r\", encoding=\"utf-8\") as f:
    sql = f.read()

with engine.begin() as conn:
    conn.execute(text(sql))
    print(\"✓ system_configs 表创建成功\")
'"

# 初始化系统配置数据
ssh mingkuan "cd /home/ubuntu/dual-agent-chat && python3 init_system_config.py"
```

### 步骤 5: 检查依赖
```bash
# 检查 Python 依赖
ssh mingkuan "cd /home/ubuntu/dual-agent-chat && pip3 list | grep -E 'sqlalchemy|celery|fastapi|redis'"
```

### 步骤 6: 重启服务
```bash
# 6.1 启动 Celery Worker
ssh mingkuan "cd /home/ubuntu/dual-agent-chat && nohup celery -A tasks.celery_app worker --loglevel=info > logs/celery_worker.log 2>&1 &"

# 6.2 启动 API 服务器
ssh mingkuan "cd /home/ubuntu/dual-agent-chat && nohup uvicorn main:app --host 0.0.0.0 --port 8080 > logs/api_server.log 2>&1 &"
```

### 步骤 7: 验证部署
```bash
# 7.1 检查进程
ssh mingkuan "ps aux | grep -E 'celery|uvicorn' | grep -v grep"

# 7.2 检查数据库表
ssh mingkuan "PGPASSWORD=dual_agent_pass /usr/local/pgsql/src/bin/psql/psql -h localhost -U dual_agent_user -d dual_agent_chat -c '\dt'"

# 7.3 检查 system_configs 表数据
ssh mingkuan "PGPASSWORD=dual_agent_pass /usr/local/pgsql/src/bin/psql/psql -h localhost -U dual_agent_user -d dual_agent_chat -c 'SELECT key, category, display_name FROM system_configs ORDER BY category, display_order'"

# 7.4 测试 API
ssh mingkuan "curl -s http://localhost:8080/health | python3 -m json.tool"
```

## 四、风险评估

### 低风险 ✅
- message_relevance_scores 表已存在，无需迁移
- topics.topic_description 字段已存在，无需迁移
- 只需创建 system_configs 表，不涉及数据修改

### 中等风险 ⚠️
- 代码版本落后94个提交，跨度较大
- 需要确认服务器 Python 依赖是否完整

### 建议
1. 在非高峰时段执行部署
2. 保留完整备份至少7天
3. 部署后密切监控日志
4. 准备回滚方案（恢复备份 + git reset）

## 五、回滚方案

如果部署后出现问题：

```bash
# 1. 停止服务
ssh mingkuan "cd /home/ubuntu/dual-agent-chat && pkill -f 'celery.*worker' && pkill -f 'uvicorn.*main:app'"

# 2. 恢复代码
ssh mingkuan "cd /home/ubuntu/dual-agent-chat && git reset --hard 3f492b4"

# 3. 删除 system_configs 表（如果需要）
ssh mingkuan "PGPASSWORD=dual_agent_pass /usr/local/pgsql/src/bin/psql/psql -h localhost -U dual_agent_user -d dual_agent_chat -c 'DROP TABLE IF EXISTS system_configs CASCADE'"

# 4. 重启服务
ssh mingkuan "cd /home/ubuntu/dual-agent-chat && nohup celery -A tasks.celery_app worker --loglevel=info > logs/celery_worker.log 2>&1 &"
ssh mingkuan "cd /home/ubuntu/dual-agent-chat && nohup uvicorn main:app --host 0.0.0.0 --port 8080 > logs/api_server.log 2>&1 &"
```

## 六、总结

### 原评估报告的修正
- ✅ 评估方法正确：通过对比数据库表结构和代码版本
- ⚠️ 迁移数量修正：从2个减少到1个
- ✅ 部署流程正确：备份 → 停止 → 更新 → 迁移 → 重启 → 验证

### 最终确认
- **需要执行的迁移**: 1个（create_system_config_table.sql）
- **代码更新**: 94个提交（3f492b4 → 0387760）
- **风险等级**: 中低风险
- **建议执行**: 是，方案可行

---

**准备就绪，等待执行确认。**
