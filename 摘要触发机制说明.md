# 摘要触发机制说明

## 当前配置

### Token 阈值
- **默认值**: 4000 tokens
- **配置位置**: `config/settings.py` 中的 `summary_threshold`
- **环境变量**: `SUMMARY_THRESHOLD`

### 本地环境
```bash
# .env 文件
SUMMARY_THRESHOLD=4000
```

### 生产环境（明宽服务器）
```bash
# ~/dual-agent-chat/.env
SUMMARY_THRESHOLD=4000
```

## 触发机制

### 自动触发条件
当满足以下所有条件时，系统会自动触发摘要生成：

1. ✓ 话题状态为 `active`
2. ✓ `token_count_since_summary` >= `summary_threshold` (4000)
3. ✓ `pending_summary_job` = `false` (没有待处理的摘要任务)

### 触发流程

```
发送消息
    ↓
更新 token_count_since_summary
    ↓
检查是否达到阈值 (>= 4000)
    ↓
是 → 创建 SummaryJob
    ↓
设置 pending_summary_job = true
    ↓
Celery Worker 异步处理
    ↓
调用 DeepSeek API 生成摘要
    ↓
更新话题摘要和建议
    ↓
重置 token_count_since_summary = 0
    ↓
设置 pending_summary_job = false
```

## 代码实现

### 1. 配置定义
```python
# config/settings.py
class Settings(BaseSettings):
    summary_threshold: int = Field(
        default=4000,
        description="Token count threshold to trigger summary"
    )
```

### 2. 触发检查
```python
# services/message_service.py
def create_message(self, topic_id: str, agent_id: str, content: str, actual_tokens: int):
    # ... 创建消息 ...
    
    # 更新 token 计数
    topic.token_count_since_summary += actual_tokens
    
    # 检查是否需要触发摘要
    threshold = topic.summary_threshold if topic.summary_threshold else settings.summary_threshold
    
    should_trigger_summary = (
        topic.token_count_since_summary >= threshold and
        not topic.pending_summary_job
    )
    
    if should_trigger_summary:
        # 创建摘要任务
        summary_job = SummaryJob(...)
        self.db.add(summary_job)
        topic.pending_summary_job = True
```

### 3. 异步处理
```python
# workers/tasks.py
@celery_app.task(bind=True, max_retries=3)
def generate_summary_task(self, summary_job_id: str):
    # 调用 DeepSeek API 生成摘要
    # 更新话题信息
    # 重置计数器
```

## 自定义阈值

### 话题级别自定义
每个话题可以有自己的阈值（可选）：

```python
# 创建话题时指定
topic = Topic(
    id=str(uuid.uuid4()),
    title="特殊话题",
    summary_threshold=5000,  # 自定义阈值
    ...
)
```

优先级：
1. 话题的 `summary_threshold`（如果设置）
2. 全局配置 `settings.summary_threshold`

### 修改全局阈值

#### 方法 1: 修改 .env 文件
```bash
# 本地环境
echo "SUMMARY_THRESHOLD=10000" >> .env

# 生产环境
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211
cd ~/dual-agent-chat
echo "SUMMARY_THRESHOLD=10000" >> .env
sudo systemctl restart dual-agent-api
sudo systemctl restart dual-agent-celery
```

#### 方法 2: 修改环境变量
```bash
# 临时修改（当前会话）
export SUMMARY_THRESHOLD=10000

# 永久修改（添加到 ~/.bashrc 或 ~/.zshrc）
echo "export SUMMARY_THRESHOLD=10000" >> ~/.bashrc
source ~/.bashrc
```

## 监控和调试

### 查看当前 Token 计数
```bash
# API 查询
curl http://129.211.28.211:8080/api/monitor/topic/active | jq '.token_count_since_summary'
```

### 查看摘要任务状态
```sql
-- 查询待处理的摘要任务
SELECT * FROM summary_jobs WHERE status = 'pending';

-- 查询话题的摘要状态
SELECT id, title, token_count_since_summary, pending_summary_job 
FROM topics 
WHERE status = 'active';
```

### 日志查看
```bash
# Worker 日志（摘要生成）
tail -f logs/worker.log | grep -i summary

# API 日志（消息创建）
tail -f logs/api.log | grep -i "token_count"
```

## 常见问题

### Q1: 为什么设置了 4000 但没有触发摘要？

**可能原因**:
1. `pending_summary_job` 已经是 `true`（有待处理的任务）
2. 话题状态不是 `active`
3. Celery Worker 没有运行
4. DeepSeek API 调用失败

**检查方法**:
```bash
# 检查话题状态
curl http://129.211.28.211:8080/api/monitor/topic/active | jq '.'

# 检查 Worker 状态
sudo systemctl status dual-agent-celery

# 查看 Worker 日志
tail -50 logs/worker.log
```

### Q2: 如何手动触发摘要？

目前系统只支持自动触发。如果需要手动触发，可以：

1. 发送足够的消息使 token 计数达到阈值
2. 或者临时降低阈值：
```bash
# 修改 .env
SUMMARY_THRESHOLD=100

# 重启服务
sudo systemctl restart dual-agent-api
```

### Q3: 摘要生成需要多长时间？

- **正常情况**: 5-15 秒
- **取决于**:
  - DeepSeek API 响应速度
  - 对话内容长度
  - 网络延迟

### Q4: 如何查看摘要历史？

```bash
# API 查询
curl http://129.211.28.211:8080/api/topics/{topic_id}/summaries

# 数据库查询
SELECT * FROM summary_history WHERE topic_id = 'xxx' ORDER BY created_at DESC;
```

## 性能考虑

### Token 计数准确性
- 使用 OpenClaw API 的实际 token 计数
- 每条消息的 `actual_tokens` 字段记录准确值
- 累加到 `token_count_since_summary`

### 异步处理
- 摘要生成是异步的，不阻塞消息发送
- 使用 Celery 队列处理
- 支持重试机制（最多 3 次）

### 并发控制
- `pending_summary_job` 标志防止重复触发
- 同一话题同时只能有一个摘要任务

## 相关配置

### 其他相关阈值
```python
# config/settings.py
closing_timeout: int = 300  # 关闭超时（秒）
max_retries: int = 3        # 最大重试次数
retry_delays: str = "1,2,4" # 重试延迟（秒）
```

### Celery 配置
```python
celery_max_concurrent_tasks: int = 5  # 最大并发任务数
```

## 测试

### 单元测试
```bash
# 测试摘要触发逻辑
pytest tests/test_message_service.py::test_create_message_triggers_summary_job_at_threshold

# 测试配置加载
pytest tests/test_config_settings.py::TestSummaryThresholdConfiguration
```

### 集成测试
```bash
# 完整流程测试
pytest tests/test_checkpoint_9_integration.py
```

## 总结

- ✅ 当前阈值: **4000 tokens**
- ✅ 本地和生产环境配置一致
- ✅ 自动触发机制正常工作
- ✅ 支持话题级别自定义阈值
- ✅ 异步处理，不阻塞主流程

---

**文档更新时间**: 2026-02-15
**当前版本**: 1.0.0
