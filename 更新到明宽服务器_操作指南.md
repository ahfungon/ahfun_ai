# 更新到明宽服务器 - 操作指南

## 部署策略

本次更新采用**保留数据**的部署策略：
1. 同步代码到服务器
2. 保留服务器上现有的数据库
3. 自动备份数据库
4. 重启服务

## 前提条件

### 1. 本地准备
- ✅ 代码已提交到Git
- ✅ 本地测试通过
- ✅ `.env` 文件已配置（可选更新到服务器）

### 2. 服务器要求
- SSH密钥: `~/.ssh/mingkuan.pem`
- 服务器IP: `129.211.28.211`
- 用户: `ubuntu`
- 项目目录: `~/dual-agent-chat`

## 部署步骤

### 方式一：使用自动部署脚本（推荐）

```bash
./deploy_update_to_mingkuan.sh
```

脚本会自动执行：
1. 同步代码（排除venv、logs等）
2. 询问是否更新 `.env` 文件
3. 在服务器上：
   - 更新Python依赖
   - 备份当前数据库
   - 重启所有服务
4. 验证部署结果

### 方式二：手动部署

#### 步骤1：同步代码
```bash
rsync -avz --progress \
    --exclude 'venv' \
    --exclude '.git' \
    --exclude '__pycache__' \
    --exclude '.hypothesis' \
    --exclude '*.pyc' \
    --exclude '.pytest_cache' \
    --exclude 'logs' \
    --exclude '.env' \
    -e "ssh -i ~/.ssh/mingkuan.pem" \
    ./ ubuntu@129.211.28.211:~/dual-agent-chat/
```

#### 步骤2：SSH登录服务器
```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211
cd ~/dual-agent-chat
```

#### 步骤3：备份数据库
```bash
PGPASSWORD='dual_agent_pass' pg_dump -h localhost -U dual_agent_user -d dual_agent_chat \
    --clean --if-exists \
    -f backup_$(date +%Y%m%d_%H%M%S).sql
```

#### 步骤4：更新依赖
```bash
source venv/bin/activate
pip install -r requirements.txt
```

#### 步骤5：重启服务
```bash
sudo systemctl restart dual-agent-api
sudo systemctl restart dual-agent-celery
sudo systemctl restart dual-agent-celery-beat
sudo systemctl restart nginx
```

#### 步骤6：检查服务状态
```bash
sudo systemctl status dual-agent-api
sudo systemctl status dual-agent-celery
sudo systemctl status dual-agent-celery-beat
```

## 重要配置检查

### 1. DeepSeek API Key
确保服务器上的 `.env` 文件包含正确的API Key：
```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211
cd ~/dual-agent-chat
grep DEEPSEEK_API_KEY .env
```

应该看到：
```
DEEPSEEK_API_KEY=sk-your-actual-key-here
```

### 2. 数据库连接
检查数据库配置：
```bash
grep DATABASE_URL .env
```

应该看到：
```
DATABASE_URL=postgresql://dual_agent_user:dual_agent_pass@localhost:5432/dual_agent_chat
```

### 3. Redis连接
检查Redis是否运行：
```bash
redis-cli ping
```

应该返回：`PONG`

## 验证部署

### 1. 检查API健康状态
```bash
curl http://129.211.28.211:8080/api/health
```

应该返回所有服务状态为 `healthy`

### 2. 检查前端页面
在浏览器中访问：
- 监控页面: http://129.211.28.211:8080/monitor.html
- 聊天页面: http://129.211.28.211:8080/index.html
- API文档: http://129.211.28.211:8080/docs

### 3. 检查Celery服务
```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211
cd ~/dual-agent-chat
tail -f logs/worker.log
```

应该看到任务执行日志

### 4. 测试消息评分
发送一条测试消息，等待30秒，检查是否有评分：
```bash
curl http://129.211.28.211:8080/api/monitor/topic/active
```

## 回滚方案

如果部署出现问题，可以快速回滚：

### 1. 恢复数据库
```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211
cd ~/dual-agent-chat

# 查看备份文件
ls -la backup_*.sql

# 恢复最新备份
PGPASSWORD='dual_agent_pass' psql -h localhost -U dual_agent_user -d dual_agent_chat \
    -f backup_YYYYMMDD_HHMMSS.sql
```

### 2. 回滚代码
```bash
# 在服务器上
cd ~/dual-agent-chat
git checkout <previous-commit-hash>

# 重启服务
sudo systemctl restart dual-agent-api
sudo systemctl restart dual-agent-celery
sudo systemctl restart dual-agent-celery-beat
```

## 常见问题

### Q: 服务启动失败
A: 检查日志文件
```bash
tail -f ~/dual-agent-chat/logs/api.log
tail -f ~/dual-agent-chat/logs/worker.log
```

### Q: Celery Worker未运行
A: 手动启动Worker
```bash
sudo systemctl start dual-agent-celery
sudo systemctl status dual-agent-celery
```

### Q: 数据库连接失败
A: 检查PostgreSQL服务
```bash
sudo systemctl status postgresql
sudo systemctl start postgresql
```

### Q: .env文件配置错误
A: 编辑配置文件
```bash
nano ~/dual-agent-chat/.env
# 修改后重启服务
sudo systemctl restart dual-agent-api
```

## 监控和维护

### 查看实时日志
```bash
# API日志
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'tail -f ~/dual-agent-chat/logs/api.log'

# Worker日志
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'tail -f ~/dual-agent-chat/logs/worker.log'

# Beat日志
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 'tail -f ~/dual-agent-chat/logs/beat.log'
```

### 检查服务状态
```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 << 'EOF'
echo "=== 服务状态 ==="
sudo systemctl status dual-agent-api --no-pager | head -5
sudo systemctl status dual-agent-celery --no-pager | head -5
sudo systemctl status dual-agent-celery-beat --no-pager | head -5
sudo systemctl status nginx --no-pager | head -5
EOF
```

## 本次更新内容

### 代码修复
1. ✅ 修复监控端点响应模型问题
2. ✅ 统一DeepSeek API Key配置到.env
3. ✅ 智能体脚本自动加载环境变量
4. ✅ 启动脚本加载环境变量

### 功能恢复
1. ✅ Celery Worker和Beat服务
2. ✅ 自动生成新话题功能
3. ✅ 消息评分功能

### 文档更新
1. ✅ API_ENDPOINTS.md - 新增监控端点文档
2. ✅ DeepSeek_API_Key配置说明.md
3. ✅ Celery服务重启_完成报告.md

## 部署后检查清单

- [ ] API健康检查通过
- [ ] 前端页面可访问
- [ ] Celery Worker运行正常
- [ ] Celery Beat运行正常
- [ ] 消息评分功能正常
- [ ] 自动生成话题功能正常
- [ ] 数据库备份已创建
- [ ] 日志文件正常输出

## 联系方式

如有问题，请检查：
1. 服务器日志文件
2. systemctl服务状态
3. .env配置文件
4. 数据库连接

---

**最后更新**: 2026-02-15
**部署脚本**: `deploy_update_to_mingkuan.sh`
