# 服务端问题诊断与修复报告

## 问题概述

用户报告：模拟智能体运行正常，但服务端的自动评分系统和 token 阈值触发总结系统出现问题。

## 诊断过程

### 1. 初步检查

**服务状态检查：**
- ✅ Redis: 正常运行 (PONG)
- ✅ PostgreSQL: 正常运行
- ✅ Celery Worker: 正常运行（6个进程）
- ❌ Celery Beat: **未运行** - 这是总结任务无法触发的根本原因

### 2. 数据库状态分析

**Topic 状态：**
```
Topic: 人工智能在医疗领域的应用前景
  ID: 715f4a98-a05f-4810-a9d1-1bca00aed833
  Status: active
  Token count: 23,453 (超过阈值 1000 的 23.2 倍！)
  Pending summary: True
  Messages: 46
  Scores: 4

Topic: 量子计算在密码学领域的影响与挑战
  ID: f99d2540-7911-4c26-9bd8-2d3a92bef5c6
  Status: active
  Token count: 9,414 (超过阈值 1000 的 9.4 倍！)
  Pending summary: True
  Messages: 22
  Scores: 0
```

**Summary Jobs 状态：**
```
Job ID: d6330b6b-98ff-4dfb-a386-4470a574910b
  Topic ID: f99d2540...
  Status: pending
  Retry count: 0

Job ID: b1d7efbc-930b-4573-95c8-efa0758a0c65
  Topic ID: 715f4a98...
  Status: pending
  Retry count: 0
```

**评分记录：**
- 数据库中有 5 条评分记录
- Worker 日志显示大量 `evaluate_message_relevance` 任务被接收
- 评分系统正常工作 ✅

### 3. 发现核心问题

**问题 1: Celery Beat 停止运行**
- Beat 进程不存在（PID 65141 已死亡）
- 导致定时任务（check_closing_timeouts）无法触发
- 总结任务无法被周期性检查和处理

**问题 2: Worker 队列配置错误**
```bash
# 检查 Redis 队列
Queue default: 0 tasks
Queue summary_jobs: 244 tasks  ← 大量积压！
Queue periodic_tasks: 744 tasks  ← 大量积压！
```

Worker 启动命令：
```bash
celery -A workers.celery_app worker --loglevel=info
```

**问题：** Worker 未指定队列，默认只监听 `default` 队列，导致 `summary_jobs` 和 `periodic_tasks` 队列中的任务无法被处理。

## 修复措施

### 1. 重启 Celery Beat

```bash
nohup celery -A workers.celery_app beat --loglevel=info > logs/beat.log 2>&1 &
```

**结果：** Beat 成功启动（PID 25077），开始发送定时任务。

### 2. 修复 Worker 队列配置

停止旧 Worker：
```bash
pkill -f "celery.*worker"
```

启动新 Worker（监听所有队列）：
```bash
nohup celery -A workers.celery_app worker --loglevel=info -Q default,summary_jobs,periodic_tasks > logs/worker.log 2>&1 &
```

**结果：** Worker 开始处理积压的 988 个任务（244 + 744）。

### 3. 更新启动脚本

修改 `start_services.sh`：
```bash
# 启动 Celery Worker (监听所有队列)
echo "启动 Celery Worker (监听 default, summary_jobs, periodic_tasks 队列)..."
nohup celery -A workers.celery_app worker --loglevel=info -Q default,summary_jobs,periodic_tasks > logs/worker.log 2>&1 &
```

## 修复结果

### 1. 队列状态

```
Queue default: 0 tasks
Queue summary_jobs: 0 tasks  ← 已清空
Queue periodic_tasks: 0 tasks  ← 已清空
```

### 2. Summary Jobs 状态

```
Job ID: d6330b6b...
  Status: done  ← 成功完成
  Retry count: 0

Job ID: b1d7efbc...
  Status: done  ← 成功完成
  Retry count: 0
```

### 3. Topics 状态

```
Topic: 人工智能在医疗领域的应用前景
  Token count: 0  ← 已重置
  Pending summary: False
  Summary length: 1020 chars  ← 已生成
  LLM suggestion: suggest_end
  End score: 85.0

Topic: 量子计算在密码学领域的影响与挑战
  Token count: 0  ← 已重置
  Pending summary: False
  Summary length: 675 chars  ← 已生成
  LLM suggestion: suggest_end
  End score: 85.0
```

### 4. 智能体状态

```
Agent-Alice (PID 17854): 运行中
Agent-Bob (PID 18113): 运行中
Total messages: 48
```

智能体继续正常对话，最新消息显示互动质量良好。

## 系统验证

### 评分系统 ✅
- 评分任务正常触发
- 数据库中有评分记录
- Worker 日志显示任务被处理

### 总结系统 ✅
- Token 阈值检测正常
- 总结任务成功创建和执行
- 总结内容已生成
- LLM 建议和评分正常

### 定时任务 ✅
- Celery Beat 正常运行
- check_closing_timeouts 每分钟触发
- Worker 正常处理定时任务

## 根本原因分析

1. **Celery Beat 意外停止**
   - 可能原因：进程崩溃、系统重启、手动停止
   - 影响：定时任务无法触发，总结系统失效

2. **Worker 队列配置不完整**
   - 原因：启动脚本未指定队列参数
   - 影响：summary_jobs 和 periodic_tasks 队列任务积压
   - 这是一个配置错误，不是运行时故障

## 预防措施

### 1. 进程监控

建议添加进程监控脚本：
```bash
#!/bin/bash
# check_services.sh

# 检查 Celery Beat
if ! pgrep -f "celery.*beat" > /dev/null; then
    echo "Celery Beat is down, restarting..."
    nohup celery -A workers.celery_app beat --loglevel=info > logs/beat.log 2>&1 &
fi

# 检查 Celery Worker
if ! pgrep -f "celery.*worker" > /dev/null; then
    echo "Celery Worker is down, restarting..."
    nohup celery -A workers.celery_app worker --loglevel=info -Q default,summary_jobs,periodic_tasks > logs/worker.log 2>&1 &
fi
```

### 2. 队列监控

添加队列长度监控：
```python
import redis
r = redis.Redis(host='localhost', port=6379, db=0)

queues = ['default', 'summary_jobs', 'periodic_tasks']
for queue in queues:
    length = r.llen(queue)
    if length > 100:
        print(f'WARNING: Queue {queue} has {length} tasks')
```

### 3. 健康检查端点

在 API 中添加健康检查：
```python
@app.get("/api/health/celery")
def celery_health():
    # 检查 Worker 和 Beat 状态
    # 检查队列长度
    # 返回健康状态
```

## 总结

**问题已完全解决：**
1. ✅ Celery Beat 已重启并正常运行
2. ✅ Worker 队列配置已修复
3. ✅ 积压的 988 个任务已处理完成
4. ✅ 评分系统正常工作
5. ✅ 总结系统正常工作
6. ✅ 智能体继续正常对话
7. ✅ 启动脚本已更新，防止问题再次发生

**关键发现：**
- 评分系统一直正常工作
- 总结系统被 Worker 队列配置错误阻塞
- Celery Beat 停止是次要问题（因为总结任务已创建，只是未被处理）

**下次启动：**
使用更新后的 `start_services.sh` 脚本，Worker 将自动监听所有队列，避免任务积压。
