# 前端显示条件说明

## 问题

用户在本地前端监控页面看不到"结束评分、LLM 建议、话题总结"这些模块。

## 原因

前端代码使用了条件判断，**只有当数据存在时才显示**：

```javascript
// LLM 建议 - 只有当 llm_suggestion 不为 null 时才显示
if (topic.llm_suggestion) {
    // 显示 LLM 建议卡片
}

// 话题摘要 - 只有当 summary 不为空字符串时才显示
if (topic.summary) {
    // 显示摘要框
}

// LLM 提示 - 只有当 llm_hint 不为 null 时才显示
if (topic.llm_hint) {
    // 显示提示信息
}
```

## 数据生成时机

这些数据是在**总结任务完成后**才会生成：

1. **触发条件**：Token 计数达到阈值（默认 8000）
2. **生成过程**：
   - 系统自动创建总结任务
   - Celery Worker 调用 DeepSeek API 生成总结
   - 更新话题的 `summary`、`llm_suggestion`、`end_score`、`llm_hint` 字段
3. **显示时机**：总结任务完成后，前端刷新即可看到

## 验证方法

### 1. 检查当前话题状态

```bash
curl http://localhost:8080/api/monitor/topic/active | python3 -m json.tool
```

查看返回数据：
- `token_count_since_summary`: 当前 Token 计数
- `summary`: 是否有摘要（空字符串表示没有）
- `llm_suggestion`: 是否有 LLM 建议（null 表示没有）
- `end_score`: 结束评分（0.0 表示还没生成）

### 2. 触发总结任务

有两种方式：

#### 方式 1：自然触发（推荐）

继续在智能体模拟器中发送消息，直到 Token 计数达到 8000：

```bash
# 查看当前 Token 计数
curl -s http://localhost:8080/api/monitor/topic/active | grep token_count_since_summary

# 计算还需要多少 tokens
# 例如：当前 7355，阈值 8000，还需要 645 tokens
# 大约需要再发送 2-3 条消息
```

#### 方式 2：手动触发（测试用）

使用提供的脚本手动触发总结：

```bash
python3 trigger_summary_manually.py
```

脚本会：
1. 获取当前活跃话题
2. 调用 DeepSeek API 生成总结
3. 更新话题数据
4. 显示生成结果

### 3. 刷新前端页面

总结生成后，刷新浏览器页面（F5 或 Ctrl+R），即可看到：

- ✅ 结束评分（数值显示）
- ✅ LLM 建议卡片（彩色边框）
- ✅ 话题摘要（橙色主题框）
- ✅ LLM 提示（黄色主题框）

## 示例对比

### 总结前（数据为空）

```json
{
  "summary": "",
  "llm_suggestion": null,
  "end_score": 0.0,
  "llm_hint": null,
  "token_count_since_summary": 7355
}
```

**前端显示**：只显示基础信息（话题标题、状态、Token 计数、结束评分 0.00）

---

### 总结后（数据已生成）

```json
{
  "summary": "对话延续生命意义的探讨，通过一系列诗意隐喻深化理解...",
  "llm_suggestion": "suggest_end",
  "end_score": 85.0,
  "llm_hint": "Consider whether the discussion has reached a natural conclusion.",
  "token_count_since_summary": 0
}
```

**前端显示**：
- ✅ 基础信息
- ✅ 结束评分：85.00
- ✅ LLM 建议：建议结束（橙色边框）
- ✅ 话题摘要：完整内容（橙色主题框）
- ✅ LLM 提示：提示信息（黄色主题框）

## 设计理由

这种条件显示的设计是合理的：

1. **避免显示空内容**：如果话题刚创建，还没有总结，显示空的摘要框会让界面看起来不完整
2. **渐进式信息展示**：随着对话进行，信息逐步丰富，用户体验更好
3. **减少视觉噪音**：只显示有意义的信息，避免空白占位符

## 常见问题

### Q1: 为什么我的话题一直没有生成总结？

**A**: 检查以下几点：
1. Token 计数是否达到阈值（默认 8000）
2. Celery Worker 是否正常运行
3. DeepSeek API Key 是否配置正确
4. 查看日志是否有错误信息

### Q2: 如何修改总结阈值？

**A**: 在 `.env` 文件中设置：
```bash
SUMMARY_THRESHOLD=5000  # 修改为 5000 tokens
```

### Q3: 总结任务失败了怎么办？

**A**: 
1. 查看 Celery Worker 日志
2. 检查 DeepSeek API 配置
3. 使用 `trigger_summary_manually.py` 手动重试
4. 查看数据库中的 `summary_jobs` 表，检查任务状态

### Q4: 可以强制显示这些模块吗（即使数据为空）？

**A**: 可以修改前端代码，移除条件判断，但不推荐：

```javascript
// 不推荐：总是显示，即使数据为空
html += `
    <div class="summary-box">
        <div class="summary-title">话题摘要</div>
        <div class="summary-content">
            ${topic.summary || '暂无摘要，等待生成...'}
        </div>
    </div>
`;
```

推荐保持当前设计，只在有数据时显示。

## 总结

- ✅ 前端代码逻辑正确，使用条件判断避免显示空内容
- ✅ 数据在总结任务完成后生成
- ✅ 使用 `trigger_summary_manually.py` 可以手动触发总结
- ✅ 刷新页面即可看到新生成的数据

这是正常的设计行为，不是 bug。
