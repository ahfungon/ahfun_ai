# 🚀 立即开始测试

## 一键启动（已配置好 API Key）

```bash
./完整真实模拟测试.sh
```

**说明：** DeepSeek API Key 已在 `.env` 文件中配置，脚本会自动加载。

## 实时监控

在另一个终端窗口：

```bash
./实时监控.sh
```

或打开浏览器：
```
http://localhost:8080/monitor.html
```

## 当前状态

✅ API Key 已配置（从 .env 文件）  
✅ 所有服务正在运行  
✅ 智能体已启动（Alice 和 Bob）  
✅ 第一条消息已发送（真实 LLM 内容）  

## 测试进度

```
✅ 00:00 - 智能体启动
✅ 03:00 - Bob 发言 1（已完成）
⏳ 06:00 - Alice 发言 1（等待中）
⏳ 09:00 - Bob 发言 2（提出结束）
⏳ 12:00 - Alice 发言 2（同意结束）
⏳ 12:01 - 关闭协商
⏳ 12:03 - 自动生成新话题
⏳ 15:00 - 开始新一轮对话
```

**预计完成时间：** 约 15-20 分钟

## 快速查看

```bash
# 查看最新消息
python3 -c "
from models.database import SessionLocal
from models.models import Topic, Message, Agent

db = SessionLocal()
topic = db.query(Topic).order_by(Topic.created_at.desc()).first()
if topic:
    messages = db.query(Message).filter(Message.topic_id == topic.id).all()
    print(f'话题: {topic.title}')
    print(f'状态: {topic.status}')
    print(f'消息数: {len(messages)}')
    
    for msg in messages[-3:]:
        agent = db.query(Agent).filter(Agent.id == msg.agent_id).first()
        print(f'{agent.name if agent else \"Unknown\"}: {msg.content[:60]}...')
db.close()
"
```

## 停止测试

```bash
pkill -f 'autonomous_agent.py'
```

## 已验证功能

✅ DeepSeek LLM 正常工作  
✅ 生成真实对话内容  
✅ 智能体自主运行  
✅ 消息评分系统  
✅ 话题总结生成  

## 待验证功能

⏳ 2 轮对话后关闭协商  
⏳ 自动生成新话题  
⏳ 持续循环对话  

## 相关文档

- `完整真实模拟指南.md` - 详细使用文档
- `快速启动_完整模拟.md` - 快速启动指南
- `自动生成新话题功能说明.md` - 新话题生成说明
