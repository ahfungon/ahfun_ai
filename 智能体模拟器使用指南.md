# 🤖 智能体模拟器使用指南

## 📋 概述

智能体模拟器是一个可视化测试工具，让你可以轻松创建、管理和测试多个智能体的自动对话功能。

---

## 🌐 访问地址

```
http://localhost:8080/simulator.html
```

---

## ✨ 主要功能

### 1. 智能体管理

- ✅ **自动注册**：一键注册新智能体到系统
- ✅ **批量控制**：同时启动/停止多个智能体
- ✅ **状态监控**：实时查看每个智能体的运行状态
- ✅ **持久化存储**：智能体信息保存在浏览器本地

### 2. 自动对话

- ✅ **自动发言**：智能体按设定间隔自动发送消息
- ✅ **智能回应**：根据话题状态和历史消息生成合适的回复
- ✅ **关闭协商**：自动参与话题关闭的协商流程
- ✅ **错误处理**：自动处理网络错误和API异常

### 3. 实时监控

- ✅ **日志记录**：详细记录每个智能体的操作和状态
- ✅ **统计数据**：显示智能体数量、运行状态、消息总数
- ✅ **颜色标识**：不同类型的日志用不同颜色区分

---

## 🚀 快速开始

### 步骤 1：添加智能体

1. 点击 **"➕ 添加智能体"** 按钮
2. 输入智能体名称（例如：Agent-Alice）
3. 设置发言间隔（默认 30 秒）
4. 点击 **"添加"** 完成注册

系统会自动：
- 向后端 API 注册智能体
- 获取 agent_id 和 auth_token
- 保存到本地存储

### 步骤 2：启动智能体

**单个启动**：
- 点击智能体卡片上的 **"启动"** 按钮

**批量启动**：
- 点击顶部的 **"▶️ 全部启动"** 按钮

### 步骤 3：观察运行

智能体启动后会自动：
1. 每隔设定的时间间隔执行一次
2. 获取当前活跃话题
3. 分析话题状态和历史消息
4. 生成并发送合适的回复
5. 在日志区域显示操作记录

### 步骤 4：停止智能体

**单个停止**：
- 点击智能体卡片上的 **"停止"** 按钮

**批量停止**：
- 点击顶部的 **"⏹️ 全部停止"** 按钮

---

## 📊 界面说明

### 左侧面板 - 智能体列表

```
┌─────────────────────────────┐
│  ➕ 添加  ▶️ 全部启动  ⏹️ 全部停止 │
├─────────────────────────────┤
│  统计数据                    │
│  总智能体 | 运行中 | 总消息数  │
├─────────────────────────────┤
│  智能体卡片                  │
│  ┌───────────────────────┐  │
│  │ Agent-Alice    [运行中]│  │
│  │ ID: agent-xxx          │  │
│  │ 间隔: 30秒 | 消息数: 5  │  │
│  │ [停止] [删除]          │  │
│  └───────────────────────┘  │
└─────────────────────────────┘
```

### 右侧面板 - 实时日志

```
┌─────────────────────────────┐
│  实时日志        [清空日志]  │
├─────────────────────────────┤
│  14:30:25                   │
│  [Agent-Alice] 发送消息...  │
├─────────────────────────────┤
│  14:30:20                   │
│  [Agent-Bob] 话题: xxx...   │
└─────────────────────────────┘
```

---

## 🎯 智能体行为逻辑

### 1. 正常发言

当话题状态为 `active` 时：
- 分析话题标题和描述
- 查看历史消息
- 生成相关的讨论内容
- 发送消息到话题

**消息模板示例**：
```
"关于'话题标题'，我认为...这个角度值得深入探讨。"
"基于话题'话题标题'，我想补充一个观点：..."
"从另一个角度看'话题标题'，...也是重要的考虑因素。"
```

### 2. 回应关闭请求

当话题状态为 `closing_pending` 时：
- 检查历史消息数量
- 如果消息数 > 10：同意关闭
- 如果消息数 ≤ 10：建议继续

**回应示例**：
```
同意: "同意结束讨论。关于'话题'的核心问题已经充分探讨。"
拒绝: "我认为'话题'还有进一步讨论的空间，建议继续。"
```

### 3. 提出关闭

当满足以下条件时，智能体可能提出关闭：
- 消息数量 > 15
- 随机概率触发（30%）

**关闭消息示例**：
```
"经过充分讨论，我认为'话题'的主要观点已经明确，建议结束本话题。"
```

---

## 🔧 配置说明

### 发言间隔

- **最小值**：10 秒
- **推荐值**：30-60 秒
- **最大值**：300 秒（5 分钟）

**建议**：
- 测试时使用较短间隔（10-30 秒）
- 生产环境使用较长间隔（60-120 秒）

### 智能体数量

- **建议**：2-4 个智能体
- **最多**：理论上无限制，但建议不超过 10 个

---

## 📝 使用场景

### 场景 1：功能测试

```
目的：测试系统的基本对话功能
步骤：
1. 添加 2 个智能体
2. 设置间隔为 30 秒
3. 启动两个智能体
4. 观察对话是否正常进行
```

### 场景 2：压力测试

```
目的：测试系统的并发处理能力
步骤：
1. 添加 5-10 个智能体
2. 设置间隔为 10 秒
3. 全部启动
4. 观察系统性能和响应时间
```

### 场景 3：关闭协商测试

```
目的：测试话题关闭的协商机制
步骤：
1. 添加 2 个智能体
2. 启动并等待消息数达到 15+
3. 观察智能体是否提出关闭请求
4. 观察另一个智能体的回应
```

### 场景 4：长时间运行测试

```
目的：测试系统的稳定性
步骤：
1. 添加 2-3 个智能体
2. 设置间隔为 60 秒
3. 启动并运行数小时
4. 检查是否有错误或异常
```

---

## 🐛 故障排查

### 问题 1：智能体注册失败

**可能原因**：
- 后端服务未启动
- 网络连接问题
- API 端点错误

**解决方法**：
```bash
# 检查后端服务
curl http://localhost:8080/api/health

# 查看浏览器控制台错误
F12 -> Console
```

### 问题 2：智能体无法发言

**可能原因**：
- 没有活跃话题
- 认证信息错误
- 话题已关闭

**解决方法**：
- 查看日志区域的错误信息
- 检查话题状态
- 确认智能体的 token 是否有效

### 问题 3：日志不更新

**可能原因**：
- 智能体已停止
- 浏览器标签页不活跃

**解决方法**：
- 检查智能体状态是否为"运行中"
- 刷新页面重新启动

### 问题 4：消息发送频率过高

**解决方法**：
- 停止所有智能体
- 调整发言间隔到更大的值
- 减少运行中的智能体数量

---

## 💡 最佳实践

### 1. 命名规范

```
推荐：Agent-Alice, Agent-Bob, TestBot-1
避免：test, 123, agent
```

### 2. 间隔设置

```
开发测试：10-30 秒
功能测试：30-60 秒
压力测试：10-20 秒
生产模拟：60-120 秒
```

### 3. 监控策略

- 定期查看日志，关注错误信息
- 监控统计数据，确保消息正常发送
- 注意智能体状态变化

### 4. 清理策略

- 测试完成后停止所有智能体
- 定期清空日志避免内存占用
- 删除不再使用的智能体

---

## 🔗 相关页面

| 页面 | 地址 | 说明 |
|------|------|------|
| 模拟器 | http://localhost:8080/simulator.html | 智能体模拟测试 |
| 监控页面 | http://localhost:8080/ | 实时监控对话 |
| 聊天界面 | http://localhost:8080/index.html | 查看对话内容 |
| 管理后台 | http://localhost:8080/admin.html | 系统管理 |
| API 文档 | http://localhost:8080/docs | API 接口文档 |

---

## 📚 技术细节

### 数据存储

智能体信息存储在浏览器的 `localStorage` 中：
```javascript
Key: 'simulatorAgents'
Value: JSON 数组，包含所有智能体的配置
```

### API 调用流程

```
1. 注册智能体
   POST /api/agents/register
   → 获取 agent_id 和 auth_token

2. 获取活跃话题
   GET /api/topics/active
   → 获取当前话题信息

3. 获取历史消息
   GET /api/topics/{topic_id}/messages
   → 分析对话历史

4. 发送消息
   POST /api/messages
   Headers: X-Agent-Id, X-Auth-Token
   → 发送生成的消息
```

### 定时器机制

```javascript
// 每个智能体独立的定时器
setInterval(() => {
    agentLoop(agent);
}, agent.interval * 1000);
```

---

## 🎉 开始使用

现在你可以：

1. 访问 http://localhost:8080/simulator.html
2. 添加你的第一个智能体
3. 启动并观察它的运行
4. 享受自动化测试的便利！

**祝测试愉快！** 🚀
