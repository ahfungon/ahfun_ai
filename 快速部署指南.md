# 快速部署指南

## 部署方式对比

### 1. 完整部署 (deploy_update_to_mingkuan.sh)

**适用场景**:
- 首次部署
- 修改了依赖 (requirements.txt)
- 修改了数据库结构
- 修改了配置文件 (.env)
- 修改了前端文件
- 需要备份数据库

**执行时间**: ~30-60秒

**命令**:
```bash
./deploy_update_to_mingkuan.sh
```

**流程**:
1. 同步所有代码文件
2. 更新 .env 配置
3. 更新 Python 依赖
4. 检查 Redis 和 PostgreSQL
5. 备份数据库
6. 停止所有服务
7. 启动所有服务
8. 验证部署

---

### 2. 快速更新 (quick_update.sh) ⚡

**适用场景**:
- 只修改了 Python 代码文件
- 不涉及依赖变化
- 不涉及数据库变化
- 不涉及配置变化

**执行时间**: ~5-10秒

**命令**:
```bash
# 更新指定文件
./quick_update.sh services/summary_service.py

# 更新多个文件
./quick_update.sh services/summary_service.py workers/tasks.py

# 更新所有 Python 文件
./quick_update.sh
```

**流程**:
1. 同步指定的代码文件（或所有 .py 文件）
2. 重启 API 和 Celery Worker 服务
3. 完成

---

## 常见场景示例

### 场景 1: 修改了摘要提示词

```bash
# 1. 修改代码
vim services/summary_service.py

# 2. 测试
python3 -m py_compile services/summary_service.py

# 3. 提交
git add services/summary_service.py
git commit -m "修改摘要提示词"
git push

# 4. 快速部署
./quick_update.sh services/summary_service.py
```

### 场景 2: 修改了多个服务文件

```bash
# 快速部署多个文件
./quick_update.sh \
    services/summary_service.py \
    services/message_scoring_service.py \
    workers/tasks.py
```

### 场景 3: 修改了依赖或配置

```bash
# 必须使用完整部署
./deploy_update_to_mingkuan.sh
```

### 场景 4: 只修改了前端

```bash
# 使用前端部署脚本
./deploy_frontend_update.sh
```

---

## 验证部署

### 检查服务状态

```bash
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 \
    'systemctl status dual-agent-api dual-agent-celery'
```

### 查看实时日志

```bash
# API 日志
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 \
    'journalctl -u dual-agent-api -f'

# Celery Worker 日志
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 \
    'journalctl -u dual-agent-celery -f'
```

### 测试 API

```bash
curl http://129.211.28.211:8080/api/health
```

### 访问监控页面

http://129.211.28.211:8080/monitor.html

---

## 部署脚本对比表

| 特性 | 完整部署 | 快速更新 | 前端部署 |
|------|---------|---------|---------|
| 脚本 | deploy_update_to_mingkuan.sh | quick_update.sh | deploy_frontend_update.sh |
| 速度 | 慢 (30-60s) | 快 (5-10s) | 中 (10-20s) |
| 同步代码 | ✅ 全部 | ✅ 指定文件 | ❌ |
| 同步前端 | ✅ | ❌ | ✅ |
| 更新依赖 | ✅ | ❌ | ❌ |
| 更新配置 | ✅ | ❌ | ❌ |
| 备份数据库 | ✅ | ❌ | ❌ |
| 重启服务 | ✅ 全部 | ✅ API+Worker | ✅ Nginx |
| 验证部署 | ✅ | ❌ | ✅ |

---

## 最佳实践

1. **开发阶段**: 使用 `quick_update.sh` 快速迭代
2. **测试阶段**: 使用完整部署确保环境一致
3. **生产发布**: 使用完整部署并做好备份
4. **紧急修复**: 使用 `quick_update.sh` 快速修复

---

## 回滚操作

如果部署后发现问题，可以快速回滚：

```bash
# 1. 本地回退到上一个版本
git log --oneline -5  # 查看提交历史
git checkout <commit-hash> services/summary_service.py

# 2. 快速部署回滚的版本
./quick_update.sh services/summary_service.py

# 3. 或者使用完整部署
./deploy_update_to_mingkuan.sh
```

---

## 注意事项

⚠️ **快速更新的限制**:
- 不会更新 Python 依赖
- 不会更新配置文件
- 不会备份数据库
- 不会更新前端文件
- 不会验证部署结果

⚠️ **何时必须使用完整部署**:
- 修改了 requirements.txt
- 修改了 .env 配置
- 修改了数据库模型
- 添加了新的依赖包
- 修改了前端文件
- 需要数据库备份

---

## 故障排查

### 服务启动失败

```bash
# 查看详细错误日志
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 \
    'journalctl -u dual-agent-api -n 50 --no-pager'
```

### 代码未生效

```bash
# 检查文件是否同步成功
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 \
    'ls -lh ~/dual-agent-chat/services/summary_service.py'

# 检查文件内容
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 \
    'grep "你是一个对话总结助手" ~/dual-agent-chat/services/summary_service.py'
```

### 服务未重启

```bash
# 手动重启服务
ssh -i ~/.ssh/mingkuan.pem ubuntu@129.211.28.211 \
    'sudo systemctl restart dual-agent-api dual-agent-celery'
```
