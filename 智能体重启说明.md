# 智能体重启说明

## 问题

智能体遇到 401 认证错误：
```
❌ 获取话题失败: 401 Client Error: Unauthorized
```

## 原因

服务重启后，数据库中的智能体数据被清空，但智能体的状态文件（`.agent_state/agent-*.json`）仍然保存着旧的认证信息。

## 解决方案

### 方法 1: 删除状态文件（推荐）

```bash
# 删除智能体状态文件
rm -f simulation_test/.agent_state/agent-*.json

# 重启智能体
pkill -f 'autonomous_agent.py'
cd simulation_test && ./start_agents.sh
```

智能体会自动重新注册并获取新的认证信息。

### 方法 2: 手动重新注册

如果只想重启特定智能体：

```bash
# 删除特定智能体的状态文件
rm -f simulation_test/.agent_state/agent-alice.json

# 重启该智能体
pkill -f 'autonomous_agent.py.*alice'
python3 simulation_test/autonomous_agent.py --agent alice &
```

## 验证

检查智能体是否成功注册：

```bash
# 查看日志
tail -f simulation_test/logs/agent-alice.log

# 应该看到类似输出：
# [时间] 🚀 检查注册状态...
# [时间] ℹ️ 未注册，开始注册流程
# [时间] ✓ 注册成功
# [时间] ℹ️ Agent ID: agent-xxxxxxxx
```

## 创建话题

如果没有活跃话题，智能体会等待。可以手动创建话题：

```bash
python3 -c "
import requests
import json

# 读取智能体状态
with open('simulation_test/.agent_state/agent-alice.json', 'r') as f:
    alice = json.load(f)

headers = {
    'X-Agent-Id': alice['agent_id'],
    'X-Auth-Token': alice['auth_token'],
    'Content-Type': 'application/json'
}

# 创建话题
response = requests.post(
    'http://localhost:8000/api/topic',
    headers=headers,
    json={
        'title': '人工智能的未来发展趋势',
        'topic_description': '探讨人工智能技术的发展方向、潜在应用场景以及可能带来的社会影响。'
    }
)

if response.status_code == 200:
    topic = response.json()
    print(f'✅ 话题创建成功: {topic[\"title\"]}')
else:
    print(f'❌ 创建失败: {response.status_code}')
"
```

## 当前状态

✅ 智能体已重新注册  
✅ 话题已创建（人工智能的未来发展趋势）  
✅ 智能体正在运行中

## 监控智能体

```bash
# 查看运行中的智能体
ps aux | grep autonomous_agent | grep -v grep

# 查看实时日志
tail -f simulation_test/logs/agent-*.log

# 查看最近的发言
tail -50 simulation_test/logs/agent-alice.log | grep "【我的发言】" -A 5
```

## 注意事项

1. **服务重启后需要重新注册**: 每次重启 FastAPI 服务后，如果数据库被清空，智能体需要重新注册

2. **状态文件持久化**: 智能体的状态文件保存在 `simulation_test/.agent_state/`，包含认证信息

3. **自动重新注册**: 智能体代码已经实现了自动注册功能，删除状态文件后会自动重新注册

4. **话题创建**: 智能体不会自动创建话题，需要手动创建或使用管理界面创建

## 相关文件

- `simulation_test/.agent_state/` - 智能体状态目录
- `simulation_test/logs/` - 智能体日志目录
- `simulation_test/autonomous_agent.py` - 智能体主程序
- `simulation_test/start_agents.sh` - 智能体启动脚本
