# 编辑按钮位置说明

## 📍 按钮位置

编辑按钮位于智能体卡片的底部，具体位置如下：

```
┌─────────────────────────────────────────┐
│  智能体名称                    [待机]    │  ← 顶部：名称和状态
│  ─────────────────────────────────────  │
│  ID: agent-abc123                       │  ← 中部：ID和统计信息
│  间隔: 30秒 | 消息数: 0                 │
│  ─────────────────────────────────────  │
│                                         │
│  [启动]  [编辑]  [删除]                │  ← 底部：操作按钮
│   绿色    蓝色    灰色                  │
└─────────────────────────────────────────┘
```

## 🎨 按钮样式

- **启动按钮**：绿色背景，文字"启动"
- **编辑按钮**：蓝色背景，文字"编辑" ← 这是新增的按钮
- **删除按钮**：灰色背景，文字"删除"

## 🔍 如何找到编辑按钮

### 步骤1：打开模拟器页面

**本地环境：**
```
http://localhost:8080/simulator.html
```

**生产环境：**
```
http://129.211.28.211:8080/simulator.html
```

### 步骤2：查看智能体列表

在页面左侧的"智能体列表"区域，你会看到所有已注册的智能体。

### 步骤3：定位编辑按钮

每个智能体卡片的底部有三个按钮：
1. 第一个按钮：启动/停止（绿色或红色）
2. **第二个按钮：编辑（蓝色）** ← 这就是编辑按钮
3. 第三个按钮：删除（灰色）

## 🖱️ 点击编辑按钮后

点击"编辑"按钮后，会弹出一个编辑窗口，包含：

```
┌─────────────────────────────────────┐
│  编辑智能体                    [×]  │
│  ─────────────────────────────────  │
│                                     │
│  智能体名称：                       │
│  [当前名称________________]         │
│                                     │
│  系统提示词：                       │
│  [当前系统提示词___________]        │
│  [_________________________]        │
│  [_________________________]        │
│                                     │
│         [取消]      [保存]          │
└─────────────────────────────────────┘
```

## ⚠️ 重要提示

### 如果看不到编辑按钮，请尝试：

1. **强制刷新页面**
   - Windows/Linux: `Ctrl + Shift + R`
   - macOS: `Cmd + Shift + R`

2. **清除浏览器缓存**
   - 按 F12 打开开发者工具
   - 右键点击刷新按钮
   - 选择"清空缓存并硬性重新加载"

3. **检查浏览器控制台**
   - 按 F12 打开开发者工具
   - 切换到 Console 标签
   - 查看是否有 JavaScript 错误

4. **使用无痕模式**
   - 在浏览器的无痕/隐私模式下打开页面
   - 这样可以避免缓存问题

## 📸 视觉参考

### 智能体卡片完整示例

```
┌─────────────────────────────────────────┐
│  技术顾问                      [待机]    │
│  ─────────────────────────────────────  │
│  ID: agent-abc123                       │
│  间隔: 30秒 | 消息数: 15                │
│  ─────────────────────────────────────  │
│                                         │
│  [启动]  [编辑]  [删除]                │
│   ↑       ↑       ↑                    │
│   绿色    蓝色    灰色                  │
│          这是编辑按钮！                 │
└─────────────────────────────────────────┘
```

### 运行中的智能体

```
┌─────────────────────────────────────────┐
│  技术顾问                    [运行中]    │
│  ─────────────────────────────────────  │
│  ID: agent-abc123                       │
│  间隔: 30秒 | 消息数: 15                │
│  ─────────────────────────────────────  │
│                                         │
│  [停止]  [编辑]  [删除]                │
│   ↑       ↑       ↑                    │
│   红色    蓝色    灰色                  │
│          这是编辑按钮！                 │
└─────────────────────────────────────────┘
```

## 🎯 快速测试

### 测试编辑功能是否正常

1. 打开模拟器页面
2. 如果没有智能体，先点击"添加智能体"创建一个
3. 在智能体卡片底部找到蓝色的"编辑"按钮
4. 点击"编辑"按钮
5. 应该会弹出编辑窗口
6. 修改名称或系统提示词
7. 点击"保存"
8. 查看智能体卡片是否更新

## 📞 仍然找不到？

如果按照以上步骤仍然找不到编辑按钮，请提供以下信息：

1. 使用的浏览器和版本（例如：Chrome 120）
2. 访问的URL（本地还是生产环境）
3. 页面截图
4. 浏览器控制台的错误信息（F12 → Console）

这样我可以帮你进一步诊断问题。
