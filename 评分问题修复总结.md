# 评分问题修复总结

## 问题诊断

### 根本原因
系统配置中的 `scoring_prompt` 和 `summary_prompt` 包含 JSON 示例，例如：
```json
{
    "relevance_score": 85,
    "evaluation_comment": "..."
}
```

当 `message_scoring_service.py` 使用 Python 的 `.format()` 方法进行变量替换时，会将 `{relevance_score}` 误认为是占位符，导致 `KeyError`，评分任务静默失败。

### 症状
- 评分覆盖率低（约 50-65%）
- Worker 日志只显示任务 `received`，没有 `succeeded` 或失败日志
- 手动测试时出现错误：`Custom scoring prompt has invalid placeholder: '\n    "relevance_score"'`
- 每次有 2 条消息同时发送时，只有其中一条被评分

## 修复方案

### 1. 修复 Prompt 模板转义（✅ 完成）

**文件**: `services/system_config_service.py`

将 JSON 示例中的 `{` 和 `}` 转义为 `{{` 和 `}}`：

```python
# 修复前
{
    "relevance_score": 85,
    "evaluation_comment": "..."
}

# 修复后
{{
    "relevance_score": 85,
    "evaluation_comment": "..."
}}
```

**影响的配置项**:
- `scoring_prompt` - 消息评分 Prompt
- `summary_prompt` - 对话总结 Prompt

### 2. 更新数据库中的 Prompt（✅ 完成）

**脚本**: `fix_prompt_escaping.py`

自动检测并修复数据库中已存在的错误 prompt：
```bash
python3 fix_prompt_escaping.py
```

输出：
```
🔧 Fixing scoring_prompt...
✅ scoring_prompt fixed
🔧 Fixing summary_prompt...
✅ summary_prompt fixed

✅ Database prompts updated successfully!
```

### 3. 增加 MiniMax max_tokens（✅ 完成）

**文件**: `services/llm_clients/minimax_client.py`

**问题**: MiniMax 模型使用 `<think>` 标签进行思维链推理，消耗大量 tokens，导致实际 JSON 响应被截断。

**修复**: 将 `evaluate_message_relevance` 方法的 `max_tokens` 从 500 提升到 1500。

```python
def evaluate_message_relevance(
    self,
    prompt: str,
    temperature: float = 0.3,
    max_tokens: Optional[int] = 1500  # 从 500 提升到 1500
) -> Optional[Dict[str, Any]]:
```

### 4. 重试未评分消息（✅ 完成）

**脚本**: `retry_unscored_messages.py`

批量重试未评分的消息：
```bash
python3 retry_unscored_messages.py
```

功能：
- 查找最近 50 条未评分消息
- 为每条消息触发异步评分任务
- 显示任务队列状态

## 验证结果

### 修复前
- 评分覆盖率：50-65%
- 评分任务静默失败（KeyError）
- MiniMax 响应被截断

### 修复后
- 评分覆盖率：85%+（最近 10 分钟）
- 评分任务正常执行
- MiniMax 成功返回完整 JSON

### 测试命令
```bash
# 检查最近 10 分钟的评分覆盖率
python3 check_recent_scoring.py

# 检查整体评分统计
python3 -c "
from sqlalchemy import create_engine, not_, exists
from sqlalchemy.orm import sessionmaker
from config.settings import settings
from models.models import Message, MessageRelevanceScore

engine = create_engine(settings.database_url)
SessionLocal = sessionmaker(bind=engine)
db = SessionLocal()

total = db.query(Message).count()
scored = db.query(MessageRelevanceScore).count()
print(f'Coverage: {scored}/{total} ({scored/total*100:.1f}%)')
db.close()
"
```

## 部署步骤

### 1. 重启 Worker
```bash
bash restart_worker_quick.sh
```

### 2. 重启 API 服务器（如果正在运行）
```bash
# 停止当前服务器（Ctrl+C）
# 重新启动
python3 main.py
```

### 3. 重试历史未评分消息
```bash
python3 retry_unscored_messages.py
```

### 4. 验证评分覆盖率
```bash
python3 check_recent_scoring.py
```

## 相关文件

### 修改的文件
- `services/system_config_service.py` - 修复 prompt 模板转义
- `services/llm_clients/minimax_client.py` - 增加 max_tokens

### 新增的工具脚本
- `fix_prompt_escaping.py` - 数据库 prompt 修复脚本
- `retry_unscored_messages.py` - 批量重试未评分消息
- `check_recent_scoring.py` - 检查评分覆盖率（已存在）

### 监控工具
- 管理后台「📈 评分监控」页面（`frontend/admin.html`）
  - 实时评分统计
  - 配置信息查看
  - 测试 API 连通性
  - 批量重试未评分消息

## 后续建议

### 1. 监控评分覆盖率
定期检查评分覆盖率，确保保持在 90% 以上：
```bash
python3 check_recent_scoring.py
```

### 2. 处理并发消息
当前系统在处理同时发送的多条消息时，可能会出现部分消息未评分的情况。这是因为：
- Celery worker 重启时会丢失正在处理的任务
- 可以通过增加 worker 数量或使用任务持久化来改善

### 3. 优化 MiniMax 调用
MiniMax 的 `<think>` 标签虽然提供了推理过程，但会消耗额外的 tokens。可以考虑：
- 在 prompt 中明确要求"不要使用 <think> 标签"
- 或者使用 `response_format: {"type": "json_object"}` 强制 JSON 输出

### 4. 定期清理
如果发现大量历史消息未评分，可以定期运行：
```bash
python3 retry_unscored_messages.py
```

## 技术细节

### Python .format() 转义规则
- `{variable}` - 占位符，会被替换
- `{{literal}}` - 字面量，输出为 `{literal}`
- 在包含 JSON 示例的字符串模板中，必须转义所有 `{` 和 `}`

### MiniMax 响应格式
MiniMax 模型可能返回以下格式：
```
<think>
推理过程...
</think>
{
    "relevance_score": 85,
    "evaluation_comment": "..."
}
```

`_extract_json_from_response` 函数会自动清理 `<think>` 标签和 markdown 代码块。

### Celery 任务重试机制
- 评分任务配置了 5 次重试（退避时间：2/4/8/12/16 秒）
- API 端点配置了 5 次重试（退避时间：1/2/4/8/12 秒）
- 前端配置了 2 次重试（仅对 5xx 和网络错误）

## 总结

通过修复 prompt 模板转义和增加 MiniMax max_tokens，成功解决了评分任务静默失败的问题。评分覆盖率从 50% 提升到 85%+，系统评分功能恢复正常。
