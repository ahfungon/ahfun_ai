# 快速启动 - 完整真实模拟

## 一键启动（3 步）

### 1. 设置 DeepSeek API Key

```bash
export DEEPSEEK_API_KEY="your-api-key-here"
```

获取 API Key: https://platform.deepseek.com/

### 2. 启动测试

```bash
./完整真实模拟测试.sh
```

### 3. 监控进度

在另一个终端窗口：

```bash
./实时监控.sh
```

或打开浏览器：
```
http://localhost:8080/monitor.html
```

## 测试内容

✅ 2 个智能体（Alice 和 Bob）  
✅ 使用 DeepSeek LLM 生成真实对话  
✅ 每人发言 2 次后协商关闭话题  
✅ 系统自动生成新话题  
✅ 持续循环对话  

## 预计时间

- 第 1 轮对话: 0-6 分钟
- 第 2 轮对话: 6-12 分钟
- 关闭协商: 12-15 分钟
- 新话题生成: 15-16 分钟
- **总计: 约 15-20 分钟**

## 停止测试

```bash
pkill -f 'autonomous_agent.py'
```

## 详细文档

查看 `完整真实模拟指南.md` 了解：
- 详细流程说明
- 监控方法
- 故障排查
- 验证清单

## 快速验证

```bash
# 查看话题和消息
python3 -c "
from models.database import SessionLocal
from models.models import Topic, Message

db = SessionLocal()
topic = db.query(Topic).order_by(Topic.created_at.desc()).first()
if topic:
    messages = db.query(Message).filter(Message.topic_id == topic.id).count()
    print(f'话题: {topic.title}')
    print(f'状态: {topic.status}')
    print(f'消息数: {messages}')
db.close()
"
```

## 成功标志

- ✅ 消息内容真实（不是"我认为这个话题很有意义"）
- ✅ 第 2 轮对话包含"可以结束了"
- ✅ 话题成功关闭
- ✅ 新话题自动生成
- ✅ 智能体继续对话

## 需要帮助？

- 查看日志: `tail -f simulation_test/logs/agent-alice.log`
- 运行诊断: `./诊断智能体状态.sh`
- 查看指南: `完整真实模拟指南.md`
