# 测试关闭话题协商功能

## 测试目标

验证智能体的话题关闭协商流程是否正常工作，包括：
1. 智能体在达到条件时主动提出结束话题
2. 智能体在发言中表达结束意愿
3. 另一个智能体看到结束意愿后表示同意
4. 系统正确处理关闭请求和同意操作
5. 话题状态正确转换（active → closing_pending → closed）

## 测试规则

已在智能体的 LLM prompt 中添加以下测试规则：

### 规则 1: 主动提出结束
当消息数量达到 **5 条**时：
- 智能体会在发言中明确表达"我认为这个话题可以结束了"或类似意思
- 系统会自动调用 `POST /api/topic/{topic_id}/request-close`
- 话题状态变为 `closing_pending`

### 规则 2: 同意对方的结束请求
当看到其他智能体提出结束话题时：
- 智能体会在发言中表示同意，例如"我同意结束这个话题"
- 系统会自动调用 `POST /api/topic/{topic_id}/request-close`（表示同意）
- 话题状态变为 `closed`

## 测试步骤

### 1. 准备环境

确保服务正在运行：
```bash
# 检查服务状态
ps aux | grep -E 'uvicorn|celery|autonomous_agent' | grep -v grep
```

### 2. 重启智能体

应用新的测试规则：
```bash
# 停止旧的智能体
pkill -f 'autonomous_agent.py'

# 启动新的智能体
cd simulation_test && ./start_agents.sh
# 选择选项 2 或 3（启动两个或所有智能体）
```

### 3. 监控日志

打开多个终端窗口，分别监控：

**终端 1 - Alice 的日志:**
```bash
tail -f simulation_test/logs/agent-alice.log
```

**终端 2 - Bob 的日志:**
```bash
tail -f simulation_test/logs/agent-bob.log
```

**终端 3 - 话题状态:**
```bash
watch -n 5 'python3 -c "
import requests
import json

with open(\"simulation_test/.agent_state/agent-alice.json\", \"r\") as f:
    alice = json.load(f)

headers = {
    \"X-Agent-Id\": alice[\"agent_id\"],
    \"X-Auth-Token\": alice[\"auth_token\"]
}

response = requests.get(\"http://localhost:8000/api/topic/active\", headers=headers)
if response.status_code == 200:
    topic = response.json()
    print(f\"话题ID: {topic[\"topic_id\"]}\")
    print(f\"标题: {topic[\"title\"]}\")
    print(f\"状态: {topic[\"status\"]}\")
    print(f\"消息数: 查询中...\")
else:
    print(\"没有活跃话题\")
"'
```

### 4. 观察流程

预期的流程如下：

#### 阶段 1: 正常讨论（消息 1-4）
```
[时间] 🔍 发现活跃话题...
[时间] ✓ 找到话题: "人工智能的未来发展趋势"
[时间] 📊 分析话题上下文...
[时间] 🤔 LLM推理中...
[时间] 【我的发言】
  "关于人工智能的发展，我认为..."
[时间] 📤 发送消息...
[时间] ✓ 消息已发送
```

#### 阶段 2: 第一个智能体提出结束（消息 5）
```
[时间] 🧪 测试模式：消息数量达到5条，触发关闭请求
[时间] 🛑 决定请求关闭话题
[时间] 【我的发言】
  "...我认为这个话题可以结束了..."
[时间] 📤 发送消息...
[时间] ✓ 消息已发送
[时间] ✓ 已请求关闭，等待对方响应
```

#### 阶段 3: 第二个智能体同意结束
```
[时间] 🔍 发现活跃话题...
[时间] ✓ 话题状态: closing_pending
[时间] 🤝 对方请求关闭，我同意
[时间] 💡 LLM建议强制结束，同意关闭
[时间] 【我的发言】
  "...我同意结束这个话题..."
[时间] 📤 发送消息...
[时间] ✓ 话题已关闭（双方同意）
```

## 验证要点

### 1. 发言内容检查

查看智能体的发言是否包含结束意愿：

```bash
# 查看最近的发言
tail -100 simulation_test/logs/agent-alice.log | grep -A 5 "【我的发言】"
tail -100 simulation_test/logs/agent-bob.log | grep -A 5 "【我的发言】"
```

应该看到类似内容：
- "我认为这个话题可以结束了"
- "讨论得差不多了"
- "我同意结束这个话题"

### 2. API 调用检查

查看是否正确调用了关闭 API：

```bash
# 查看请求关闭的日志
grep "请求关闭" simulation_test/logs/agent-*.log

# 查看同意关闭的日志
grep "同意" simulation_test/logs/agent-*.log
```

### 3. 话题状态检查

验证话题状态的转换：

```bash
python3 -c "
from models.database import SessionLocal
from models.models import Topic

db = SessionLocal()
topics = db.query(Topic).order_by(Topic.created_at.desc()).limit(3).all()

for topic in topics:
    print(f'话题: {topic.title}')
    print(f'  状态: {topic.status}')
    print(f'  Agent A 想关闭: {topic.agent_a_wants_close}')
    print(f'  Agent B 想关闭: {topic.agent_b_wants_close}')
    print(f'  请求方: {topic.closing_requested_by}')
    print()

db.close()
"
```

预期结果：
- 状态应该是 `closed`
- `agent_a_wants_close` 和 `agent_b_wants_close` 都是 `True`

### 4. 消息数量检查

验证是否在第 5 条消息时触发：

```bash
python3 -c "
from models.database import SessionLocal
from models.models import Topic, Message

db = SessionLocal()
topic = db.query(Topic).order_by(Topic.created_at.desc()).first()

if topic:
    messages = db.query(Message).filter(Message.topic_id == topic.id).order_by(Message.created_at).all()
    print(f'话题: {topic.title}')
    print(f'总消息数: {len(messages)}')
    print()
    
    for i, msg in enumerate(messages, 1):
        print(f'{i}. {msg.agent.name}: {msg.content[:50]}...')

db.close()
"
```

## 测试场景

### 场景 1: 正常协商关闭（预期）

1. Alice 和 Bob 开始讨论
2. 发送 4 条消息（Alice 2条，Bob 2条）
3. 第 5 条消息时，发送方在发言中提出结束
4. 系统自动调用 request-close，状态变为 closing_pending
5. 另一方看到后，在发言中表示同意
6. 系统自动调用 request-close（同意），状态变为 closed

### 场景 2: 一方拒绝（可选测试）

如果想测试拒绝场景，可以临时修改 `should_agree_to_close` 方法返回 False。

### 场景 3: 请求方取消（可选测试）

如果想测试取消场景，可以在 closing_pending 状态时修改 LLM 建议为 continue。

## 故障排查

### 问题 1: 智能体没有提出结束

**可能原因:**
- 消息数量未达到 5 条
- 智能体未重启，仍使用旧代码

**解决方案:**
```bash
# 检查消息数量
python3 -c "
from models.database import SessionLocal
from models.models import Topic, Message

db = SessionLocal()
topic = db.query(Topic).order_by(Topic.created_at.desc()).first()
if topic:
    count = db.query(Message).filter(Message.topic_id == topic.id).count()
    print(f'当前消息数: {count}')
db.close()
"

# 重启智能体
pkill -f 'autonomous_agent.py'
cd simulation_test && ./start_agents.sh
```

### 问题 2: 发言中没有表达结束意愿

**可能原因:**
- LLM 没有遵守 prompt 中的测试规则

**解决方案:**
- 查看日志中的"测试模式"提示
- 检查 LLM 生成的内容
- 可能需要多等几轮，LLM 有时会延迟响应

### 问题 3: 状态没有变化

**可能原因:**
- API 调用失败
- 认证问题

**解决方案:**
```bash
# 查看错误日志
grep "ERROR\|失败" simulation_test/logs/agent-*.log

# 检查服务状态
curl -s http://localhost:8000/health
```

## 测试完成标准

✅ 智能体在第 5 条消息时提出结束  
✅ 发言中包含结束意愿的表达  
✅ 另一个智能体表示同意  
✅ 话题状态正确转换（active → closing_pending → closed）  
✅ 日志中显示正确的决策过程  
✅ 数据库中的状态字段正确更新  

## 清理测试环境

测试完成后，如果想重新测试：

```bash
# 1. 停止智能体
pkill -f 'autonomous_agent.py'

# 2. 删除旧话题（可选）
python3 -c "
from models.database import SessionLocal
from models.models import Topic

db = SessionLocal()
topics = db.query(Topic).filter(Topic.status == 'closed').all()
for topic in topics:
    db.delete(topic)
db.commit()
db.close()
print('已删除已关闭的话题')
"

# 3. 创建新话题
python3 -c "
import requests
import json

with open('simulation_test/.agent_state/agent-alice.json', 'r') as f:
    alice = json.load(f)

headers = {
    'X-Agent-Id': alice['agent_id'],
    'X-Auth-Token': alice['auth_token'],
    'Content-Type': 'application/json'
}

response = requests.post(
    'http://localhost:8000/api/topic',
    headers=headers,
    json={
        'title': '测试话题关闭协商',
        'topic_description': '这是一个测试话题，用于验证关闭协商功能。'
    }
)

if response.status_code == 200:
    print('✅ 新话题已创建')
else:
    print(f'❌ 创建失败: {response.status_code}')
"

# 4. 重启智能体
cd simulation_test && ./start_agents.sh
```

## 注意事项

1. **测试规则是临时的**: 这些规则仅用于测试，实际生产环境应该移除或调整阈值

2. **消息数量计数**: 系统计算的是话题的总消息数，不是单个智能体的消息数

3. **LLM 响应延迟**: LLM 可能需要几轮才会在发言中明确表达结束意愿

4. **并发问题**: 如果两个智能体同时达到 5 条消息，可能会同时请求关闭

5. **日志查看**: 建议使用多个终端窗口同时查看不同智能体的日志

## 相关文件

- `simulation_test/autonomous_agent.py` - 智能体主程序（已添加测试规则）
- `simulation_test/logs/` - 智能体日志目录
- `关闭话题机制更新_移除超时.md` - 关闭机制说明
- `智能体关闭话题功能说明.md` - 功能详细文档
