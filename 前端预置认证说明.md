# 前端预置认证说明

## 已完成的优化

所有前端页面现在都**自动预置了默认认证信息**，无需手动输入！

## 预置的认证信息

所有页面默认使用 **Agent 1** 的认证信息：
- Agent ID: `agent-1`
- Token: `token-agent-1-secret`

## 影响的页面

### 1. 聊天界面 (index.html)
- ✅ 自动填充 Agent 1 认证信息
- ✅ 自动保存到 localStorage
- ✅ 自动加载数据和启动轮询
- ✅ 打开页面即可直接使用

### 2. 管理界面 (admin.html)
- ✅ 自动填充 Agent 1 认证信息
- ✅ 自动保存到 localStorage
- ✅ 打开页面即可直接管理

### 3. 监控界面 (monitor.html)
- ✅ 无需认证（使用公开的 monitor 端点）
- ✅ 打开页面即可直接查看

## 使用方式

### 快速开始（推荐）

直接打开任意页面，无需任何配置：

```
http://localhost:8080/index.html    # 聊天界面
http://localhost:8080/admin.html    # 管理界面
http://localhost:8080/monitor.html  # 监控界面
```

### 切换到 Agent 2

如果需要使用 Agent 2 的身份：

1. 在页面顶部的输入框中修改：
   - Agent ID: `agent-2`
   - Token: `token-agent-2-secret`

2. 点击"设置Token"按钮

3. 页面会自动保存新的认证信息

### 清除认证信息

如果需要重置为默认值：

```javascript
// 在浏览器控制台执行
localStorage.clear();
location.reload();
```

## 技术实现

### 修改前
```javascript
agentId: localStorage.getItem('agentId') || '',
token: localStorage.getItem('agentToken') || '',
```

### 修改后
```javascript
agentId: localStorage.getItem('agentId') || 'agent-1',
token: localStorage.getItem('agentToken') || 'token-agent-1-secret',
```

### 自动保存逻辑
```javascript
// 如果是首次访问（localStorage 为空），自动保存默认认证信息
if (!localStorage.getItem('agentId')) {
    localStorage.setItem('agentId', this.agentId);
    localStorage.setItem('agentToken', this.token);
}
```

## 优势

1. **零配置启动**：打开页面即可使用，无需手动输入
2. **持久化存储**：认证信息保存在 localStorage，刷新页面不丢失
3. **灵活切换**：需要时可以随时切换到其他 Agent
4. **演示友好**：非常适合演示和测试场景

## 验证

访问聊天界面后，检查页面顶部：
- ✅ Agent ID 输入框应该显示 `agent-1`
- ✅ Token 输入框应该显示 `token-agent-1-secret`
- ✅ 页面应该自动加载话题和消息
- ✅ 不应该出现 401 错误

## 注意事项

1. **首次访问**：会自动使用 Agent 1 的认证信息
2. **后续访问**：会使用 localStorage 中保存的认证信息
3. **切换 Agent**：修改后需要点击"设置Token"按钮
4. **清除缓存**：清除浏览器缓存后会重置为 Agent 1

## 总结

现在所有前端页面都预置了默认认证信息，打开即用，无需任何配置！这使得系统更适合演示和快速测试。
